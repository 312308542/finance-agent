# 项目稳定运行差距与决策清单

> 最后核对：2026-07-16
> 用途：由项目负责人填写“用户备注/决策”和“期望时间”；Codex 后续以本文档为规划入口，按依赖顺序执行已明确授权且当前可执行的事项。
> 状态约定：`待决策`、`外部阻塞`、`待执行`、`执行中`、`已完成`、`接受缺口`、`暂缓`。
> 进度约定：`0%`（未开始）、`25%`（设计完成）、`50%`（实现中）、`75%`（待真实验收）、`100%`（验收完成）。

## 1. 当前可用基线

| 能力 | 当前状态 | 真实证据 |
| --- | --- | --- |
| API 与数据库 | 可用 | API 监听 `127.0.0.1:8000`；`GET /api/health` 返回 API、数据库均为 `ok` |
| A 股推荐就绪度 | 可用 | `recommendation_readiness.status=ready`、`executable=true`、无 warning |
| 后端回归 | 通过 | 2026-07-16 全量 `728 passed` |
| 前端生产构建 | 通过 | TypeScript 与 Vite 生产构建成功 |
| A 股行情、评分与推荐链 | 可用 | 已完成真实全市场运行，最新主板推荐链可执行 |
| 用户闭环 | 可用 | 推荐确认、订单草案、外部执行登记、持仓、复盘、Finance Memory 已完成真实验收 |
| 项目运行期调度 | 可用-观察中 | `personal-ashare` 常驻 loop 已安全重启并加载最新代码；状态文件为 `state=running`，逻辑 PID `45756`，到期任务已真实执行 |
| 无人值守运行 | 不可宣称 | Windows 计划任务未注册；当前手工进程不能证明开机自启或异常退出自动恢复 |

## 2. P0：稳定运行前必须处理

| 编号 | 清单项 | 当前真实状态 | 默认建议 | 验收标准 | 用户备注/决策（请填写） | 期望时间 | 状态 | 进度 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RUN-001 | 注册 Windows API 计划任务 | `FinanceAgent-Api` 不存在；当前 API 为手工进程；根 Compose 的 PostgreSQL/Redis 已应用 `restart: unless-stopped` 并保持 healthy | 继续手工启动 API；无人值守需求恢复后再注册计划任务 | 开机自启；异常退出后自动恢复；API/数据库健康均为 `ok` | 暂时不用，我自己会手动启用；不过项目的中间件`docker-compose`需要帮忙改成开机自启 |  | 暂缓 | 0% |
| RUN-002 | 注册基础数据调度器计划任务 | `FinanceAgent-BaseDataScheduler` 不存在；当前调度器为手工常驻 loop，逻辑 PID `45756`；默认运行范围已由 RUN-003 收口 | 项目运行期间手工启动；无人值守/开机自恢复需要时再注册 | 状态心跳持续更新；到期任务执行；异常退出后自动恢复 | 暂缓，仅在无人值守/开机自恢复需要时执行 |  | 暂缓 | 0% |
| RUN-003 | 确定生产调度任务范围 | `personal-ashare` 已成为核心配置、CLI、API 和 Web 默认值；按北向披露边界收口后，真实计划 36 项、启用 32 项、crypto 任务 0；dry-run `completed`、无错误 | 已完成；crypto 继续使用独立 `crypto-comprehensive` 显式配置 | 启用任务清单经过负责人确认；dry-run 只出现获批任务；不触发暂缓能力 | 按建议执行 |  | 已完成 | 100% |
| RUN-004 | 整机重启与自动恢复验收 | 尚未获得整机重启授权 | RUN-001、RUN-002 完成后安排维护窗口重启 | 重启后 API、调度器、数据库连接、状态心跳和关键只读接口全部恢复 | 暂不验收 |  | 待决策 | 0% |
| MODEL-001 | 恢复 GPT-5.6-sol 高风险复核 | 真实端点 HTTP 200、路由 ready；复用原隔离事件且输入哈希不变，得到真实 reject；决策 `rejected_by_review`、订单草案 0、待复核队列 0 | 已完成；旧 403 unavailable 结果继续保留审计 | 真实 reject 写入审计链；被 reject 的卖出/换股建议不能生成订单草案 | 按建议执行 |  | 已完成 | 100% |
| OBS-001 | 恢复调度器运行态监控 | `personal-ashare` 常驻 loop 已于 2026-07-16 再次安全重启，逻辑 PID `45756`；36 项/启用 32 项。状态心跳、运行中/排队任务和 JSONL 事件持续更新；旧新闻正文子进程保留自然收尾 | 保持项目运行期调度，继续观察完整交易日；不注册 Windows 计划任务 | 心跳不超过 300 秒；运行中、排队、失败任务可观测；陈旧状态能告警 | 按建议执行 |  | 执行中 | 75% |

## 3. P1：影响推荐质量与长期稳定性

| 编号 | 清单项 | 当前真实状态 | 默认建议 | 验收标准 | 用户备注/决策（请填写） | 期望时间 | 状态 | 进度 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DATA-001 | 财务/估值新鲜度口径与覆盖 | 已拆分季度财报与日级估值：当前应有报告期 `2026-03-31`，主板财报达标 `3,215/3,482`、缺口 267；当日估值达标 `3,192/3,482`，旧估值 12、无估值 278。3 只固定样本 raw PE/PB 与标准表一致，因子同时保留 spot 与历史来源 | 已完成；继续保留结构性缺口，不写 0、不造数；估值当前截面随 `ashare.realtime_quotes` 更新，财报补采按报告期选择 | `reporting_period` 不再受 72 小时误判；估值使用交易日 freshness；来源可追溯；推荐 readiness 保持 `ready/executable=true` | 按建议执行 |  | 已完成 | 100% |
| DATA-002 | 个股资金流与北向覆盖 | 已修复同花顺深市代码前导零丢失：常驻调度器最新真实截面为 `3,191/3,194` 个当前可交易主板标的，覆盖率 99.91%；3 个缺口均为无行情、无 K 线的财报源占位资产。北向逐股数据停在 2024-08-16 与监管披露调整一致，自 2024-08-19 起改为季度第 5 个交易日披露上季度末单只证券持有量 | 个股资金流继续每 30 分钟刷新；保留北向历史事实，移除默认日度轮询。现有 AKShare 季度接口真实返回空对象，可靠季度源出现前不进入当前评分或动作 | 最新个股资金流覆盖当前可交易主板 ≥ 85%；固定深沪样本可追溯；北向旧数据不伪装新鲜，默认计划无 `ashare.northbound` 日度任务 | 需要先查清楚是数据源的问题还是什么原因导致无法同步 |  | 已完成 | 100% |
| DATA-003 | 行业/概念成员覆盖 | 行业 `727/3,482`，概念 `1,090/3,482`；部分成员接口返回 401 | 优先接入完整成员源或取得接口权限；现有首屏 fallback 只作降级 | 主板板块归属达到确认阈值；源失败和 fallback 覆盖差异有报告 | 需要先查清楚是数据源的问题还是什么原因导致无法同步 |  | 外部阻塞 | 0% |
| DATA-004 | 新闻/公告重点池连续覆盖 | 已完成单日[真实报文核验](新闻重点池报文核验记录.md)：200 只中近 7 日覆盖 170 只；30 只均返回 10 条历史新闻，源端空列表/过滤为空均为 0；8 只新闻连接超时经真实重试全部恢复；未来时间戳 0 | 保持连续观察到一周验收，不把真实无新闻误报为失败，也不补造新闻 | 每日任务稳定运行；无新闻不造数；超时可重试；发布时间无未来时间戳 | 这个可以看看报文返回的是什么内容 |  | 执行中 | 75% |
| SCORE-001 | 题材/价值权重最终校准 | 真实题材因子样本仅 79 个，目前只有参考分布 | DATA-003 达标后重跑全市场截面，再固化权重 | 固化分位线和融合权重；价值/题材/混合画像结果可解释且可复现 | 按建议执行 |  | 依赖阻塞 | 0% |
| OPS-001 | 完整交易日连续运行观察 | 常驻 loop 已于 2026-07-15 手工启动；Codex 一次性自动任务“finance-agent 一周稳定性验收”已安排在 2026-07-22 20:00（北京时间） | 保持当前项目运行；到期检查 API、心跳、任务错误/堆积、Docker、数据 freshness 和推荐 readiness | 无进程退出；任务无异常堆积；心跳、数据 freshness、推荐 readiness 持续达标 | 按建议进行，做个一次性的定时任务在一周后执行 | 2026-07-22 20:00 | 待执行 | 25% |

## 4. P2：质量增强与人工取舍

| 编号 | 清单项 | 当前真实状态 | 默认建议 | 验收标准 | 用户备注/决策（请填写） | 期望时间 | 状态 | 进度 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FUND-001 | 开放式基金结构性缺口 | 20,604 只中，9 只无历史、32 只缺中间年份，例外率约 0.20% | 在“接第二数据源”和“接受 41 只例外”之间人工选择 | 若补源则完成真实复采；若接受则更新正式口径并保留例外清单 | 缺少一些数据可以理解，只要最近的数据不缺失即可 |  | 待决策 | 0% |
| METHOD-001 | structural-lite 真实校准与事件研究 | 方案 22 T11/T14 明确暂缓；结构证据不进评分和动作 | 负责人在场时完成黄金样本及信号后 5/10/20 日事件研究 | 参数有真实分布依据；信号价值结论落档；再决定是否向风险提示升级 | 解禁并执行。首轮仅使用 A 股主板日 K，研究 structural-lite 各类信号出现后 5/10/20 个交易日的绝对收益和相对主板基准超额收益。必须严格使用当时可见数据，划分样本内与样本外区间，并统计样本数、胜率、中位收益、超额收益和最大不利波动。只有样本外结果稳定、至少两个周期具有正向信息量时，才提出接入方案；首轮只允许作为风险过滤候选，不直接修改评分、推荐动作或交易逻辑。 |  | 待执行 | 0% |
| METHOD-002 | 缠论 `czsc` 生产依赖 | 已有适配器接口和 fail-closed 门控，但未批准安装依赖 | 先确认版本、许可证和维护状态，再决定是否接入 | 固定 K 线样本稳定；结果入库；圆桌观点可追溯；无引擎时继续拒绝运行 | 批准研究试用，不批准直接生产接入。待 METHOD-001 的统一事件研究框架完成后，确认 `czsc` 版本、许可证和依赖兼容性，并在隔离研究环境接入真实引擎。使用与 structural-lite 完全相同的 A 股主板日 K、5/10/20 日窗口和样本外评估协议进行对照。只有 `czsc` 相对基准和 structural-lite 提供稳定的增量效果时，才提交生产接入提案；否则继续保持 fail-closed，不启用缠论能力。(使用以前的历史数据进行回测判断，看成功率) |  | 依赖 METHOD-001 | 0% |
| SEC-001 | API 远程访问方案 | 当前无认证，仅安全绑定本机；本机使用不受影响 | 没有远程需求则保持本机；需要远程访问时另建认证/TLS/反向代理方案 | 未认证端口不暴露；远程入口具备身份认证、TLS 和访问审计 | 这个暂缓，不需要完善 |  | 暂缓 | 0% |

## 5. 全局约束与负责人备注

| 决策项 | 可填写示例 | 用户备注/决策（请填写） |
| --- | --- | --- |
| 本轮最高优先级编号 | `RUN-003、RUN-001、RUN-002` | 都要做 |
| 是否允许提权 PowerShell | `允许；由我手工打开提权终端` / `暂不允许` | 允许 |
| 是否允许安排整机重启 | `允许，时间为……` / `暂不允许` | 暂不允许 |
| crypto 调度策略 | `全部禁用` / `拆成独立配置` / `继续启用` | 拆成独立配置，默认不启用 |
| GPT-5.5 额度处理 | `已补充，可重试` / `继续停用` / `更换模型` | 已补充，可重试，换成了gpt-5.6-sol |
| 外部数据源预算与权限 | `允许新增免费源` / `允许采购` / `只接受现有缺口` | 允许新增免费源 |
| 基金 41 只例外处理 | `接受缺口` / `接入第二数据源` | 接受缺口 |
| structural-lite T11 是否解禁 | `执行` / `继续暂缓` | 执行 |
| `czsc` 依赖决策 | `批准调研并试装` / `继续 fail-closed` | 批准调研并试装 |
| API 是否需要远程访问 | `仅本机` / `局域网` / `公网` | 暂缓远程访问，继续绑定 `127.0.0.1` |
| 其他范围、成本或时间限制 | 自由填写 |  |

## 6. 备注转下一步计划的规则

1. Codex 只推进已在“用户备注/决策”中明确授权，且依赖已满足的事项。
2. 未填写、标记为“暂缓”或仍需密钥/权限的事项继续保留，不伪造完成。
3. 默认依赖顺序为：`RUN-003 → RUN-001/RUN-002 → OBS-001 → RUN-004 → OPS-001`。
4. 数据质量顺序为：`DATA-001/DATA-002/DATA-003/DATA-004 → SCORE-001`。
5. 功能或修复继续遵循 TDD、PyCharm 符号/调用影响分析、真实验证、中文文档同步和提交前变更审计。
6. 下一轮规划将列出：目标、依赖、风险、执行步骤、验证命令、需要负责人参与的检查点和预期提交边界。
