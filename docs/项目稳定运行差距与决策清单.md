# 项目稳定运行差距与决策清单

> 最后核对：2026-07-15
> 用途：由项目负责人填写“用户备注/决策”和“期望时间”；Codex 后续以本文档为规划入口，按依赖顺序执行已明确授权且当前可执行的事项。
> 状态约定：`待决策`、`外部阻塞`、`待执行`、`执行中`、`已完成`、`接受缺口`、`暂缓`。

## 1. 当前可用基线

| 能力 | 当前状态 | 真实证据 |
| --- | --- | --- |
| API 与数据库 | 可用 | API 监听 `127.0.0.1:8000`；`GET /api/health` 返回 API、数据库均为 `ok` |
| A 股推荐就绪度 | 可用 | `recommendation_readiness.status=ready`、`executable=true`、无 warning |
| 后端回归 | 通过 | 全量 `700 passed` |
| 前端生产构建 | 通过 | TypeScript 与 Vite 生产构建成功 |
| A 股行情、评分与推荐链 | 可用 | 已完成真实全市场运行，最新主板推荐链可执行 |
| 用户闭环 | 可用 | 推荐确认、订单草案、外部执行登记、持仓、复盘、Finance Memory 已完成真实验收 |
| 无人值守运行 | 不可宣称 | Windows 计划任务未注册，调度器当前没有存活进程 |

## 2. P0：稳定运行前必须处理

| 编号 | 清单项 | 当前真实状态 | 默认建议 | 验收标准 | 用户备注/决策（请填写） | 期望时间 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RUN-001 | 注册 Windows API 计划任务 | `FinanceAgent-Api` 不存在；当前 API 为手工进程 | 在提权 PowerShell 注册，保持 `127.0.0.1` 监听和失败重启 | 开机自启；异常退出后自动恢复；API/数据库健康均为 `ok` |  |  | 待决策 |
| RUN-002 | 注册基础数据调度器计划任务 | `FinanceAgent-BaseDataScheduler` 不存在；调度器进程数为 0 | 先完成 RUN-003，再在提权 PowerShell 注册 | 状态心跳持续更新；到期任务执行；异常退出后自动恢复 |  |  | 待决策 |
| RUN-003 | 确定生产调度任务范围 | 46 个任务中启用 42 个；包含 5 个暂缓 crypto 任务和 GPT-5.5 高风险复核 sweep | A 股优先配置中禁用 crypto；额度恢复前禁用复核 sweep，另保留可恢复配置 | 启用任务清单经过负责人确认；dry-run 只出现获批任务；不触发暂缓能力 |  |  | 待决策 |
| RUN-004 | 整机重启与自动恢复验收 | 尚未获得整机重启授权 | RUN-001、RUN-002 完成后安排维护窗口重启 | 重启后 API、调度器、数据库连接、状态心跳和关键只读接口全部恢复 |  |  | 待决策 |
| MODEL-001 | 恢复 GPT-5.5 高风险复核 | 真实请求返回 HTTP 403 `insufficient_user_quota`；安全降级已通过 | 补充额度后复用既有隔离事件重跑，不新造用户交易事实 | 真实 reject 写入审计链；被 reject 的卖出/换股建议不能生成订单草案 |  |  | 外部阻塞 |
| OBS-001 | 恢复调度器运行态监控 | `runtime/base_data_scheduler/status.json` 是旧 dry-run 完成记录 | 随 RUN-002 启动常驻调度器，接入现有任务监控页 | 心跳不超过 300 秒；运行中、排队、失败任务可观测；陈旧状态能告警 |  |  | 待执行 |

## 3. P1：影响推荐质量与长期稳定性

| 编号 | 清单项 | 当前真实状态 | 默认建议 | 验收标准 | 用户备注/决策（请填写） | 期望时间 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DATA-001 | 财务/估值新鲜度口径与覆盖 | 3,482 只主板股票有历史快照，但 72 小时内新鲜数据仅 1 只 | 改为财报周期感知的 freshness 口径，再按季度数据源补采 | 健康检查不把正常季度数据误判过期；真实结构性缺口单独列出 |  |  | 待决策 |
| DATA-002 | 个股资金流与北向覆盖 | 普通资金流约 `1,696/3,482`，48.7%；北向最新业务日为 2024-08-16 | 调研第二数据源或正式下调覆盖目标，禁止重复调度伪装补齐 | 达到负责人确认的覆盖线；缺失标的、数据日期和来源限制可追溯 |  |  | 外部阻塞 |
| DATA-003 | 行业/概念成员覆盖 | 行业 `727/3,482`，概念 `1,090/3,482`；部分成员接口返回 401 | 优先接入完整成员源或取得接口权限；现有首屏 fallback 只作降级 | 主板板块归属达到确认阈值；源失败和 fallback 覆盖差异有报告 |  |  | 外部阻塞 |
| DATA-004 | 新闻/公告重点池连续覆盖 | 200 只重点池近 7 日新闻覆盖 169 只；其余 31 只采集成功但无近期新闻 | 连续观察多个交易日，区分“无新闻”和“采集失败” | 每日任务稳定运行；无新闻不造数；超时可重试；发布时间无未来时间戳 |  |  | 待执行 |
| SCORE-001 | 题材/价值权重最终校准 | 真实题材因子样本仅 79 个，目前只有参考分布 | DATA-003 达标后重跑全市场截面，再固化权重 | 固化分位线和融合权重；价值/题材/混合画像结果可解释且可复现 |  |  | 依赖阻塞 |
| OPS-001 | 完整交易日连续运行观察 | 已完成单次和分批真实验收，尚未在正式常驻任务下观察完整交易日 | 计划任务上线后观察至少一个完整交易日，建议连续 3 个交易日 | 无进程退出；任务无异常堆积；心跳、数据 freshness、推荐 readiness 持续达标 |  |  | 依赖阻塞 |

## 4. P2：质量增强与人工取舍

| 编号 | 清单项 | 当前真实状态 | 默认建议 | 验收标准 | 用户备注/决策（请填写） | 期望时间 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| FUND-001 | 开放式基金结构性缺口 | 20,604 只中，9 只无历史、32 只缺中间年份，例外率约 0.20% | 在“接第二数据源”和“接受 41 只例外”之间人工选择 | 若补源则完成真实复采；若接受则更新正式口径并保留例外清单 |  |  | 待决策 |
| METHOD-001 | structural-lite 真实校准与事件研究 | 方案 22 T11/T14 明确暂缓；结构证据不进评分和动作 | 负责人在场时完成黄金样本及信号后 5/10/20 日事件研究 | 参数有真实分布依据；信号价值结论落档；再决定是否向风险提示升级 |  |  | 暂缓 |
| METHOD-002 | 缠论 `czsc` 生产依赖 | 已有适配器接口和 fail-closed 门控，但未批准安装依赖 | 先确认版本、许可证和维护状态，再决定是否接入 | 固定 K 线样本稳定；结果入库；圆桌观点可追溯；无引擎时继续拒绝运行 |  |  | 待决策 |
| SEC-001 | API 远程访问方案 | 当前无认证，仅安全绑定本机；本机使用不受影响 | 没有远程需求则保持本机；需要远程访问时另建认证/TLS/反向代理方案 | 未认证端口不暴露；远程入口具备身份认证、TLS 和访问审计 |  |  | 待决策 |

## 5. 全局约束与负责人备注

| 决策项 | 可填写示例 | 用户备注/决策（请填写） |
| --- | --- | --- |
| 本轮最高优先级编号 | `RUN-003、RUN-001、RUN-002` |  |
| 是否允许提权 PowerShell | `允许；由我手工打开提权终端` / `暂不允许` |  |
| 是否允许安排整机重启 | `允许，时间为……` / `暂不允许` |  |
| crypto 调度策略 | `全部禁用` / `拆成独立配置` / `继续启用` |  |
| GPT-5.5 额度处理 | `已补充，可重试` / `继续停用` / `更换模型` |  |
| 外部数据源预算与权限 | `允许新增免费源` / `允许采购` / `只接受现有缺口` |  |
| 基金 41 只例外处理 | `接受缺口` / `接入第二数据源` |  |
| structural-lite T11 是否解禁 | `执行` / `继续暂缓` |  |
| `czsc` 依赖决策 | `批准调研并试装` / `继续 fail-closed` |  |
| API 是否需要远程访问 | `仅本机` / `局域网` / `公网` |  |
| 其他范围、成本或时间限制 | 自由填写 |  |

## 6. 备注转下一步计划的规则

1. Codex 只推进已在“用户备注/决策”中明确授权，且依赖已满足的事项。
2. 未填写、标记为“暂缓”或仍需密钥/权限的事项继续保留，不伪造完成。
3. 默认依赖顺序为：`RUN-003 → RUN-001/RUN-002 → OBS-001 → RUN-004 → OPS-001`。
4. 数据质量顺序为：`DATA-001/DATA-002/DATA-003/DATA-004 → SCORE-001`。
5. 功能或修复继续遵循 TDD、PyCharm 符号/调用影响分析、真实验证、中文文档同步和提交前变更审计。
6. 下一轮规划将列出：目标、依赖、风险、执行步骤、验证命令、需要负责人参与的检查点和预期提交边界。
