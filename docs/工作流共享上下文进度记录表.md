# 工作流共享上下文进度记录表

更新时间：2026-05-20

本文档用于跟踪 `finance-agent` 的 Workflow 共享上下文改造进度。范围只包含上下文分层、角色视图、记忆注入、Prompt Registry 和审计回放，不重复记录数据采集、因子、评分、推荐、触发或前端任务。

## 1. 总体进度

| 模块 | 当前状态 | 验收标准 | 备注 |
| --- | --- | --- | --- |
| 统一上下文分层 | 已完成基础版 | 所有 Workflow 可以通过同一份 `ContextEnvelope` 携带 stable / context / volatile 三层上下文 | 已落地运行时对象 |
| 角色视图裁剪 | 已完成基础版 | 每个角色只看到自己需要的字段，不直接吃全量上下文 | 已覆盖 5 个圆桌角色 |
| 记忆按需注入 | 已完成基础版 | Finance Memory 只进入 volatile 层，并按标的聚合摘要 | 精准向量召回仍待接入 |
| A 股 / 数字货币分链路 | 已完成基础版 | 上下文显式携带 `market_type`，A 股和数字货币不共用同一候选池语义 | 已支持 state / profile / recommendation 推断 |
| Prompt Registry | 已完成基础版 | 稳定层、上下文层、波动层和角色 Prompt 集中管理 | 已提供 prompt bundle |
| 审计可回放 | 已完成基础版 | Workflow 运行审计能回放上下文摘要、市场链路和角色视图数量 | 已写入 `agent_workflow_runs.payload` |

## 2. 任务总表

| 编号 | 任务 | 状态 | 优先级 | 产物 |
| --- | --- | --- | --- | --- |
| C1 | 定义共享上下文对象 | 已完成基础版 | P0 | `src/finance_agent/agents/runtime/context_envelope.py` |
| C2 | 抽离 Prompt Registry | 已完成基础版 | P0 | `src/finance_agent/agents/runtime/prompts.py` |
| C3 | Workflow 角色接入 | 已完成基础版 | P0 | `langgraph_graphs.py` 已在圆桌节点写入 `context_envelope` |
| C4 | 记忆注入策略 | 已完成基础版 | P0 | volatile 层汇总 `memory_summary`、`risk_summary` |
| C5 | 审计增强 | 已完成基础版 | P1 | `LangGraphWorkflowAdapter` 已归档完整 envelope 与摘要 |
| C6 | 冒烟验证 | 已完成基础版 | P1 | `smoke_workflow_context_envelope.py`、`smoke_langgraph_workflow_adapter.py` |

## 3. 已完成内容

| 项目 | 完成情况 | 说明 |
| --- | --- | --- |
| `ContextEnvelope` | 已完成 | 包含 `version`、`workflow_type`、`market_type`、`stable`、`context`、`volatile`、`role_views`、`audit` |
| `RoleView` | 已完成 | 表示单个 Workflow 角色可见字段、角色 Prompt 片段和资产范围 |
| `market_type` 推断 | 已完成 | 优先读取 state 显式字段，其次资产 profile，最后推荐结果 market |
| 角色视图 | 已完成 | 覆盖 `technical_analyst`、`factor_analyst`、`risk_rebuttal`、`portfolio_manager`、`memory_manager` |
| Prompt Bundle | 已完成 | 支持总调度、DeepSeek 主分析、GPT-5.5 高风险复核和圆桌角色 Prompt |
| 审计落库 | 已完成 | `agent_workflow_runs.payload.context_envelope` 保存完整上下文，`context_envelope_summary` 保存轻量摘要 |

## 4. 待增强内容

| 编号 | 任务 | 优先级 | 建议下一步 |
| --- | --- | --- | --- |
| N1 | 接入真实模型 Planner | P0 | 让 planner 使用 `build_prompt_bundle()` 组装模型输入，并允许模型按需调用工具、Workflow、图谱记忆 |
| N2 | 精准记忆召回 | P0 | 在构建 envelope 前接 `MemoryService` / `MemoryRepository` / GraphStore，按 owner、market、asset、workflow、risk 过滤 |
| N3 | Scheduler 常驻轮询 | P1 | 把触发事件生成、派发和内部 Agent Loop 运行串成长期服务化进程 |
| N4 | 审计事件细化 | P1 | 在 `agent_workflow_events` 中增加 `context_envelope_summary` 节点事件，便于前端单独展示上下文快照 |
| N5 | 测试扩展 | P1 | 补充 A 股 / 数字货币 market_type 分链路、推荐决策上下文、角色可见字段的专门测试 |

## 5. 验证记录

| 日期 | 命令 | 结果 |
| --- | --- | --- |
| 2026-05-20 | `.\.venv\Scripts\python.exe scripts\storage\smoke_workflow_context_envelope.py` | 通过 |
| 2026-05-20 | `.\.venv\Scripts\python.exe scripts\storage\smoke_langgraph_workflow_adapter.py` | 通过 |
| 2026-05-20 | `.\.venv\Scripts\python.exe scripts\storage\smoke_langgraph_workflow_builders.py` | 通过 |
| 2026-05-20 | `.\.venv\Scripts\python.exe -m py_compile ...` | 通过 |

## 6. 交接说明

当前改造已经把共享上下文从“设计文档”推进到“Workflow 状态 + 审计落库”。下一步不建议继续扩散新角色，而应该优先把 `build_prompt_bundle()` 接入真实模型 Planner，并让模型 Planner 使用同一份 `ContextEnvelope` 做工具调用、Workflow 调度和高风险复核。
