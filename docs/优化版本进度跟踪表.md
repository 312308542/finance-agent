# 优化版本进度跟踪表

更新时间：2026-07-15

本文档用于跟踪 `finance-agent` 从“基础可用”走向“私人金融助手长期运行”的优化版本任务。这里不重复记录已经完成的数据表、基础推荐链路和 CLI/MCP 入口，只跟踪当前尚未完成的 9 条主线。

## 1. 总体路线

| 阶段 | 目标 | 当前状态 | 验收标准 |
| --- | --- | --- | --- |
| V1.1 | 让 Hermes 能稳定调用金融工具 | 已完成 | Hermes/Skill 能通过 CLI 或 MCP 调用 Workflow、工具、报告和审计查询 |
| V1.2 | 让系统能被行情和风险事件自动唤起 | 调度器接入已完成 | 持仓波动、观察池触发条件、风险事件能唤醒 Hermes-Agent 或内部金融 Agent |
| V1.3 | 接入真实模型分析和高风险复核 | 主分析模型 Planner、高风险复核回写和聊天摘要沉淀开发已完成，真实复核模型联调待统一执行 | DeepSeek V4 Pro / GPT-5.5 Pro 能按路由协议输出结构化结论并回写审计 |
| V1.4 | 把持仓和观察池 Workflow 升级为圆桌 | 真实模型版开发已完成，真实 API 联调待统一执行 | `portfolio_monitoring`、`watchlist_management` 具备工具调用、模型增强圆桌观点、报告和复核 |
| V1.5 | 数据层生产化和因子增强 | 数据层 V1.5 已完成 | 批量刷新、缺口补采、交易日历、限流、完整因子窗口可稳定运行 |
| V2.0 | 私人金融助手体验闭环 | 已完成 | 前端、长期记忆、人工确认、复盘和操作反馈形成闭环 |

## 2. 优化任务总表

| 编号 | 优化项 | 状态 | 优先级 | 当前基础 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| O1 | Hermes 真正接入 | 已完成 | P0 | CLI/MCP 已完成基础入口，仓库已入库 Hermes Skill 规范副本、可见性检查脚本、CLI direct/WSL bridge 冒烟、MCP 接入模板和 MCP 握手冒烟 | 后续由 02 方案接 Scheduler/内部 Agent 常驻唤醒 |
| O2 | 实时/准实时触发 | 已完成 | P0 | `assistant_trigger_events`、`TriggerService`、CLI/MCP 触发入口、Scheduler 常驻评估、Agent 事件消费和盘中波动规则已落地 | 后续随实时行情链路稳定后启用 intraday 任务并调优阈值 |
| O3 | 内部金融 Agent Loop | 真实模型 Planner、聊天工具循环、高风险复核回写和聊天摘要沉淀开发已完成 | P0 | `src/finance_agent/agents/loop/` 已实现 LangGraph 图、模型增强 planner、runner、状态对象和 CLI；可消费 `assistant_trigger_events` 中已派发的 Agent 唤醒任务，主分析模型可按需调用只读事实、记忆和图谱工具并选择 Domain Workflow；`finance-agent chat` 已支持 PostgreSQL 聊天会话恢复和模型工具循环；模型供应商、模型实例、路由规则和检索 profile 已入库；`HighRiskReviewService`、`agent review-pending` CLI 与 `analytics.high_risk_reviews.*` 调度任务已接入，能把复核结果回写审计链和决策日志；`ChatSummaryService` 已能把长聊天压缩为会话摘要并沉淀 `chat_summary` 记忆 | 后续补真实复核模型联调、Hermes 接入和前端办公室状态页 |
| O4 | 持仓监控/观察池管理圆桌化 | 真实模型版开发已完成，真实联调待补 | P1 | 6 个 Workflow 已保留规则版 fallback，并在圆桌节点内接入真实模型观点增强；报告可展示模型/规则来源、要点、反方和缺口；反馈 API 可记录用户确认并反写 Finance Memory | 后续在统一联调阶段补真实 API key 样例，再按 07/08 接前端确认和报告展示 |
| O5 | 数据层生产化 | 主体完成，FUND-015 分批执行中 | P1 | 任务队列、水位、限流、K 线生命周期、基金市场、LOF 备源、北向/解禁/质押、调度控制和部署模板均已完成；ETF 1,507 只、LOF 390 只全历史初始化已收口，开放式基金前二十批已处理 10,000 个任务，当前事实表覆盖 10,006 只、16,319,074 行，另有 3 只终态 `unavailable` | 第二十一批运行中；同窗口 resolver 剩余 10,629 个候选继续串行，Windows 计划任务注册需提升令牌，重启验收需用户授权 |
| O6 | 因子和评分增强 | 真实主板推荐与五年回测已验收 | P1 | 指标、因子、初筛、评分、信号、回避池、多池合并、策略权重、真实性闸门、大盘环境闸门、可买入性和记忆回流均已完成；方案 10 T6 固定参数回测两次得到同一结果 `bt:b9047b4e3278fcafe0de2b6a`，CAGR 12.14%、最大回撤 -37.37%、Sharpe 0.617、Sortino 0.880，且明确 `replayed` 存在前视偏差，只作为历史模拟 | 先完成方案 15 的资金流、行业/概念、风险情绪与新闻数据刷新，再执行方案 18 T6 题材因子联合校准 |
| O7 | Finance Memory 增强 | 已完成增强闭环，投资画像内核和排序回流已接入 | P1 | 决策、入池原因、每日关注原因、复盘任务、投资画像、向量召回、用户反馈回写、复盘反写、可信度衰减和 GraphStore 查询已写入/可查；`profile.upsert` 会同步 `assistant_memories(memory_type=investment_profile)`；方案 19 已支持用记忆回流调整推荐呈现排序并写入 `memory_ranking_adjustment` 审计，不改确定性 `total_score` | 后续接前端记忆视图和真实用户反馈采集入口 |
| O8 | 前端接真实后端 | 端到端用户闭环已真实验收 | P2 | 2026-07-13 已通过浏览器验证 Workflow 报告、推荐采纳、订单草案、外部执行登记、持仓更新、执行历史和 Finance Memory；数据库从 `decision_logs` 到复盘记忆可追溯，页面明确不连接券商或交易所自动下单 | 继续补更多空态/错误态；高风险复核真实 reject 仍受模型账户额度阻塞 |
| O9 | 人工确认和交易动作闭环 | 已完成 | P2 | 已实现 `confirm_decision`、`order_drafts`、`execution_records`、`reviews_due`、前端确认/草案/执行登记/待复盘入口和端到端闭环冒烟；当前仍只生成文档性质订单草案，执行登记只接受用户外部交易后的手工填报 | 真实下单继续禁用；后续如接交易系统，必须新增权限开关、二次确认、审计和安全评审 |

> 真实模型调用仍然是 P0 能力。当前已完成模型供应商入库、模型实例入库、路由规则在线切换、检索 profile 配置、路由预览、dry-run 测试、轻量 TUI、内部 Agent Loop 主分析模型 Planner、CLI 聊天模型工具循环、高风险复核回写开发和聊天摘要沉淀服务。GPT-5.5 Pro 真实复核联调按跳过策略留到统一联调阶段补验。

## 3. O1 Hermes 真正接入

| 子任务 | 状态 | 说明 |
| --- | --- | --- |
| 编写金融助手 Skill 使用规范 | 已完成 | 已新增 `docs/集成/Hermes金融助手Skill规范.md`，以仓库版作为准源 |
| 配置 Hermes 调用 CLI | 已完成 | 已新增 `scripts/integration/check_hermes_visibility.py` 和 `smoke_hermes_skill_commands.py`，direct 模式可验证 Windows venv CLI 链路 |
| 配置 Hermes 调用 MCP | 已完成 | 已新增 `docs/集成/MCP接入模板.md` 和 `scripts/integration/smoke_mcp_handshake.py`，MCP initialize/list_tools 握手通过 |
| 明确工具权限边界 | 已完成 | 已新增 `tests/test_hermes_tool_boundary.py`，要求 Hermes 可见事实工具带 `read_only: true` 且不得出现交易/执行类工具 |
| 增加 Hermes 冒烟脚本 | 已完成 | direct 和 WSL bridge 冒烟均可列出 Workflow/工具、查看模型配置、检查图谱、消费 Agent 单轮任务、运行单标的分析并读取 Markdown 报告 |

验收标准：

- Hermes 能发现 `list_workflows`、`run_workflow`、`get_report`、`call_tool`。
- Hermes 能基于用户问题选择合适 Workflow。
- Hermes 的输出能引用 Workflow 报告和审计事件，而不是自己凭空给结论。

## 4. O2 实时/准实时触发

| 子任务 | 状态 | 说明 |
| --- | --- | --- |
| 定义触发事件模型 | 已完成基础版 | 新增 `assistant_trigger_events`，记录 `trigger_type`、`trigger_ref`、`dedup_key`、`agent_runtime`、`agent_task_id`、`requested_workflow_type`、状态和 payload |
| 持仓触发规则 | 已完成基础版 | 已支持持仓回撤 `position_drawdown`、信号转弱 `signal_flip`、高风险 `risk_event_detected` |
| 观察池触发规则 | 已完成基础版 | 已支持 `watchlist_condition_hit`，条件可读取 `signal_snapshots`、`indicator_frames`、`factor_frames` 和 `asset_scores` |
| 推荐触发规则 | 已完成基础版 | 已支持 `recommendation_run_ready`，唤醒 Agent，并建议按需调用 `recommendation_decision` |
| 数据质量触发规则 | 已完成基础版 | 已支持 `data_quality_degraded`，读取 `data_quality_snapshots` 并唤醒 Agent 复核 |
| 调度器接入 | 已完成 | 已新增 `analytics.triggers.evaluate.*` 与 `agent.loop.consume.*` 调度任务；daily 触发评估接收盘后依赖，intraday 触发默认关闭待实时快照链路确认；真实冒烟已创建并派发触发事件 |

验收标准：

- 基础版已具备 CLI/MCP 触发入口；下一步接 Scheduler 后做到无需手动运行 CLI。
- 每个触发都有 `trigger_type`、`trigger_ref`、`dedup_key` 和 `assistant_trigger_events` 审计。
- 高频数据变化通过冷却窗口抑制重复触发。

## 5. O3 内部金融 Agent Loop

| 子任务 | 状态 | 说明 |
| --- | --- | --- |
| LangGraph Loop 设计 | 已完成 | 已落盘 `docs/内部金融AgentLoop设计.md`，明确 Hermes 与内部运行时边界 |
| Agent Loop 状态定义 | 已完成基础版 | 已新增 `AgentLoopLimits`、`AgentLoopPlan`、`AgentLoopTaskResult`、`AgentLoopRunResult` 和任务上下文 |
| Agent Loop 图构建 | 已完成基础版 | `build_internal_agent_loop_graph()` 已实现 `load_task -> plan -> execute_workflow -> persist_result` 状态图，并记录 `agent_node_trace` |
| 触发事件消费 | 已完成基础版 | 可消费 `assistant_trigger_events` 中 `dispatched + agent_task_id` 的 Agent 唤醒任务，并通过 payload 状态避免重复处理 |
| CLI 入口 | 已完成基础版 | 已新增 `finance-agent agent run-once`、`finance-agent agent run-task` 和 `finance-agent agent run-loop` |
| 冒烟验证 | 已完成 | `smoke_internal_agent_loop.py` 和 `smoke_internal_agent_loop_graph_and_daemon.py` 已验证直接 runner、LangGraph 图入口和 CLI 轮询均能消费触发事件、调用 Workflow 并避免重复执行 |
| 模型配置和测试入口 | DB 配置中心基础版已完成 | 已新增 `finance-agent models init/list/set-provider/set-model/set-route/set-retrieval/retrieval/config/route-preview/test/tui`，支持数据库模型配置、检索 profile、JSON/env 调试回退、路由预览、dry-run 请求和脚本化轻量 TUI |
| CLI 聊天窗口 | 聊天记忆基础版已完成 | 已新增 `finance-agent chat`，支持类似 Hermes-Agent 的持续对话、脚本化多轮 smoke、Workflow/工具/模型配置/路由预览/历史查看意图；新增 `assistant_chat_sessions`、`assistant_chat_messages`，支持 `--session-id` 续聊 |
| 模型 Planner 接入 | 已完成 | 新增 OpenAI-compatible 模型客户端；`ModelFinanceAgentPlanner` 已支持“模型 -> 工具 -> 观察 -> 模型”的受控循环，模型只能请求白名单只读工具并选择 6 个受支持 Workflow；模型不可用、输出不可解析或请求越权时自动回到确定性 fallback |
| CLI 聊天模型工具循环 | 已完成 | 普通自然语言问题在模型 ready 时可按需调用只读事实工具、Finance Memory 和图谱工具，再输出中文摘要；固定意图仍走确定性接口 |
| 高风险复核回写 | 开发已完成，真实联调待补 | 当前 Workflow 已记录 GPT-5.5 Pro 复核路由和输入协议；`HighRiskReviewService` 已能消费 `model_review` 事件、调用复核模型客户端、校验结构化 JSON、回写 `model_review_result` 审计事件和 `decision_logs.payload.review_status`；CLI 与调度任务入口已接入，真实 API key 联调按跳过策略后续统一补验 |
| 聊天摘要沉淀 | 已完成 | `ChatSummaryService` 可在聊天消息达到阈值后调用常规模型生成严格 JSON 摘要，写回 `assistant_chat_sessions.summary/payload`，并沉淀 `assistant_memories(memory_type=chat_summary)`；当前保持独立服务入口，不自动挂入聊天主路径，避免隐式真实模型成本 |

验收标准：

- 模型输出必须能解析为结构化 JSON。
- 高风险动作必须经过复核策略。
- 模型不可用时不能阻断基础监控，只能降低置信度或标记待复核。

## 6. O4 持仓监控/观察池管理圆桌化

| 子任务 | 状态 | 说明 |
| --- | --- | --- |
| `portfolio_monitoring` 工具化上下文 | 已完成基础版 | 已调用 `portfolio.get_snapshot`、`factor.get_asset_factor_context`、`signal_risk.get_asset_context`、`memory.recall_asset_memories` |
| `portfolio_monitoring` 圆桌观点 | 真实模型版开发已完成 | 已输出技术分析、因子分析、风险反驳、组合经理、记忆管理员圆桌观点；模型配置可用时走真实模型增强，不可用时保留规则版 fallback |
| `portfolio_monitoring` 中文报告 | 模型观点明细已完成 | 已输出持有、减仓、观察等待等裁决，并在完整中文报告中展示圆桌观点来源、要点、反方和缺口 |
| `watchlist_management` 工具化上下文 | 已完成基础版 | 已调用 `watchlist.get_active_items`、`factor.get_asset_factor_context`、`signal_risk.get_asset_context`、`memory.recall_asset_memories` |
| `watchlist_management` 圆桌观点 | 真实模型版开发已完成 | 已输出继续观察、升级候选、剔除或等待相关圆桌观点；模型增强与 fallback 机制和持仓监控一致 |
| 高风险复核接入 | 已完成基础版 | 已复用 `HighRiskReviewPolicy` 和 `ModelRoutingPolicy`，高风险或数据缺口会生成 GPT-5.5 Pro 复核协议 |
| 用户确认反馈 API | 已完成基础版 | 已新增待确认决策查询和用户反馈写回 API，反馈会写入决策日志、Finance Memory 和原决策 `payload.user_feedback` |

验收标准：

- 六个 Workflow 都有 `roundtable_opinion`、`high_risk_review`、`report_draft`。
- 每个建议能引用信号、风险、因子、记忆和数据质量。
- 规则版 fallback 保留，真实模型不可用时不阻断 Workflow。

当前验证：

- `smoke_portfolio_watchlist_roundtable_workflows.py` 已验证两个 Workflow 均输出 `node_trace`、`tool_calls`、`roundtable_opinions`、`model_routes`、`high_risk_reviews`、`report`。
- 旧的 `smoke_portfolio_monitoring_workflow.py` 和 `smoke_watchlist_management_workflow.py` 仍通过，说明规则版闭环未被破坏。

## 7. O5 数据层生产化

| 子任务 | 状态 | 说明 |
| --- | --- | --- |
| 批量刷新任务 | 基础版已完成 | `collect_base_data.py` 支持 A 股 P0/P1/P2/风险和 crypto 分组，`BaseDataScheduler` 可按配置运行；A 股 P1 Universe 已先拉指数/行业/概念目录，再展开成分采集任务 |
| 缺口补采 | 基础版已完成 | `check_base_data_health.py` 已输出表计数、freshness、缺口、refresh hints 和 `backfill_jobs`；`data production backfill-plan` 可把健康检查 JSON 转成补采任务 |
| 交易日历对齐 | 基础版已完成 | A 股通过 AKShare `tool_trade_date_hist_sina` 写入 `market_calendars`，数字货币写入 7x24 日历；`calendar_refresh` 可独立调度，缺口判断会排除 A 股休市日 |
| Binance 限流处理 | 基础版已完成 | Binance 原生 Provider 支持 fapi 备用端点轮换和 request trace；ccxt 错误 payload 标记 `rate_limited`；采集摘要透出限流状态 |
| 停复牌/退市替代源 | 基础版已完成 | `stock_zh_a_stop_em` 主源和 curl fallback 失败时，仍会尝试 ST、沪深退市、两网退市/暂停上市等补充源，并以 `partial` 状态写入可用风险 |
| 生产调度配置 | 已完成 | 外部 JSON 配置、`--loop` 常驻运行、状态文件、JSONL 事件日志、失败重试、健康检查命令和全量/增量默认策略已完成；Windows 任务计划模板、Docker 调度器模板和运维手册已补齐，其中 Windows 真实注册因当前权限不足待目标环境联调 |
| LOF 备源和基金放量 | ETF/LOF 完成，开放式基金持续分批 | ETF 1,507 只、LOF 390 只均已完成全历史初始化，1,897 条水位全部 `available`；开放式基金前二十批 10,000 个任务最终为 9,997 `available`、3 `unavailable`，当前事实表覆盖 10,006 只、16,319,074 行，同窗口 resolver 剩余 10,629 个候选；结构性无历史数据已写终态水位，瞬时超时均经单标的重试收口；无界 V8 长任务已改为单 worker、每批 500 只，第二十一批正在运行 |
| 北向/解禁/质押 | 已完成 | 北向市场级和个股级快照已接入资金流因子；限售解禁写事件和临近风险；股权质押写风险并进入回避池策略消费 |
| 多候选池合并 | 基础版已完成 | `ProductionUniverseService.merge_universes` 和 `data production merge-universe` 支持同市场候选池按来源权重、排名去重合并，拒绝 mixed 和跨市场来源 |
| 回避池策略 | 基础版已完成 | `AvoidPoolPolicy` 和 `data production rebuild-avoid-pool` 可按资产状态、ST 名称、退市/高风险发现生成 `included=false` 回避池成员 |

验收标准：

- 数据刷新可以稳定覆盖日常推荐需要。
- 失败接口写入 `raw_records` 和数据质量快照。
- 数据质量不足时推荐和 Agent 决策会降级。

## 8. O6 因子和评分增强

| 子任务 | 状态 | 说明 |
| --- | --- | --- |
| 完整资金流窗口 | 基础版已完成 | 已支持资金流历史窗口、主力净流入强度、排名分位和连续性；北向市场级与个股级快照已接入，资金流因子已消费北向净流入强度和北向连续流入窗口 |
| 估值历史分位 | 基础版已完成 | 已支持 PE/PB 历史分位和股息率得分 |
| 事件衰减因子 | 基础版已完成 | 已新增 `event_decay` 因子组，按发布时间做指数衰减并区分正负事件 |
| 数字货币衍生品窗口因子 | 基础版已完成 | 已支持资金费率 z-score、未平仓量变化率、多空拥挤和基差 |
| 策略权重配置 | 已完成基础版 | 已新增 `scoring_strategies` 表、默认策略 seed、`finance-agent data strategy` CLI；A 股短线波段、防守、均衡成长和数字货币波段策略可配置，评分 payload 保留权重快照 |
| 回避池 | 已完成基础版 | 已能按 ST、停牌/不可交易、退市风险和高风险发现生成回避池；推荐流水线运行前会剔除 `included=false` 回避成员，并把剔除原因写入推荐运行审计 |
| 多候选池合并 | 已完成基础版 | 已支持同市场多个候选池合并去重、来源权重和排名冲突处理；调度侧已新增合并池任务，并把推荐任务指向合并候选池产物 |
| 研究池冷却 | 已完成基础版 | 最近被移出或过期的研究池标的默认 7 天内不会因同类推荐自动重新入池，跳过原因写入 `recommendation_skipped` 审计 |

验收标准：

- 因子快照能说明缺失组和来源。
- 评分可解释，并能按策略配置调整。
- A 股和数字货币继续保持单市场链路，不混池。

## 9. O7 Finance Memory 增强

| 子任务 | 状态 | 说明 |
| --- | --- | --- |
| 向量召回 | 已完成增强版 | `MemoryService.upsert_memory` 默认为活跃记忆建立 `memory_embeddings`；`recall_similar_memories` 已按语义相似度、置信度和时间衰减综合排序 |
| GraphStore 图谱查询增强 | 已完成 | 已新增 `src/finance_agent/graph`，配置文件在 Neo4j / DozerDB 与 Apache AGE 中二选一；`GraphSyncService` 可投影资产、决策、观察池事件、记忆、风险、证据和复盘；GraphStore 支持 schema 初始化、健康检查、路径追踪、原因链、记忆冲突、相似历史决策和风险传导；CLI/MCP 已暴露 graph 入口 |
| 用户反馈记录 | 已完成增强版 | `record_user_feedback` 会写入决策日志、反馈记忆和向量索引，并按 accepted / rejected / modified 调整源记忆置信度 |
| 复盘结果反写 | 已完成增强版 | `complete_review_task` 会写入 `review_result` 记忆和向量索引，并把复盘 outcome 反写到同源决策记忆 |
| 记忆可信度衰减 | 已完成基础生产版 | `decay_memory_confidence` 已支持半衰期、`stale`、`archived` 标记和 payload 审计 |
| 候选池原因历史 | 已完成增强版 | `record_watchlist_daily_reason` 统一写入 `daily_watch_reason` 事件、`watchlist_daily_reason` 记忆、向量索引和历史兼容边 |

验收标准：

- Agent 能召回“相似历史场景”，而不只是最近几条记忆。
- 用户反馈会影响后续建议。
- 每次入池、继续关注、移除、买卖建议都有可追溯记忆。

验证记录：

- `.venv\Scripts\python.exe scripts/storage/smoke_finance_memory_closed_loop.py`
- `.venv\Scripts\python.exe scripts/storage/smoke_finance_memory_tools.py`

## 10. O8 前端接真实后端

| 子任务 | 状态 | 说明 |
| --- | --- | --- |
| Workflow 状态面板 | 已完成 | Agent 运行页已展示 Workflow 运行状态、耗时、触发来源、模型/规则来源占比、复核状态和节点摘要 |
| 中文报告展示 | 已完成 | 报告页已展示报告列表、复核状态、结构化摘要和有限 Markdown 渲染详情 |
| 持仓监控页面 | 已完成 | 持仓页已展示组合指标、集中度提示、持仓表、风险标记和最近建议动作 |
| 观察池/候选池页面 | 部分完成 | 观察池页已有真实摘要；推荐页可展示推荐候选、回避池剔除摘要和报告跳转，候选池深度运营留给后续体验增强 |
| 提醒中心 | 已完成 | 风险中心已合并展示监控提醒、触发事件和风险发现，并按严重度分色 |
| 前端 API 层 | 已完成 | `api.ts` 已集中封装报告、推荐、持仓、风险、Workflow 和记忆等后端 HTTP 只读接口，前端不直接访问数据库 |

验收标准：

- 用户能在前端看到持仓、观察池、报告和提醒。
- 前端不直接访问数据库。
- 前端展示的每个建议能跳转到证据和审计。

## 11. O9 人工确认和交易动作闭环

| 子任务 | 状态 | 说明 |
| --- | --- | --- |
| 人工确认模型 | 已完成 | `confirm_decision` 会记录用户确认、拒绝、延后、修改，并复用 Finance Memory 反馈写回 |
| 操作草案 | 已完成 | `order_drafts` 只保存文档性质草案，买入、卖出、加仓、减仓均不触发真实下单 |
| 执行反馈 | 已完成 | `execution_records` 只接受 `user_reported`，记录用户实际执行价格、数量、时间、费用和备注 |
| 决策复盘 | 已完成 | 到期复盘会比较建议、执行和后续表现，并把执行教训反写 Finance Memory |
| 权限开关 | 已完成边界 | 当前系统不接券商或交易所写接口，也不自动真实下单；后续接交易前必须重新设计权限和审计 |

验收标准：

- 系统只给建议和草案，不自动真实交易。
- 用户操作结果能回写 Finance Memory。
- 后续建议会参考用户历史偏好和执行结果。

## 12. 最近更新

| 日期 | 更新 |
| --- | --- |
| 2026-07-04 | 批次 7 方案 22 完成 T1~T10/T12：structural-lite 升级为 v2，修复谐波四比例、默认窗口、SMC 置信度/FVG 降噪、as-of 前视偏差、Elliott 误报和 `*_v2` schema；新增收盘后结构方法论调度任务、`indicator_frames` 幂等入库、`structure.get_asset_context`、圆桌技术分析师上下文和推荐页结构证据卡片；结构信号仍不进入评分。T11 真实校准、黄金样本、事件研究和外部库 D1 决策登记为待真实数据阶段 |
| 2026-07-03 | 批次 7 方案 21 完成：参考 Vibe-Trading 的 swing→结构识别路径，但不新增外部依赖；新增 `finance-agent-structural-lite`，支持 Swing 结构基座、Harmonic-lite、SMC-lite、Elliott-lite，输出统一 evidence_id、置信度、caveats 和 red_lines；harmonic/elliott-wave/smc skill 已改为只读取引擎输出；专项验证 `13 passed` |
| 2026-07-03 | 批次 6 剩余 1~4 收口：行业/概念目录增加东财/同花顺兜底并补跑成员池；推荐合并池改为主板可交易池 + 技术初筛池后重跑真实推荐链路；产出评分和题材因子参考分布；从 10 年 A 股日 K 生成弱市/震荡市/强市样本材料，并用最新 200 个真实候选执行只读闸门验收；相关方案进度和待决策记录已同步 |
| 2026-06-30 | 17 选股引擎修复代码完成：评分缺失惩罚拆为 `data_confidence`，推荐动作裁决改为候选池相对分位+画像阈值+绝对地板，CLI 聊天推荐/单标的分析入口预加载 Workflow，新增推荐真实性闸门阻断 smoke、过期、未来时间戳、数据质量异常和缺失回测证据；真实阈值校准与真实联调按跳过策略后置 |
| 2026-06-30 | 19 选股上下文层代码完成：新增大盘环境服务和买入分位环境闸门、可买入性服务和 blocked 降级、记忆回流排序调整、推荐 payload 上下文呈现；真实弱市数据验收归方案 10 |
| 2026-06-13 | O9 人工确认和交易动作闭环完成：订单草案与执行登记闭环已实现，推荐页、持仓页和风险中心已接入确认、草案、执行登记、执行历史和待复盘入口；端到端冒烟验证“推荐建议 -> 用户确认 -> 订单草案 -> 外部执行登记 -> 持仓变化 -> 到期复盘 -> Finance Memory 召回”通过；系统仍不接券商或交易所写接口，也不会自动真实下单 |
| 2026-06-13 | 09 轻量回测 T5 完成：新增 `finance-agent backtest run`、数据库回测 runner、`analytics.backtest.weekly` 调度任务和配置导出测试；smoke universe 当前评分截面为空时命令返回 `partial` 并落库，说明回测入口链路可用，后续进入推荐证据链和报告摘要接入 |
| 2026-06-13 | 09 轻量回测 T6 完成：推荐生成时读取同市场、同评分策略、同候选池最近可用回测结果，并在推荐 payload/run payload 中写入 `backtest_evidence`；中文报告关键证据区会展示回测摘要，缺失时明确提示“暂无同策略回测证据”，不阻塞推荐但降低确定性措辞 |
| 2026-06-13 | 09 轻量回测 T8 完成：README 路线图、00 总览、项目进度和优化版本进度已同步轻量回测证据链状态；T7 真实 A 股主板数据冒烟按跳过策略待联调；方案收尾全量后端 `454 passed`，前端 `npm run build` 通过 |
| 2026-06-12 | O5 数据层生产化尾项完成：LOF 备源、北向资金、限售解禁、股权质押、Windows/Docker 部署模板和运维手册均已完成；基金放量能力和 `market_bars_intraday` 预留决策已落地，LOF/FUND-015 全量初始化仍按断点队列持续分批执行 |
| 2026-06-13 | O8 前端用户闭环页面主体完成：报告、推荐、持仓、风险中心、Agent 运行和 Finance Memory 页面均已接真实后端接口，`main.tsx` 已拆分为独立页面组件；全部 10 个前端 view 脚本、`tests/test_console_read_api.py`、前端 build 和文档一致性测试通过；真实 Workflow 到 `decision_logs` 落库端到端验收按跳过策略留到统一联调阶段 |
| 2026-06-12 | O4 圆桌 Workflow 真实模型节点开发完成：新增圆桌模型观点节点、Prompt、六个 Workflow 模型增强接入、报告模型观点明细渲染和决策反馈 API；方案验收、两个圆桌 smoke、全量后端 `329 passed` 和前端构建均通过；真实模型 API key 联调按跳过策略留到统一联调阶段补验 |
| 2026-06-12 | O6 推荐链路质量收尾完成：推荐入口接入回避池剔除、多候选池合并调度、评分策略表与 CLI、调度策略透传、研究池 7 天冷却和离线端到端冒烟；方案相关回归 `146 passed`，推荐运行与单条推荐 payload 均可追溯评分策略 |
| 2026-06-12 | O3 高风险复核回写与聊天摘要沉淀开发完成：新增 `HighRiskReviewService`、`agent review-pending` CLI、`analytics.high_risk_reviews.after_agent/sweep` 调度任务和 `ChatSummaryService`；调度回归 `96 passed`、复核/摘要服务组合测试 `10 passed`、CLI dry-run 返回 5 条待复核事件、全量后端 `316 passed`；真实复核模型联调按跳过策略后续统一补验 |
| 2026-06-12 | O2 触发常驻唤醒完成：触发评估与 Agent 事件消费纳入基础数据调度器，盘中急跌/放量异动规则落地；单任务真实冒烟执行成功，创建并派发 21 条触发事件；方案 02 局部回归 `97 passed`，批次 1 收尾全量后端 `304 passed`、前端构建通过 |
| 2026-06-12 | O1 Hermes 接入验收完成：Skill 规范副本、可见性检查、CLI direct/WSL bridge 冒烟、MCP 接入模板、MCP initialize/list_tools 握手和工具边界防回归均完成；方案 01 全量后端测试 `296 passed` |
| 2026-06-11 | 批次 0 P0-1/P0-2 基线整理：现有数据层 V1.5、任务监控、技术初筛、推荐研究池等改动已分主题提交；O1 校准为“Skill 已写待验收”；全量后端测试 284 passed，前端构建通过 |
| 2026-05-21 | 数据同步配置与调度分发完成：`data config init/preview/export/tui` 已支持全量 Universe 配置，`run_base_data_scheduler.py` 和 `collect_base_data.py` 已按 `sync_task_type` 分发，缓存导入已改为懒加载 |
| 2026-05-21 | Scheduler 常驻运行基础版完成：`run_base_data_scheduler.py --loop` 已支持状态文件、JSONL 事件日志、健康检查命令、失败重试和默认全量/增量调度策略 |
| 2026-05-21 | 数据层生产化基础增强完成：补交易日历独立刷新、健康检查补采计划、停复牌/退市替代源、Binance 限流备用端点、同市场候选池合并和回避池策略 |
| 2026-05-21 | Agent Loop 真实模型 Planner 完成：新增 OpenAI-compatible 模型客户端，内部 Agent Loop 默认使用模型增强 planner，支持按需调用只读事实、记忆和图谱工具并自动 fallback |
| 2026-05-21 | CLI 聊天模型工具循环完成：普通自然语言问题可在模型 ready 时按需调用事实工具，固定意图和无模型场景保持确定性降级 |
| 2026-05-18 | V1.2 基础版完成：新增触发事件表、触发服务、6 类触发、CLI/MCP 入口和冒烟验证 |
| 2026-05-18 | 新建优化版本进度表，纳入 9 条未完成主线 |
| 2026-05-18 | O3 内部金融 Agent Loop 基础版完成：新增 runner、planner、状态对象、CLI 和 smoke |
| 2026-05-18 | O3 内部金融 Agent Loop 图化和轮询基础版完成：新增 LangGraph 图入口、`agent run-loop` 和 daemon smoke |
| 2026-05-18 | V1.3 模型配置与测试入口完成：新增 `models config/route-preview/test/tui`，先支持 CLI/TUI dry-run，不接 Hermes |
| 2026-05-21 | V1.3 模型配置中心基础版完成：参考 Dify 将模型供应商、模型实例、路由规则和检索 profile 入库，并支持 CLI/TUI 在线切换 |
| 2026-05-18 | CLI 聊天窗口基础版完成：新增 `finance-agent chat`，先支持多轮命令式对话和脚本化验证 |
| 2026-05-19 | CLI 聊天记忆基础版完成：新增聊天会话/消息表、`--session-id` 续聊、`--new-session` 新会话和 `/history` 历史查看 |
| 2026-05-19 | GraphStore 图谱查询闭合：新增 schema/health、owner/all 同步、相似历史决策、风险传导、CLI graph 命令和 MCP graph 工具入口 |
