# 待决策记录

## 隔夜进度摘要

本轮 A 股缺口收口完成。已完成方案 16、18、20 P0/P1/P2/P3、方案 17 和方案 19 代码开发；方案 15-T7 推荐就绪度健康报告已完成。2026-07-02 收盘后已重新执行 A 股推荐流水线，最新 run `run:balanced_swing_v1:swing:20260702T081113Z:14e6bb07a60c` 返回 `status=available`，覆盖 2,919 条评分和 200 条推荐，动作分布 `buy_candidate=24`、`watch=172`、`avoid=4`；`check_base_data_health.py --readiness` 仍返回 `recommendation_readiness.status=ready`、`executable=true`。注意：本次评分截面仍混合 2026-07-01 与 2026-07-02，D-003 阈值仅作为参考统计，暂不固化。D-004 已由 Codex 选择 `statsmodels` 作为配对交易严格协整检验依赖，并补齐 `PairTradingAdapter` 输出；`czsc`/`pyharmonics` 暂缓。D-005 已补齐生产题材上下文服务和推荐流水线接入，小型真实库 smoke 证明 `factor_frames` 可真实出现 `sector_strength`/`leadership`，但当前真实库仅 33 个资产可生成题材上下文，原因是行业/概念成员池覆盖仍低。D-006/D-007/D-008 已完成代码侧回归复核：`tests\test_chat_invokes_workflow.py`、`tests\test_recommendation_data_gate.py`、`tests\test_market_regime_service.py`、`tests\test_recommendation_market_gate.py`、`tests\test_tradability_service.py`、`tests\test_recommendation_tradability_gate.py`、`tests\test_memory_feedback_ranking.py`、`tests\test_p3_methodology_skills.py`、`tests\test_p3_methodology_adapters.py` 共 `27 passed`；其中 D-006 仍等待真实 key 与人工审阅，D-007 仍等待真实弱市/震荡样本人工判断，D-008 第一版按 C 收口，保持 harmonic/elliott-wave/smc 无引擎不上线。D-009 按总负责人要求暂缓，先专注 A 股。本轮收尾验证：后端全量 `.\.venv\Scripts\python.exe -m pytest -q` 为 `610 passed`，前端 `apps\agent-office` 下 `npm run build` 通过。

| 编号 | 方案/任务 | 需要我决策什么 | 选项 | 你的推荐 | 阻塞的下游 | 当前临时处理 |
| --- | --- | --- | --- | --- | --- | --- |
| D-001（已完成） | 全局/GitNexus | 已处理：总负责人已重建索引；GitNexus `finance-agent` 当前索引仍停在 commit `dfb4a50`、branch `codex/batch1-hermes-o1`，本地分支 ahead 15，且当前 Codex 会话未暴露 GitNexus MCP namespace | A. 后续继续尝试 MCP/WSL CLI impact + staged detect-changes；B. 如果 LadybugDB 继续报错，完成本轮后由总负责人统一重建索引 | B | 后续提交前的图谱复核 | 2026-07-02 复核：WSL CLI `impact FactorService`、`impact collect_base_data`、`impact _compute_factor` 均返回 LadybugDB unavailable / UNREACHABLE_CODE，风险为 UNKNOWN；本轮按本地 diff、TDD 和目标测试兜底，并已写入 `GitNexus差异记录.md` |
| D-002（已完成） | 方案15/真实数据规模化入库 | 刷新 7 月交易日历后，真实库期待 2026-07-01 收盘截面；是否立即补跑 2026-07-01 日级数据链 | A. 先补 2026-07-01 日 K、指标、因子、信号、评分，再进入方案 10；B. 暂缓补数，先做 dry-run；C. 只记录缺口等待人工同步 | A | 方案17 T3 真实阈值校准、方案18 T6 题材分布校准、方案10 PC3/L1~L4 真实联调质量 | 2026-07-02 已补数并通过健康检查：`market_bars` 2026-07-01 覆盖 3,176 只；`analytics.recommendations.ashare.all_a` run `run:balanced_swing_v1:swing:20260702T051848Z:0f02f82a6531` 返回 `status=available`，`factor_count=2,835`、`signal_count=2,835`、`scored_count=2,919`、`recommendation_count=200`；`check_base_data_health.py --readiness` 返回 `recommendation_readiness.status=ready`、`executable=true`，仅剩非阻塞 `capital_flow_snapshots_stale` warning |
| D-003（已完成-收盘后参考统计） | 方案17-T3 | 评分阈值/分位校准是否基于真实数据执行 | A. 基于本次 2026-07-02 最新混合截面临时校准；B. 等完整最终截面再固化；C. 先保留当前阈值，只将本次统计作为参考 | C | 买入阈值最终数值、真实推荐质量验收 | 已完成收盘后统计：最新 run `run:balanced_swing_v1:swing:20260702T081113Z:14e6bb07a60c`，评分样本 2,919，P00=15.792、P08=31.013、P12=33.157、P20=36.697、P50=47.180、P80=57.750、P90=63.514、P100=82.325，推荐动作 `buy_candidate=24`、`watch=172`、`avoid=4`。本次 run 混合 `as_of=2026-07-01` 1,649 条与 `as_of=2026-07-02` 1,159 条，另有少量历史截面，暂不固化阈值 |
| D-004（已完成-statsmodels） | 方案20-B型引擎依赖 | 是否安装 czsc/pyharmonics/statsmodels 等成熟库 | A. 对配对交易安装并锁定 `statsmodels`；B. `czsc`/`pyharmonics` 继续接口+stub；C. 全部自研替代 | A+B | 配对交易严格协整检验已解除；缠论/谐波仍待专项 | 已选择并安装 `statsmodels>=0.14.6,<0.15`（当前 0.14.6，传递依赖 `patsy==1.0.2`），`PairTradingAdapter` 现在输出 Engle-Granger 协整统计量、p 值、临界值和 `cointegration_status`；`tests/test_p2_methodology_adapters.py` 4 passed。`TA-Lib` 仍负责低层技术指标/蜡烛形态，`statsmodels` 负责统计建模与协整，两者职责不重叠 |
| D-005（代码完成-待板块成员补数/分布校准） | 方案18-T6 | 加入题材因子后是否立即用真实 A 股分布重跑分位校准 | A. 立即校准；B. 等真实行业/概念成员池覆盖补齐后再校准；C. 第一版继续不把题材因子纳入最终权重 | B | 方案17 真实买入阈值、题材/价值融合最终参数 | 已新增 `ThemeContextService`，从板块成员、最新日 K、资金流和交易状态生成 `theme_context.factor_groups`；`UniverseRecommendationPipeline` 已在因子阶段接入生产题材上下文并合并 `sector_strength`/`leadership`。真实库当前可生成 33 个资产题材上下文；小型真实库 smoke（000001、600036，事务 rollback）验证 `factor_frames` 已出现 `sector_strength`/`leadership`；相关回归 23 passed |
| D-006（代码侧已复核-待真实联调） | 方案17-T7/方案10 | 是否用真实模型 key、真实推荐数据跑“有哪些适合买入”的端到端人工验收 | A. 当前分支立即联调；B. 等总负责人在场提供 key/数据并人工审阅；C. 先只保留 dry-run 命令清单 | B | 方案17 最终验收、方案10 真实联调归档、Hermes 对话质量判断 | 2026-07-03 已补代码侧复核：聊天选股会预加载 `recommendation_decision`，单标的追问会预加载 `asset_deep_analysis`，数据真实性闸门阻断 smoke/过期/未来数据；相关回归纳入 27 passed。真实联调命令清单：`models route-preview --workflow-type recommendation_decision --high-risk`、`models test --role analysis`、`workflows run recommendation_decision ...`、`agent review-pending ...`；执行仍需真实 key 与人工审阅 |
| D-007（代码侧已复核-待真实样本） | 方案19-T7/方案10 | 是否用真实弱市/震荡市数据验证大盘环境闸门是否合理收紧买入 | A. 当前分支直接用现有库验收；B. 等总负责人同步真实数据并人工审阅；C. 先保留 fixture 验收 | B | 方案19 最终验收、批次6“真的会选股”端到端验收 | 2026-07-03 已复核 fixture/确定性代码侧：`MarketRegimeService` 可识别 bear/bull/range，bear 环境会按 `timing_posture` 收紧买入分位；一字板/停牌/ST/低流动性会降级；记忆回流只调排序不改 `total_score`；相关回归纳入 27 passed。真实弱市/震荡市样本与人工合理性判断后置到方案 10 |
| D-008（第一版已收口-P3主观结构后置） | 方案20-T7 | P3 B 型方法论是否引入 `pyharmonics`、自研 elliott-wave/smc 引擎，或继续保持不可上线 stub | A. 批准 `pyharmonics` 并锁版本；B. 自研 elliott-wave/smc 引擎；C. 第一版只保留 skill + 明确拒绝无引擎运行 | C，先让 seasonal 上线，其余主观结构等后续专项 | harmonic/elliott-wave/smc 真实计算型方法论上线 | 2026-07-03 已按 C 收口并复核：`SeasonalAdapter` 可用；`HarmonicAdapter` 在无 `pyharmonics` 时抛出明确异常；elliott-wave/smc 只声明引擎依赖，不让 LLM 自行计算；`tests\test_p3_methodology_skills.py` 与 `tests\test_p3_methodology_adapters.py` 纳入 27 passed。后续如需真实 harmonic/elliott-wave/smc 引擎，另立专项 |
| D-009（暂缓-A股优先） | 方案20-T7 | onchain-analysis/defi-yield 是否接入链上与 DeFi 数据源 | A. 现在接入 DeFiLlama/链上服务；B. 等 A 股 MVP 稳定后再做；C. 第一版仅保留解读 skill | B | crypto 链上/DeFi 方法论真实上线 | 按总负责人 2026-07-02 决策，数字货币/DeFi 本轮不做，先专注 A 股。已保留 skill 与禁令；无入库链上/DeFi 数据时不得输出链上/DeFi 结论 |
| D-010（已完成） | 方案15-T7 | 日 K freshness 阈值是否改为交易日感知或日 K 专用阈值 | A. `market_bars` 按 timeframe 区分阈值，日 K 至少覆盖到下一交易日收盘后；B. 保持 12 小时阈值；C. 健康检查允许手工 override | A | 推荐 readiness、方案10 真实联调前置检查 | 已完成：`check_base_data_health.py` 对 A 股 1d 日 K 改为交易日收盘感知，`market_bars` 不再因 12 小时阈值在次日凌晨误判 stale；真实健康检查显示 `market_bars` 为 ready，且 D-013 已把日级评分/因子/信号同步纳入同一 freshness 语义 |
| D-011（已完成） | 方案15/候选池维护 | `UniverseRepository.replace_members` 是否实现真正替换语义，自动剔除旧成员 | A. 改为 upsert 新成员后将缺席旧成员标记 `included=false`；B. 保持当前 upsert 语义并新增显式 prune API；C. 继续用运行态脚本临时清理 | B，避免破坏已有调用对 upsert 语义的隐含依赖，同时让合并池任务显式 prune | 推荐候选池覆盖率、后续主板/基金池口径稳定性 | 已完成：新增 `UniverseRepository.prune_missing_members`，`ProductionUniverseService.merge_universes` 在写入完整合并快照后显式剔除缺席旧成员；健康检查的主板覆盖基数改为优先使用可交易主板池 included 数，当前为 3,482 |
| D-012（已完成） | 工程/数据库连接 | 已处理：专项修复 PostgreSQL idle 连接堆积问题 | A. 排查 PyCharm/脚本/SQLAlchemy engine 生命周期并限制连接池；B. 只在故障时手动清理；C. 提高 Postgres max_connections | A | 长任务稳定性、readiness/调度器并发执行 | 已完成：`create_engine_from_settings` 与 `create_session_factory` 按连接串和连接池配置复用进程内 Engine/sessionmaker，默认连接池限制为 `pool_size=10`、`max_overflow=20`、`pool_timeout=30`、`pool_recycle=1800`，单进程最大 30 个连接；本地 TimescaleDB compose 默认 `max_connections=300`；新增 `dispose_cached_engines()` 用于测试和进程退出清理；测试 `tests/test_storage_db.py` 3 passed；真实库 20 次创建会话工厂烟测后没有新增重复 client idle backend，`pg_stat_activity` 中仅当前查询连接为 `active`，其余为 TimescaleDB / logical replication 后台连接 |
| D-013（已完成） | 方案15-T7 | 日级因子/评分/信号 freshness 是否应沿用日 K 交易日收盘语义 | A. 继承日 K 交易日收盘 freshness；B. 保持固定 24 小时阈值；C. 每天上午强制重算但 as_of 不变 | A | 推荐 readiness、方案10 PC3 前置检查 | 已完成：`factor_frames`、`asset_scores`、`signal_snapshots` 在健康检查中补充 `freshness_policy=trading_day_close`、`expected_latest_as_of` 和 `expected_close_at`；新增测试覆盖下一次收盘前不误判 stale、缺失预期交易日仍判 stale；真实健康检查显示 `recommendation_readiness.status=ready` |
