# 待决策记录

## 隔夜进度摘要

进行中。已完成方案 16、18、20 P0/P1/P2/P3、方案 17 和方案 19 代码开发；方案 15-T7 推荐就绪度健康报告已完成，真实库当前 `recommendation_readiness.status=ready`，核心推荐闸门已可执行。2026-07-01 已完成 D-010/D-011/D-012/D-013 的代码修复：日 K 与日级因子/评分/信号 freshness 改为交易日收盘感知，合并候选池新增显式 prune，数据库 Engine/SessionFactory 改为进程内复用并限制 SQLAlchemy 连接池，主板覆盖基数优先使用可交易主板池 included 成员数。方案 17 已把缺失惩罚拆为 `data_confidence`、推荐动作改为候选池相对分位+画像阈值、聊天选股接入 Workflow，并新增推荐数据真实性闸门；方案 19 已补大盘环境闸门、可买入性校验、记忆回流排序和推荐 payload 上下文呈现；方案 20 已补齐 P1/P2/P3 方法论 skill，P2/P3 中可用 pandas/numpy 稳定实现的轻量适配器已完成，重依赖和链上/DeFi 数据源留待审批。当前最关键待决策项：D-002 真实阈值校准前的数据放量口径、D-003 真实阈值校准、D-004 B 型引擎依赖、D-006/D-007 真实模型与真实弱市数据人工验收。方案 17-T3、18-T6、19-T7 等真实数据校准/验收 TODO 仍需总负责人统一处理。

| 编号 | 方案/任务 | 需要我决策什么 | 选项 | 你的推荐 | 阻塞的下游 | 当前临时处理 |
| --- | --- | --- | --- | --- | --- | --- |
| D-001（已完成） | 全局/GitNexus | 已处理：总负责人已重建索引；GitNexus `finance-agent` 当前为 commit `dfb4a50`、branch `codex/batch1-hermes-o1`，可用于既有符号影响分析 | A. 后续继续按 MCP/WSL CLI impact + staged detect-changes；B. 如再次 stale 再统一重建 | A | 后续提交前的图谱复核 | 当前 MCP 专用 namespace 仍未暴露，已按 WSL CLI 复核 15-T7、D-010、D-011 相关既有符号 impact；`merge_universes` 因挂在 CLI/调度入口被标 HIGH，但本次只追加 prune 调用，未改合并评分和调度参数 |
| D-002 | 方案15/真实数据规模化入库 | 真实库现在已有规模数据；推荐闸门已 ready，是否继续刷新非阻塞维度并作为方案 17/18/19 真实校准输入 | A. 现在手动刷新全部非阻塞缺口；B. 暂缓，先进入方案 10 dry-run/人工联调；C. 分维度逐步刷新 | C | 方案17 T3 真实阈值校准、方案10 L1~L4 真实联调质量 | 已部分处理：`market_bars`、`asset_scores`、`factor_frames`、`signal_snapshots` 均按交易日收盘 freshness 判断，真实健康检查 `recommendation_readiness.status=ready`；剩余非阻塞缺口为 `crypto_derivative_snapshots`、`market_calendars`、`risk_findings`，真实阈值校准前建议继续补齐 |
| D-003 | 方案17-T3 | 评分阈值/分位校准是否基于真实数据执行 | A. 本轮用现有库校准；B. 等总负责人手动同步真实数据后校准 | B | 买入阈值最终数值、真实推荐质量验收 | 留 TODO 占位，任务标记“代码完成-待真实数据” |
| D-004 | 方案20-B型引擎依赖 | 是否安装 czsc/pyharmonics/statsmodels 等成熟库 | A. 批准安装并锁版本；B. 先接口+stub/轻量确定性适配器；C. 自研替代 | B，待批准后优先评估 `czsc==0.10.12` 稳定版或等待 `1.0.0` 正式版；配对交易如要严格协整检验再评估 `statsmodels` | 缠论/谐波/配对交易等计算型方法论上线 | 已调研 czsc：PyPI 稳定版 `0.10.12`，GitHub 最新为 `1.0.0-rc.8` 预发布，license 为 Apache-2.0，Python 要求 `>=3.10`；本轮不安装依赖，先实现 `ChanlunAdapter` 接口、stub/fake 测试和解读 skill；P2 其余已用 pandas/numpy 完成 `IchimokuAdapter`、`CorrelationAdapter`、`PairTradingAdapter` 轻量确定性适配器，其中配对交易当前为 OLS hedge ratio + 残差 z-score，不声称完成 statsmodels 协整检验 |
| D-005 | 方案18-T6 | 加入题材因子后是否立即用真实 A 股分布重跑分位校准 | A. 立即校准；B. 等方案 15 真实数据由总负责人手动同步后再校准 | B | 方案17 真实买入阈值、题材/价值融合最终参数 | 本轮只完成确定性服务、题材策略和 fixture 测试；真实分布校准标记为“代码完成-待真实数据” |
| D-006 | 方案17-T7/方案10 | 是否用真实模型 key、真实推荐数据跑“有哪些适合买入”的端到端人工验收 | A. 当前分支立即联调；B. 等总负责人在场提供 key/数据并人工审阅；C. 先只保留 dry-run 命令清单 | B | 方案17 最终验收、方案10 真实联调归档、Hermes 对话质量判断 | 本轮只完成 fake workflow/闸门/目标导向防回归测试；真实模型调用和人工质量判断后置 |
| D-007 | 方案19-T7/方案10 | 是否用真实弱市/震荡市数据验证大盘环境闸门是否合理收紧买入 | A. 当前分支直接用现有库验收；B. 等总负责人同步真实数据并人工审阅；C. 先保留 fixture 验收 | B | 方案19 最终验收、批次6“真的会选股”端到端验收 | 本轮只完成 fixture/fake 测试和可审计 payload；真实弱市数据验证后置 |
| D-008 | 方案20-T7 | P3 B 型方法论是否引入 `pyharmonics`、自研 elliott-wave/smc 引擎，或继续保持不可上线 stub | A. 批准 `pyharmonics` 并锁版本；B. 自研 elliott-wave/smc 引擎；C. 第一版只保留 skill + 明确拒绝无引擎运行 | C，先让 seasonal 上线，其余主观结构等后续专项 | harmonic/elliott-wave/smc 真实计算型方法论上线 | 已补齐 skill 红线；`SeasonalAdapter` 已可用；`HarmonicAdapter` 在无 `pyharmonics` 时抛出明确异常；elliott-wave/smc 只声明引擎依赖，不让 LLM 自行计算 |
| D-009 | 方案20-T7 | onchain-analysis/defi-yield 是否接入链上与 DeFi 数据源 | A. 现在接入 DeFiLlama/链上服务；B. 等真实数据源方案确认；C. 第一版仅保留解读 skill | B | crypto 链上/DeFi 方法论真实上线 | 已补 skill 与禁令；无入库链上/DeFi 数据时不得输出链上/DeFi 结论 |
| D-010（已完成） | 方案15-T7 | 日 K freshness 阈值是否改为交易日感知或日 K 专用阈值 | A. `market_bars` 按 timeframe 区分阈值，日 K 至少覆盖到下一交易日收盘后；B. 保持 12 小时阈值；C. 健康检查允许手工 override | A | 推荐 readiness、方案10 真实联调前置检查 | 已完成：`check_base_data_health.py` 对 A 股 1d 日 K 改为交易日收盘感知，`market_bars` 不再因 12 小时阈值在次日凌晨误判 stale；真实健康检查显示 `market_bars` 为 ready，且 D-013 已把日级评分/因子/信号同步纳入同一 freshness 语义 |
| D-011（已完成） | 方案15/候选池维护 | `UniverseRepository.replace_members` 是否实现真正替换语义，自动剔除旧成员 | A. 改为 upsert 新成员后将缺席旧成员标记 `included=false`；B. 保持当前 upsert 语义并新增显式 prune API；C. 继续用运行态脚本临时清理 | B，避免破坏已有调用对 upsert 语义的隐含依赖，同时让合并池任务显式 prune | 推荐候选池覆盖率、后续主板/基金池口径稳定性 | 已完成：新增 `UniverseRepository.prune_missing_members`，`ProductionUniverseService.merge_universes` 在写入完整合并快照后显式剔除缺席旧成员；健康检查的主板覆盖基数改为优先使用可交易主板池 included 数，当前为 3,482 |
| D-012（已完成） | 工程/数据库连接 | 已处理：专项修复 PostgreSQL idle 连接堆积问题 | A. 排查 PyCharm/脚本/SQLAlchemy engine 生命周期并限制连接池；B. 只在故障时手动清理；C. 提高 Postgres max_connections | A | 长任务稳定性、readiness/调度器并发执行 | 已完成：`create_engine_from_settings` 与 `create_session_factory` 按连接串和连接池配置复用进程内 Engine/sessionmaker，默认连接池限制为 `pool_size=10`、`max_overflow=20`、`pool_timeout=30`、`pool_recycle=1800`，单进程最大 30 个连接；本地 TimescaleDB compose 默认 `max_connections=300`；新增 `dispose_cached_engines()` 用于测试和进程退出清理；测试 `tests/test_storage_db.py` 3 passed；真实库 20 次创建会话工厂烟测后没有新增重复 client idle backend，`pg_stat_activity` 中仅当前查询连接为 `active`，其余为 TimescaleDB / logical replication 后台连接 |
| D-013（已完成） | 方案15-T7 | 日级因子/评分/信号 freshness 是否应沿用日 K 交易日收盘语义 | A. 继承日 K 交易日收盘 freshness；B. 保持固定 24 小时阈值；C. 每天上午强制重算但 as_of 不变 | A | 推荐 readiness、方案10 PC3 前置检查 | 已完成：`factor_frames`、`asset_scores`、`signal_snapshots` 在健康检查中补充 `freshness_policy=trading_day_close`、`expected_latest_as_of` 和 `expected_close_at`；新增测试覆盖下一次收盘前不误判 stale、缺失预期交易日仍判 stale；真实健康检查显示 `recommendation_readiness.status=ready` |
