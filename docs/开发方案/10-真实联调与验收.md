# 方案 10：统一真实联调与验收（开发完成后的端到端验证）

> 优先级：P0 ｜ 批次 5（收尾验收）｜ 依赖：方案 01~09 全部开发完成
> 前置阅读：`00-总体规划与执行约定.md`

## 1. 背景与现状

方案 01~09 已全部完成**开发**：154 文件改动、全量后端 454 passed、前端构建通过、五条红线经代码审查确认守住。但有一个贯穿性事实：

> **整个模型层和真实数据链路从未被真实联调过一次。**

所有涉及真实外部资源的任务在开发阶段按 `00` 的跳过策略统一推迟，目前都标记为"待联调"：

| 待联调项 | 来源方案 | 当前验证方式 | 缺口 |
| --- | --- | --- | --- |
| 高风险复核真实模型调用 | 03-T6 | fake model client | 真 API key 下 verdict 解析、回写、降级是否真跑通 |
| 圆桌五角色真实模型观点 | 04-T6 | fake client + 禁用模型 smoke | 真实观点质量、evidence 引用、风险反驳是否言之有物 |
| 端到端 Workflow → `decision_logs` 落库 | 07-T8 | 前端接口单测 | 前端确认动作是否真写进决策日志并触发记忆反写 |
| 真实 A 股主板数据回测 | 09-T7 | fixture 固定数据 | 真实评分截面 + 10 年日 K 的回测结果是否合理 |
| Windows 任务计划真实注册 | 06-T6 | `-WhatIf` 预演 | 真实注册 + 开机自启 + 失败重启是否生效（开发时权限不足） |
| 全量基金初始化放量 | 06-T2 | 小批量 | FUND-015 全量断点续跑 |

**本方案的唯一目的**：用真实模型、真实数据、真实部署环境，把这条主链路完整跑通一次并留存证据，把"代码正确"升级为"系统真能用"。

```text
触发唤醒 → 圆桌真实模型分析 → 高风险复核回写 → 中文报告 → 前端确认 → 执行登记 → 复盘 → Finance Memory 反写
```

## 2. 目标与非目标

**目标**

1. 配置真实 DeepSeek V4 Pro（常规分析）和 GPT-5.5 Pro（高风险复核），完成全链路端到端真实运行一次。
2. 每个待联调项产出脱敏的真实请求/响应样本，归档进对应方案文档附录。
3. 真实数据回测产出合理性经人工审阅的绩效结果。
4. 暴露并修复仅在真实环境才会出现的问题（编码、超时、限流、JSON 漂移、成本）。

**非目标**

- 不改任何已通过测试的业务逻辑——除非真实联调暴露出确定性缺陷（暴露了就按 TDD 补测试再修）。
- 不接真实交易（红线不变）。
- 不做性能压测和并发调优（归后续运维阶段）。

## 3. 联调前置检查（开工第一步，全部通过才进 3.2）

- [x] PC1 GitNexus 索引已由总负责人重建（当前可用；如后续 stale 再停下提醒）。
- [x] PC2 数据库迁移到 head：`.\.venv\Scripts\python.exe -m alembic upgrade head`（已确认 0017/0018/0019 应用到 head）。
- [ ] PC3 真实数据当前就绪：历史上已通过 A 股主板 10 年日 K 与评分截面检查；2026-07-02 补跑后曾返回 `recommendation_readiness.status=ready`、`executable=true`。2026-07-15 重新检查时已退化为 `blocked/executable=false`：A 股日 K、因子和评分仅到 2026-07-02，预期交易日为 2026-07-14；资金流、财务和事件也已过期。当前正在执行 `ashare.bars.1d.close_final` 全主板增量恢复，完成数据和分析重算前不得复用历史 ready 结论。
- [x] PC4 调度任务可复现性确认（依赖方案 11-T2 完成）：`data config export` 生成的计划包含触发评估、Agent 消费、复核、回测全部新任务，不依赖本地手改的 runtime json。

## 3.2 模型配置（不进代码、不进 .env 提交）

通过既有 `models` CLI 把真实 key 写入数据库：

```powershell
.\.venv\Scripts\python.exe -m finance_agent.cli models init `
  --deepseek-base-url https://api.deepseek.com/v1 `
  --deepseek-api-key "<真实 key>"
.\.venv\Scripts\python.exe -m finance_agent.cli models set-provider --provider openai --base-url <...> --api-key "<真实 key>"
.\.venv\Scripts\python.exe -m finance_agent.cli models set-route --role analysis --model deepseek-v4-pro
.\.venv\Scripts\python.exe -m finance_agent.cli models set-route --role high_risk_review --model gpt-5.5-pro
.\.venv\Scripts\python.exe -m finance_agent.cli models route-preview   # 确认路由
.\.venv\Scripts\python.exe -m finance_agent.cli models test --role analysis   # 单次连通性 dry-run
```

安全：key 只进数据库；联调结束后总负责人决定是否保留或轮换；任何样本入档前脱敏（base_url、key、token 用量保留量级即可）。

## 3.3 分项联调（每项独立可回退，失败不影响其他项）

### L1 圆桌真实模型（04-T6）
- 跑 `finance-agent workflows run asset_deep_analysis`（用真实有数据的 owner/asset）。
- 人工审阅五角色观点：① `generated_by=model` 是否生效；② 每条 key_point 能否追溯到工具上下文；③ **风险反驳是否言之有物**（重点，不能是套话）；④ evidence_ids 是否都在上下文白名单内（凭空引用应已被剔除进 data_gaps）。
- 拔掉模型配置重跑一次，确认 fallback 到规则版且报告标注来源。
- 样本存 `04` 方案附录（脱敏）。

### L2 高风险复核回写（03-T6）
- 构造一条卖出/换股裁决（真实数据触发或手工造待复核事件）。
- 跑 `finance-agent agent review-pending --owner-id ... --limit 5`（去掉 dry-run）。
- 验证审计链可追溯：`model_route → model_review → model_review_result`；reject 的决策在 `decision_logs` 状态为 `rejected_by_review`。
- 断网/错 key 重跑一次，确认 `review_unavailable` 且不默认放行、置信度打折。
- 样本存 `03` 方案附录。

### L3 端到端主链路 + 前端确认（07-T8）
- 真实跑一轮：触发 → Workflow → 报告落库 → 前端报告页可见 → 推荐页点「确认接受」→ 生成订单草案 → 「执行登记」→ 持仓更新 → 复盘任务生成。
- 验证前端确认动作真写进 `decision_logs.user_action` 且触发 `MemoryService` 反写。
- 验证草案卡片只有"去外部登记"入口、无下单字样（红线）。

### L4 真实数据回测（09-T7）
- `finance-agent backtest run --strategy factor_score_topn --universe <真实A股池> --years 5`。
- 人工审阅：年化/最大回撤/夏普是否在合理量级（不是 NaN、不是离谱值）；`data_versions` 记录了 bars 水位和 `score_mode`；报告证据区出现"近 N 年模拟回放"措辞。
- 同参数重跑确认结果稳定。
- 样本存 `09` 方案附录。

### L5 Windows 生产部署（06-T6）
- 用管理员权限真实执行 `deploy\windows\register_scheduler_task.ps1` 和 `register_api_task.ps1`。
- 重启机器，确认调度器和 API 自动拉起、状态文件心跳正常。
- 按 `docs\运维手册.md` 走一遍故障演练（Cookie 过期刷新、源熔断恢复）。

### L6 全量基金初始化（06-T2 / FUND-015）
- 分批执行 `fund.etf.bars.1d.bootstrap` / `fund.lof.bars.1d.bootstrap` / `fund.open.nav.bootstrap`，记录每批水位与失败清单，失败率 < 5%。
- 更新 `docs\基金行情同步任务方案.md` FUND-013/015 状态。

## 4. 任务拆解

- [x] T1 前置检查 PC1~PC4 全通过（PC3 已在 2026-07-02 补数后通过 readiness；后续进入模型 key 与真实联调阶段）。
- [ ] T2 模型配置（3.2）+ route-preview/test 连通（主分析已连通；高风险复核端点已纠正，外部账户额度不足）。
- [x] T3 L1 圆桌联调 + 样本归档。
- [ ] T4 L2 复核回写联调 + 样本归档。
- [x] T5 L3 端到端主链路 + 前端确认联调。
- [x] T6 L4 真实数据回测 + 人工审阅。
- [ ] T7 L5 Windows 部署真实注册 + 重启验证。
- [ ] T8 L6 全量基金初始化放量。
- [ ] T9 联调问题清单：把每项暴露的真实问题登记成 issue 列表（确定性缺陷当场 TDD 修，模型质量问题记录待调优）。
- [ ] T10 文档同步：把 03/04/06/07/09 进度表的"待联调"全部更新为"已联调"并附样本；更新 `docs/优化版本进度跟踪表.md` 把 V1.3/V2.0 标记为真实验收通过。

## 5. 验收命令

```powershell
.\.venv\Scripts\python.exe -m finance_agent.cli models route-preview
.\.venv\Scripts\python.exe -m finance_agent.cli workflows run asset_deep_analysis --owner-id owner:demo --asset-id <真实asset>
.\.venv\Scripts\python.exe -m finance_agent.cli agent review-pending --owner-id owner:demo --limit 5
.\.venv\Scripts\python.exe -m finance_agent.cli backtest run --strategy factor_score_topn --universe <真实A股池> --years 5
.\.venv\Scripts\python.exe -m pytest -q   # 联调若修了代码，全量必须仍全绿
```

验收标准（全链路一次真实通过）：

- 一条建议从触发到复盘全程在审计链可追溯，每节点引用前一节点 ID。
- 真实模型观点的风险反驳经人工判定"有信息量"，evidence 引用可追溯。
- 复核 reject 的卖出建议无法生成订单草案。
- 真实回测结果合理且可复现。
- Windows 重启后服务自动恢复。
- 全量 pytest 仍全绿。

## 6. 风险与注意事项

- **成本控制**：圆桌 6 Workflow × 5 角色全开 + 复核，真实调用费用可观。联调先用 `roundtable_model_roles` 收缩到 `["risk_rebuttal","portfolio_manager"]`，单 Workflow 验证通过再全开；记录每次联调的 token 量级。
- **模型质量问题 ≠ 代码缺陷**：若真实观点质量不佳（套话、反驳无力），那是 prompt 调优问题，记录到 T9 待调优清单，**不要**为了"看起来更好"去改确定性裁决逻辑或放宽 evidence 校验。
- **真实暴露的确定性 bug 必须 TDD 修**：先加失败测试复现，再修，保证回归。
- **联调用真实 key 全程不入库代码/不进提交**；样本脱敏后才入文档。
- 真实数据回测注意复权口径（方案 06 已要求实测确认腾讯直连源复权方式）；口径不符会让回测结论失真。

## 7. 进度表

| 任务 | 状态 | 验证记录 |
| --- | --- | --- |
| T1 前置检查 | 进行中-PC3 重新阻断 | 2026-07-01：PC1 已通过；PC2 已通过，`alembic current/heads` 均为 `20260614_0019 (head)`，`alembic upgrade head` 成功。PC4 已通过，`data config export --output runtime\_pc4_verify_export.json` 成功，`tests/test_scheduler_jobs_regenerable_from_preset.py -q` 为 2 passed。2026-07-02 PC3 补数曾使 `recommendation_readiness.status=ready`。2026-07-15 复查时报告为 `blocked/executable=false`，`market_bars/asset_scores/factor_frames` 只到 2026-07-02，预期交易日为 2026-07-14；已启动 `ashare.bars.1d.close_final` 全主板增量恢复，共 16 个内部批次、每批 200 只。PC3 需在日 K、资金流、财务、事件和分析重算后重新验收。 |
| T2 模型配置 | 进行中-复核账户额度阻塞 | `deepseek-v4-pro` 真实请求已于 2026-07-03 HTTP 200 通过。2026-07-13 复查 `gpt-5.5` 时发现供应商 Base URL 缺 `/v1`，旧连通测试把网关 HTML 200 误报为 `ok=true`；已提交 `72e1ac6` 增加 OpenAI-compatible JSON 结构校验，并把数据库端点纠正为 `https://claude.aiapis.help/v1`。纠正后 `models test --model-key gpt-5.5 --real-request` 真实返回 HTTP 403、`ok=false`、`insufficient_user_quota`；route-preview 的 `ready=true` 只代表配置字段齐全，不能替代真实连通。 |
| T3 圆桌联调 | 已完成-L1 单角色真实联调通过 | 2026-07-03 已完成代码侧回归：聊天选股预加载 `recommendation_decision`、单标的追问预加载 `asset_deep_analysis`，数据真实性闸门可阻断 smoke/过期/未来数据；相关测试纳入 `27 passed`。真实联调暴露两处确定性问题并已 TDD 修复：Workflow final_state 内嵌 ORM 导致 CLI JSON 序列化失败、圆桌模型上下文过大导致 ds-pro 读超时。修复后真实运行 `recommendation_decision` 单条推荐 + `roundtable_model_roles=["risk_rebuttal"]` 成功，最新 run `workflow:owner_smoke_o4_roundtable:recommendation_decision:20260703020217` 状态 `succeeded`，`roundtable:risk_rebuttal` 为 `generated_by=model`、`model_instance_id=deepseek-v4-pro`，引用白名单证据 `evidence:b97d8f2bc82910fb`。 |
| T4 复核联调 | 进行中-外部额度阻塞 | 2026-07-13 已用独立 owner 和可关联 decision 的 `recommendation_sell` 样本真实运行。review_unavailable 路径已通过：`model_route → model_review → model_review_result` 可追溯，决策置信度乘数为 0.7、`user_action=unknown`、订单草案为 0。真实 reject 调用被供应商 HTTP 403 `insufficient_user_quota` 阻塞；补充额度后按同一事件重跑，不绕过模型、不伪造 reject。脱敏样本见方案 03 附录 8.2。 |
| T5 端到端+前端 | 已完成 | 2026-07-13：使用现有真实推荐 `asset_rec:ashare:603259:swing:2ec07104b026` 完成 Workflow `workflow:default-owner:recommendation_decision:20260713:t5`，报告、风险、记忆引用和复核协议均入库。浏览器依次验证中文报告正文、推荐采纳、订单草案、外部执行登记红线、持仓更新、执行历史和 Finance Memory；页面明确“系统不会连接券商或交易所执行真实下单”。数据库链路为 `decision:scheme10:default-owner:20260713:t5`（`user_action=accepted`）→ `draft:default-owner:ashare-603259:20260713022021317654` → `execution:default-owner:ashare-603259:20260713021500000000`（隔离验收记录，不代表真实交易）→ `position:portfolio:scheme10:default-owner:20260713:ashare-603259:long` → `review:execution:default-owner:ashare-603259:20260713021500000000:outcome` → `memory:review:execution:default-owner:ashare-603259:20260713021500000000:outcome:review_result`。复盘任务人为调到期后因没有晚于执行时间的行情，正确完成为 `partial` 且 `missing_fields=[latest_price]`，未编造收益。联调缺陷提交 `97b5475`；专项 `27 passed`、全量 `673 passed`，4 个前端专项脚本和生产构建通过，staged GitNexus 完整分析 6/6 文件、11/11 hunks、风险 low、0 条受影响流程。 |
| T6 真实回测 | 已完成 | 2026-07-13：固定窗口 2021-07-13 至 2026-07-13，对 `universe:merged:ashare:recommendation` 运行 `factor_score_topn / strategy:ashare:short_swing / replayed / Top20 / monthly`。首次运行 `bt:f7f004c2111ddcfd1177a869` 因回测源错误过滤生产推荐使用的 `partial` 评分而返回 `partial`；同时发现主结果缺 Sortino。两处确定性缺陷经 TDD 修复并提交 `ec2ad4b`。修复后两次固定参数运行均得到 `bt:b9047b4e3278fcafe0de2b6a`，入选资产、1,205 点曲线、数据版本和指标逐字段一致：CAGR 12.14%、最大回撤 -37.37%、Sharpe 0.617、Sortino 0.880、总收益 76.73%、波动率 24.01%，全部有限且无 warning；曲线最后日期 2026-07-02，无未来行情。合理性审阅通过，但 `score_mode=replayed` 存在前视偏差，结论仅为历史模拟回放，不代表实盘收益。回测专项 `18 passed`、全量 `674 passed`，前端生产构建通过。 |
| T7 Windows 部署 | 进行中-当前令牌权限阻塞 | 2026-07-13：`data config export` 成功生成 55,553 bytes 的可再生调度配置，调度器/API 注册脚本 `-WhatIf` 均退出 0。随后用正式任务名真实执行两个注册脚本，均返回 `HRESULT 0x80070005 / Access is denied`；当前账户为 `DESKTOP-ELT87C4\Administrator`，但会话令牌 `IsAdmin=False`、Medium 完整性，`Get-ScheduledTask` 确认两项任务均不存在。真实失败暴露脚本仍打印成功并退出 0 的确定性缺陷；GitNexus LOW impact 后经 TDD 显式补 `-ErrorAction Stop`，同场景复跑两个进程均退出 1 且无成功提示，专项 5 passed。因此启动、失败恢复、AtStartup 和重启后恢复验收未执行；当前手工 API `/api/health` 为 `status=ok` 不能替代计划任务证明。需在提权 PowerShell 重跑；整机重启仍需用户明确授权。 |
| T8 基金放量 | 进行中-ETF/LOF 完成，开放式基金有界分批中 | 2026-07-14 至 2026-07-15：ETF 1,507 只、LOF 390 只已收口。开放式基金第三十二批为 496 `available`、`022131` 瞬时超时、3 个已知结构性 `unavailable`，`022131` 单标的重试成功；第三十三批 500/500 `available`。当前唯一水位为 16,491 `available`、9 `unavailable`，事实表覆盖 16,492 只、20,671,966 行且非 available 行为 0；仍有 4,103 只从未尝试。动态窗口推进曾把 11,719 只实际已覆盖新结束日的基金误判为成立前年份缺口，已按 TDD 修复起始边界信任，当前 `20160716..20260714` resolver 为 6,393 个真实待初始化或待补齐候选，9 个终态缺口均排除。未标记全量完成。 |
| T9 问题清单 | 进行中 | 已登记模型、部署、调度、覆盖查询、长任务和基金终态水位等真实问题；确定性缺陷均按 TDD 修复，外部额度/权限/重启和结构性无数据保持真实阻塞或失败清单。 |
| T10 文档同步 | 进行中 | 2026-07-15：已开始同步方案 06、基金方案、待决策记录和项目/优化进度表；方案 10 仍有 T2/T4 外部模型额度、T7 提权与重启授权、T8 开放式基金剩余批次，不能标记总体验收完成。 |

## 8. 联调问题清单

| 编号 | 类型 | 状态 | 证据与处理 |
| --- | --- | --- | --- |
| I-10-001 | 确定性代码缺陷 | 已修复 | `models test` 与 Web 连通性测试曾把网关 HTML 200 当作模型成功响应。TDD 红灯 `2 failed, 2 passed`；提交 `72e1ac6` 后专项 `4 passed`、相邻回归 `15 passed`、全量 `670 passed`、前端构建通过；GitNexus staged 风险为 low、0 条受影响流程。 |
| I-10-002 | 外部账户状态 | 阻塞中 | `gpt-5.5` Base URL 已纠正为 `/v1`，真实端点返回 HTTP 403 `insufficient_user_quota`。review_unavailable 安全降级已通过；需账户补充额度后才能完成真实 reject 和人工质量审阅。 |
| I-10-003 | 历史验收数据缺口 | 已规避 | 最近 500 条既有 `model_review` 待处理事件均无可关联 `decision_logs`，不能证明回写。使用独立 owner 构造可追溯验收事件，不修改旧用户数据；该现象先作为历史样本缺口记录，不在没有真实 Workflow 复现前判定为代码缺陷。 |
| I-10-004 | 确定性代码缺陷 | 已修复 | 真实 Workflow 报告模板收到 SQLAlchemy `AssistantMemoryORM` 时曾按 dict 调用 `.get()`，导致报告生成中断。先补 ORM 红灯测试，再兼容 dict/ORM 两种输入；修复纳入 `97b5475`。 |
| I-10-005 | 确定性代码缺陷 | 已修复 | 执行复盘曾把 `2026-07-02` 的旧日 K 当作 `2026-07-13` 执行后的后续表现并错误得出 `confirmed/+22.7%`。GitNexus upstream impact 为 low，直接调用者仅 `run_due_reviews`；TDD 锁定“价格观测时间必须严格晚于执行时间”，修复后真实任务正确降级为 `partial/latest_price` 缺失。 |
| I-10-006 | 确定性代码缺陷 | 已修复 | `GET /api/reports/{workflow_run_id}` 以位置参数调用 keyword-only 的 `FinanceAgentInterface.get_report`，真实 API 返回 `unavailable`。GitNexus impact 为 low；红灯复现后改为关键字调用，重启 venv API 后真实接口返回 `status=ok`、报告 Markdown 长度 1802，浏览器可见完整正文。 |
| I-10-007 | 确定性代码缺陷 | 已修复 | 生产推荐允许带缺失惩罚和置信度的 `partial` 评分，但 `DatabaseBacktestScoreSource` 只读取 `available`，使真实 universe 的 7,007 条评分全部被过滤。GitNexus upstream impact 为 low、2 个直接入口；TDD 后统一接受 `available/partial`。 |
| I-10-008 | 确定性代码缺陷 | 已修复 | `QuantstatsPerformanceAdapter` 已实现 Sortino，但 `BtBacktestAdapter` 主结果未接入，真实回测缺少验收指标。固定下跌 fixture 红灯后在既有收益序列上补 quantstats Sortino；真实值 `0.8802086970`，两次复跑一致。 |
| I-10-009 | Windows 权限/重启边界 | 阻塞中 | 当前 PowerShell 令牌不是提升令牌，两个正式计划任务注册均返回 `HRESULT 0x80070005`，查询确认任务数为 0。需在提权 PowerShell 中注册后再做启动和失败恢复；整机重启会中断当前会话，未获明确授权前不执行。 |
| I-10-010 | 确定性部署脚本缺陷 | 已修复 | 两个注册脚本遇到 `Register-ScheduledTask` 非终止权限错误后仍打印成功并让子进程退出 0。GitNexus upstream impact 为 LOW、无直接调用者/流程；TDD 要求注册失败必须终止，给两个命令显式添加 `-ErrorAction Stop` 后，同一真实权限场景均退出 1、不再含成功提示，任务数保持 0。 |
| I-10-011 | 确定性调度 CLI 缺陷 | 已修复 | 导出配置把 bootstrap 正确标记为禁用的 manual job，但 `run_base_data_scheduler.py --run-once --only <manual-job>` 只过滤任务名、不临时启用，导致命令退出 0 却执行 0 jobs。GitNexus upstream impact 为 LOW、1 个入口上游、0 条流程；TDD 后仅在显式 run-once 场景临时启用所选任务，ETF dry-run 从 0 jobs 变为 1 个 planned job。 |
| I-10-012 | 确定性调度参数缺陷 | 已修复 | 基金 bootstrap 的 `limit=null` 未覆盖采集脚本默认 `limit=5`，十年任务实际每只只抓 5 条。首次真实 ETF 在 55 只后主动停止，未继续扩大。`build_collection_args` upstream impact 为 HIGH（7 直接/29 总上游/4 模块）；TDD 后仅为基金日 K/净值全历史任务显式传 `None`，真实 dry-run 已显示十年窗口和 `limit=null`，55 只残缺标的会按覆盖不足重新入队。 |
| I-10-013 | 确定性覆盖查询缺陷 | 已修复 | ETF 入库扩展到 826 万条 market_bars 后，1,507 asset_id 的单条覆盖聚合耗尽 TimescaleDB 容器 64MB `/dev/shm`，返回 `could not resize shared memory segment`。四个覆盖函数 impact 均 LOW；TDD 后按 200 ID 分块并合并结果，真实 resolver 查询成功，无需重启容器或修改系统安全边界。 |
| I-10-014 | 确定性断点水位缺陷 | 已修复 | 基金成功水位未保存完整请求窗口，且覆盖判断在检查水位前把成立前/源端最新日后的边界空窗当缺口，导致 1,505 个成功 ETF 中 1,143 个仍反复入队。新增请求范围持久化和前后边界可信规则，中间整年缺口仍必须补采；本次成功水位受限补写 1,505 行后，真实 resolver 只剩两只 locked 标的。 |
| I-10-015 | 确定性调度日期缺陷 | 已修复 | 基金全历史 bootstrap 曾把运行当天作为结束日，盘中执行时会不断追逐尚未形成的未来/未闭合净值日。TDD 将全历史初始化结束日固定为前一个完整自然日，日常 NAV 与 close-final 语义保持不变；提交 `80a6746`，相关回归 188 passed、全量 682 passed。 |
| I-10-016 | 长任务进程稳定性 | 已处理-持续分批验证 | 一次性处理 20,604 只开放式基金时，前两只后子进程以 `exitcode=2147483651` 崩溃，V8 报 `Check failed: !IsConfigurablePoolInitialized()`，且无界任务会碰到 6 小时总超时。项目 `.venv` 的 `_base_executable` 指向创建它的 Anaconda Python，不是解释器泄漏；真实问题是无界 V8 长任务稳定性。提交 `1f1b153` 将 bootstrap 固定为单 worker、`source_limit=500`；两批各 500 只真实执行均 0 error，全量回归 682 passed。 |
| I-10-017 | 外部数据源瞬时超时 | 已重试收口 | 开放式基金第三批 `002612` 首次请求东财时连接超时，主批摘要如实为 499 available/1 error；错误原始记录和 error 水位已落库。随后同一 URL 0.428 秒返回 HTTP 200，单标的有界重试 1/1 available、写入 2,387 行，水位 `fail_count=0`、错误字段清空，请求窗口为 `20160715..20260714`。该事件不判定为代码缺陷，后续协调器遇任一非成功项仍立即停止。 |
| I-10-018 | 确定性终态水位缺陷 | 已修复 | `fund_nav_full_history_backfill --no-schedule-failure-retry` 返回 `unavailable` 时仍调用 `record_failure()`，固定写成 `status=error`；`next_retry_at=null` 又使它立即进入下一批。PyCharm Call Hierarchy 无法解析 Python 顶层函数，回退符号/引用检索后确认共享 `record_failure` 调用面广，因此不改其语义，新增 `record_unavailable()` 并仅在基金 `unavailable + schedule_retry=false` 路径使用。TDD 专项 31 passed、调度回归 167 passed、全量 689 passed；提交 `5223f6a`。 |
| I-10-019 | 外部源结构性无数据 | 已登记终态清单 | `005471` 与 `005792` 的东财直连均 HTTP 200，但 `Data_netWorthTrend=[]`，分别写入终态 `unavailable`，不生成净值事实、不伪造 `last_success_at`、不安排自动重试。相同请求窗口 resolver 均已排除，扩大窗口或手工单标的仍可重新验证；两项计入失败清单，不计入 available 覆盖。 |
| I-10-020 | 确定性指数目录缺陷 | 已修复 | PyCharm lint 发现 `build_ashare_p1_universe_tasks` 对无代码目录项调用不存在的 `record_risk_stage_result`，并引用未定义的 `task/results/batch_index`。真实 `CollectionTaskResult.payload.indexes` 构造红灯稳定复现 `NameError`；循环改用既有过滤结果 `index_progress_items` 后进度专项 17 passed、PyCharm lint 零错误、build 成功，修复并入 `5223f6a`。 |
| I-10-021 | 确定性动态窗口缺陷 | 已修复 | 开放式基金全历史结束日从 `20260713` 推进到 `20260714` 后，旧成功水位不再覆盖完整窗口；即使事实最新日期已到 `20260714`，成立前缺失年份和 `earliest_nav_date` 仍会触发重复入队。PyCharm 顶层函数调用层次不可用，符号/引用检索确认生产直接调用者仅 `batch_open_fund_nav_symbols`，风险 LOW。新增红灯稳定得到 `requires_collection=True`；最小修复仅继承旧成功水位对起始边界的信任，最新日不足和中间年份缺口仍严格补采。专项 27 passed、全量 697 passed；真实 resolver 从 17,634 个候选降到 6,393，首批仍为未初始化基金，9 个终态缺口全部排除。 |
