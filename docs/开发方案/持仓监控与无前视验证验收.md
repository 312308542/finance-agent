# 持仓监控与无前视验证验收记录

## 2026-09-05 修复范围

- 盘中持仓监控入口纳入 Git 发布；每轮检查当前持仓、T+1 与行情年龄，事务失败可重试，局部失败提交保护动作后退避。
- 仅持久化业务动作变化产生触发事件；重复轮询不重复唤醒，风险消失后重新出现不会被旧冷却窗口吞掉。Agent 实际派发仍由现有触发器调度执行。
- `exit` 是研究建议，不是自动下单。T+1 不可卖出时保留原意图并降级为不可执行。
- 候选池刷新在同一事务追加不可变成员有效区间，历史查询按信号日过滤；缺少历史表或无法证明历史成员时研究 fail-closed。
- 旧回测和证据不完整的 v2 结果仅作为研究附件；历史准入拒绝缺失字段、非法计数、NaN/Infinity、不完整置信区间及缺失证据 ID。
- 推荐服务重新读取持久化策略状态，不接受调用参数伪造验证状态；新增买入和生命周期落库前统一门控，保留原始建议和证据，观察、回避及持仓退出继续输出。
- 前向结算任务与历史回测分开接线；已验证策略持续接受滚动评估，前向指标只来自实际成熟截面，不再伪造覆盖率。
- 策略状态 upsert 后刷新 ORM 会话对象，停用、晋级和历史证据撤销立即作用于运行时门控；周度评估拒绝旧时点评估覆盖当前状态，独立记录实际计数周，数据不足不占用计数机会。

## 尚未完成

- 历史 walk-forward runner 仍输出 `strategy_walk_forward_v1`，Rank IC、真实周换手、成交仿真统计、集中度和市场/板块分层收益尚未完整接入生产链路。因此不能宣称阶段 04 完成，也不能把 v1 重命名为 v2 来放行。
- 真实交易时段连续 120 分钟采集压力和分钟刷新验收尚未完成。2026-09-05 为非交易日，周末健康检查不能替代该验收。
- 当前数据库已有 research 策略状态，但 `allow_new_buys=false`（验证证据与成熟 T+20 样本不足）；当前仍为空仓，模拟测试不等于已验证真实持仓监控效果。

## 验证证据

- 发布基线 `cc8dc7c` 的 Python 全量测试：`1389 passed, 2 skipped, 30 warnings`，耗时 117.37 秒；警告来自 pandas/ffn 的既有弃用提示。
- 候选后端镜像使用 Python 3.11.11，`pip check` 通过，源码从 `/app/src` 加载。容器全量复跑为 `1386 passed, 3 skipped, 2 deselected`，耗时 143.43 秒；排除项分别依赖 Windows `.venv/Scripts/python.exe` 和 Git 工作区，已由本机测试覆盖。初次未排除时这两项失败，不计为镜像全量通过。
- 容器跳过的三项持久化调度集成测试另用独立 tmpfs PostgreSQL、Redis 和内部网络补跑：`3 passed`，耗时 13.09 秒。空库完整迁移至 `20260904_0031`，覆盖 Redis 清空后恢复、资源池并发和依赖代次幂等；未接入业务数据库或 Redis。测试容器和专用网络均已清理。
- Go 网关在 Linux 容器中执行 `go test ./...` 通过；构建允许用 `GOPROXY` 参数覆盖模块下载代理，不关闭校验服务。该结果不替代真实行情包与持久连接验收。
- 独立规格复验：8 个文件 `151 passed`，另有 ORM 停用、旧周重放和缺数据后首次完整评估三个原始探针通过。
- Ruff：`src scripts tests` 全部通过。
- PyCharm 构建返回成功，但 IDE 提示此项目构建诊断有限；该结果不替代 pytest 和前端实际构建。
- 前端全部 12 个 `test-*.mjs` 脚本通过，Vite 生产构建通过，保留 chunk 体积提示。
- 根 Compose、独立调度模板与发布覆盖配置校验通过；发布配置已确认保留原运行目录和命名卷，不挂载主分支源码。

## 发布方式

根 `docker-compose.yml` 保留开发挂载；发布时合并 `deploy/docker/compose.release.yml`，设置 `FINANCE_AGENT_HOST_RUNTIME_DIR` 为既有运行目录、`FINANCE_AGENT_HOST_ENV_FILE` 为既有环境文件。固定使用现有 Compose 项目名 `finance-agent`，不执行 `down -v`，不重建或清空数据库卷。

从提交的 Git 内容构建镜像，核查镜像内存在 `scripts/runtime/run_position_monitor.py`。发布后检查容器实际挂载、代码版本、API、策略准入输出、监控健康状态及浏览器页面；未完成上述检查不得标记为已部署验收。

### 2026-09-05 首次发布基线与现场结果

- 发布源为 `cc8dc7c` 的干净 Git archive，目录为 `runtime/releases/20260905-150324/source-gateway`。保留旧镜像回退标签 `rollback-20260905-150324` 和两份运行配置；`backup/schema.sql` 仅为数据库结构备份，不是业务数据完整备份。
- 15:57（Asia/Shanghai）业务容器已更新启动。API、Web、Scheduler、MCP、实时行情和持仓监控均未挂载 `/app/src`；后端只保留既有 runtime 挂载。数据库与 Redis 仍使用原命名卷，启动时间均为 2026-08-27，没有重建或清空。
- 调度配置补齐为 39 项、启用 35 项，保留全局并发 10 和用户资源池配置。推荐策略 ID 切换为 `strategy:ashare:adaptive_v1`；市场、板块、历史验证、前向结算和验证门控均已接线。
- `/api/health`、持仓、推荐和调度接口返回成功。启动恢复扫描后调度器心跳持续更新，API 与调度器配置摘要一致；新闻采集进度从 89% 增至 96%，并非固定 0%。
- 持仓监控状态每约 5 秒更新，`status=completed`、`error_count=0`；当前为空仓，不能据此推断有真实持仓时的效果。策略验证命令返回 `research`、`strategy_state_missing`、`allow_new_buys=false`，未手工放行。
- 浏览器检查覆盖 1440×900、390×844；持仓、推荐、任务监控页面未发现整页横向溢出，控制台无错误。新前端显示新增买入为 0，旧推荐保留在等待入场分组，不属于已验证新机会。
- 本批复验：Web `/` 与 `/index.html` 返回 `Cache-Control: no-cache, must-revalidate`，生产资源为新哈希 `index-mwzr9BnS.js`；生命周期分组保持用户选择，`buy_candidate` 显示为“候选待确认”；GoTDX 低层纯行情请求已加入身份/数量/重复/缺失校验、异常连接清理与一次重连；市场快照依赖先收敛最新父代次，失败/重启不会倒序回放历史。前端桌面 1440×900 现场交互通过，未发现整页横向溢出。
- 本批提交后的新镜像联调、1/10/50/80 股单包和 100 股分批往返已在下述最终发布验收中完成；真实交易时段连续 120 分钟压力仍未完成，不得仅以容器 healthy 宣称整体计划完成。
- 快照数据不足有真实依据：现场采样 47/5208 个标的满足 61 根日 K 和同截面要求，另有 3137 个过期、2024 个缺失。不得调低门槛或伪造时间戳以消除阻塞。

### 2026-09-05 21:09 最终滚动发布与现场验收

- 最终发布提交为 `7738acd`，分支为 `codex/ashare-adaptive-recommendation-next`。发布源目录为 `runtime/releases/20260905-150324/source-forward-index-181810`，压缩包 SHA-256 为 `6C5EDADD4F86E4112C554835F1FFE57082CC1C352D715F65A742C048A7EA21CE`；本批 14 个相关文件经 Git 属性规范化后与该提交全部一致。
- 新镜像 ID：API `97c70a69007d`、Scheduler `0d5a93fb2d34`、Realtime Monitor `145597631ff3`、Position Monitor `8a2108b2fb84`、MCP `752229658792`、Migrate `0f851620de48`、Web `0c1da2d192dd`、GoTDX `e5d9a869c414`。滚动发布前镜像使用 `rollback-20260905-210718` 标签保留。
- 七个业务容器均从最终发布目录的 Compose 配置启动，运行中且 `RestartCount=0`；API、Web、MCP 和 GoTDX 健康检查为 healthy。PostgreSQL、Redis 已连续运行 9 天，发布过程未重建容器、未删除命名卷。
- 数据库迁移版本为 `20260904_0031`，`idle in transaction=0`。`analytics.snapshot.ashare.close` 仍为 59 条，最新任务创建时间为 17:31，晚于 21:09 的部署未生成历史倒序回放。
- 调度器状态为 healthy，任务配置 39 项、启用 35 项、全局并发 10，配置摘要与 API 一致且无漂移。任务定义视图显示全部 45、运行中 2、已暂停 0、等待中 7、已完成 34、失败 2；本次浏览器观察中，A 股新闻进度从 10% 增至 18%，开放式基金净值从 8% 增至 9%，运行中任务不再固定显示 0%。
- 部署后手工执行 `analytics.strategy.forward_settlement.daily`，从 21:12:45 到 21:12:57 完成，耗时约 12.23 秒，任务事件返回 `status=available`；修复前同一查询耗时 42 分 45 秒。
- GoTDX 对 1/10/50/80 只有效实时标的的单包请求均返回 HTTP 200，单次约 20 至 26 毫秒；81 只请求按协议返回 HTTP 400；Python provider 对 100 只自动拆为 50+50，两个批次均返回 HTTP 200，身份、数量和顺序一致，总耗时约 56 毫秒。使用退市历史代码得到非请求证券时按设计 fail-closed 为 HTTP 502，不把错误身份写入事实层。
- 上线后雷达通道的 280 次样本中 274 次成功。6 次 3 秒读取超时集中在手工异常/边界行情验收窗口；网关用单个 TCP 客户端和互斥锁串行访问，异常连接的两次尝试会让并发监控请求等待并超时。该窗口之后连续 177 次成功，成功请求 P95 为 0.642 秒，最近一次收到 109/109；失败轮次没有进入可执行动作。后续生产压力验收不得与异常帧测试共用同一网关实例，应使用隔离网关，且不得靠放宽持仓时效门槛掩盖竞争。
- 持仓监控约每 5 秒刷新，最近三次均为 `status=completed`、`error_count=0`；组合接口返回空仓。策略验证结果为 `research`、`matured_t20_count=0`、`allow_new_buys=false`，原因包含缺少验证证据、T+20 样本不足及超额收益指标无效，未进行人工放行。
- 浏览器在 1440x900 和 390x844 下复核任务定义视图，均无整页横向溢出；连续快速重载 8 次未出现 `signal is aborted without reason`，warning/error 控制台记录为 0。桌面端和移动端均能看到非零实时进度。
- 最终提交复验：Python 全量测试 `1427 passed, 2 skipped, 30 warnings`，耗时 137.34 秒；Ruff 通过；Go `go test ./...` 通过；前端 12 个 `test-*.mjs` 全部通过；Vite 转换 3130 个模块并构建成功，仅保留 chunk 体积提示。
- 本次 Docker 构建成功，但核心 pip 安装步骤曾耗时 9926.9 秒。当前镜像构建依赖未完整锁定且外部 PyPI 下载极慢，这是可重复构建风险；后续需引入锁文件、稳定缓存或内部镜像源，不能把本次成功等同于构建链路已稳定。
- 本批生产修复已上线并完成周末可执行范围内的联调；整体 A 股改造仍受真实交易时段连续 120 分钟压力、真实持仓场景和阶段 04 完整 walk-forward 指标关卡约束，继续保持 `allow_new_buys=false`。

## 真实数据上线前提

- 候选池成员历史需持续积累至少 120 个独立截面。
- 策略需积累至少 60 个成熟 T+20 前向样本，并通过收益、置信区间、阶段、回撤、Rank IC、换手和可成交性门槛。
- 在真实数据不足期间，`allow_new_buys=false` 属于设计行为，不应通过手工配置绕过。
