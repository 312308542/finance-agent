# GitNexus 差异记录

> 用途：开发过程中如 GitNexus 索引落后、MCP 不可用或只能使用 CLI fallback，在此记录差异和处理方式。全部开发完成后由总负责人统一执行 `gitnexus analyze` 更新索引。

| 日期 | 当前任务 | 差异项 | 处理方式 |
| --- | --- | --- | --- |
| 2026-06-13 | 批次 4 / 09-T4 | GitNexus MCP 可调用，但 `finance-agent` 索引落后 25 个提交；修改 `orm.py` 的声明式模型前对 `Base` 执行 upstream impact，风险为 CRITICAL，原因是所有 ORM 类都继承 `Base`；修改 `repositories.py` 前执行文件级 upstream impact，风险为 CRITICAL，原因是仓储聚合文件被大量服务导入；新增 `BacktestResultORM`、`BacktestRepository` 和 0019 迁移尚未纳入索引 | 本轮限定为追加独立 `backtest_results` 表模型、仓储和导出，不修改 `Base` 行为、不改既有 ORM 字段、不改既有 Repository 方法签名；先写 `tests/test_backtest_persistence.py` 红灯测试确认 ORM/Repository/迁移缺失，再实现 `BacktestRepository.upsert_result/get_latest_result/list_results` 和 `20260614_0019_create_backtest_tables.py`，中文 DDL 注释齐全；验证 `pytest tests/test_backtest_persistence.py -q` 4 passed，`alembic upgrade head` 成功执行 0019；提交前 staged `detect_changes` 返回 high，changed symbols 归到相邻的 `SignalSnapshotORM`、`SignalSnapshotRepository` 和 `RecommendationRepository`，经 `git diff --cached` 复核实际只是在这些旧类之前插入新 ORM/Repository，未改旧方法实现，判断为索引行号漂移 |
| 2026-06-13 | 批次 4 / 09-T3 | GitNexus MCP 可调用，但 `finance-agent` 索引落后 25 个提交；T2 新增的 `BacktestResult` 尚未纳入索引，`impact(BacktestResult)` 返回 not found；本轮新增 `BacktestService` 和 `tests/test_backtest_service.py` 尚不在索引中 | 已执行 `query(asset_scores ScoringService MarketDataRepository market_bars list bars score strategy repository)` 定位评分与行情上下文，并确认本轮不修改既有 `ScoringService`、`MarketDataRepository`、`AssetScoreRepository` 或推荐链路；先写 T3 红灯测试覆盖 replayed/historical/partial/未知模式拒绝，再新增独立 `backtesting.service` 端口和服务，实现 TopN 选股、宽表价格构造和 `BacktestResult` 输出；验证回测相关测试 7 passed、全量后端 `438 passed`、前端 build 通过，提交前继续执行 staged `detect_changes` |
| 2026-06-13 | 批次 4 / 09-T2 | GitNexus MCP 可调用，但 `finance-agent` 索引落后 24 个提交；回测模块为本轮新增，`query(BacktestResult PerformanceReport bt quantstats adapter domain protocol)` 主要命中文档，新增 `finance_agent.backtesting.*` 和 `tests/test_backtest_adapters.py` 尚不在索引中 | 已读取 `$gitnexus-codegraph` skill 并执行 `query`；本轮限定为新增独立回测适配器包，不修改推荐、报告、调度或存储既有业务符号；先写 `tests/test_backtest_adapters.py` 红灯测试确认模块缺失，再实现 `BacktestResult`、`PerformanceReport`、`BtBacktestAdapter.run_equal_weight` 和 `QuantstatsPerformanceAdapter.analyze`，第三方库仅在 `adapters.py` 内调用；测试中发现 quantstats `win_rate` 会排除 0 收益期，已按领域字段 `period_win_rate` 改为项目自算周期胜率并记录口径；验证适配器组合测试 3 passed、全量后端 `434 passed`，提交前继续执行 staged `detect_changes` |
| 2026-06-13 | 批次 4 / 09-T1 | GitNexus MCP 可调用，但 `finance-agent` 索引落后 23 个提交；回测模块尚未实现，`query(backtesting dependency pyproject bt quantstats performance report)` 主要命中文档和既有 `collect_performance_report` 财报采集符号；修改 `pyproject.toml` 前执行 `impact(pyproject.toml)`，风险 LOW、无受影响流程；本轮新增的 `tests/test_backtest_dependencies.py` 尚不在索引中 | 已读取 `$gitnexus-codegraph` skill 并执行 `list_repos`、`query`、`impact(pyproject.toml)`；本轮限定为 T1 依赖确认，不新增回测业务模块、不修改推荐/报告链路；先写 `tests/test_backtest_dependencies.py` 红灯测试确认依赖缺失，再声明 `bt>=1.1,<2.0` 与 `quantstats>=0.0.64,<1.0`，通过项目 venv `pip install -e .` 安装并验证可导入 `bt 1.2.0` / `quantstats 0.0.81`；提交前继续执行 staged `detect_changes` |
| 2026-06-13 | 批次 4 / 08-T8 | GitNexus MCP 可调用，但 `finance-agent` 索引落后 22 个提交；本轮新增的 `tests/test_development_progress_docs.py::test_action_loop_docs_reflect_08_completion` 未纳入索引，`context(test_action_loop_docs_reflect_08_completion)` 返回 not found | 已读取 `$gitnexus-codegraph` skill 并执行 `list_repos`、`context(test_action_loop_docs_reflect_08_completion)`；本轮仅做文档同步和文档一致性测试，不修改既有业务函数、类、方法或 API 行为；先写文档红灯测试确认 README、项目计划、项目进度、优化版本进度、00 总览和 08 方案未同步，再更新 O9/V2.0 完成态和“系统不接券商或交易所写接口、不自动真实下单”边界；验证 `tests/test_development_progress_docs.py` 3 passed、`alembic upgrade head`、08 服务/API 回归 19 passed、真实闭环 smoke、08 相关组合回归 27 passed、前端 build 和全量后端 `431 passed`，提交前继续执行 staged `detect_changes` |
| 2026-06-13 | 批次 4 / 08-T7 | GitNexus MCP 可调用，但 `finance-agent` 索引落后 21 个提交；08-T2 后新增的 `ActionLoopService` 仍未纳入索引，`context(ActionLoopService)` 返回 not found；本轮新增 `scripts/storage/smoke_action_loop_closed_cycle.py` 和 `tests/test_action_loop_closed_cycle_smoke.py` 尚不在索引中 | 已读取 `$gitnexus-codegraph` skill 并执行 `list_repos`、`query(ActionLoopService order draft execution record review task memory closed loop smoke script)`、`context(ActionLoopService)`、`context/impact(MemoryService)`、`context/impact(PortfolioService)`；本轮只新增端到端 smoke 脚本和测试，不修改既有服务/仓储/API 业务符号，不引入真实下单或外部模型/行情调用；先写 `tests/test_action_loop_closed_cycle_smoke.py` 红灯测试确认脚本缺失，再实现 `--dry-run` 契约和真实 PostgreSQL 闭环验证；验证 dry-run pytest、真实 smoke、`alembic upgrade head` 和 08 相关回归通过，提交前继续执行 staged `detect_changes` |
| 2026-06-13 | 批次 4 / 08-T6 | GitNexus MCP 可调用，但 `finance-agent` 索引落后 20 个提交；前端 07 拆分后新增的 `RecommendationPage`、`PortfolioPage`、`RiskPage`、`actionLoopView.ts` 以及 `buildExecutionPayload` 均未纳入索引，`impact` 返回 Target not found；`api.ts` 文件级 impact 也因索引文件映射落后返回 not found | 已读取 `$gitnexus-codegraph` skill 并执行 `list_repos`、`impact(RecommendationPage)`、`impact(PortfolioPage)`、`impact(RiskPage)`、`impact(api.ts)`、`impact(buildExecutionPayload)`；本轮限定为前端闭环接入和视图模型新增，不修改后端服务/仓储业务逻辑，不引入真实下单或自动交易；先写 `test-action-loop-view.mjs` 红灯测试，再实现 API 封装、推荐确认/草案、持仓执行登记/历史、风险待复盘提醒；验证 T6 专项 node 测试、相关前端回归和前端 build 通过，提交前继续执行 staged `detect_changes` |
| 2026-06-13 | 批次 4 / 08-T5 | GitNexus MCP 可调用，但 `finance-agent` 索引落后 19 个提交；08-T2~T4 新增的 `ActionLoopService`、`ActionLoopRepository` 仍未纳入索引，`context(ActionLoopService)` 与 `context(ActionLoopRepository)` 返回 not found；新增 API 前对 `routes.py`、`schemas.py`、`submit_decision_feedback`、`list_pending_confirmation_decisions` 执行 impact，风险均为 LOW；一次误跑 `detect_changes(scope=unstaged)` 因历史 diff/索引映射被截断且风险为 CRITICAL，不作为提交依据 | 已读取 `$gitnexus-codegraph` skill 并执行 `list_repos`、`api_impact(file=src/finance_agent/api/routes.py)`、`context/impact(submit_decision_feedback)`、`impact(list_pending_confirmation_decisions)`、`impact(routes.py)`、`impact(schemas.py)`；本轮限定为 API 层追加请求模型、路由和序列化函数，不修改 `ActionLoopService`/`ActionLoopRepository` 已有业务逻辑，不引入真实下单；先写 `tests/test_action_loop_api.py` 红灯测试，再实现 6 个闭环端点；验证 `tests/test_action_loop_api.py` 6 passed、08 相关回归 26 passed、全量后端 `429 passed`；staged `detect_changes(scope=staged,max_files=50,max_hunks=300)` 无 warnings、未截断，风险 MEDIUM，受影响流程映射到既有 `upsert_model_provider` API 流程，判断为 `routes.py`/`schemas.py` 行号漂移导致的索引归因偏差 |
| 2026-06-13 | 批次 4 / 08-T4 | GitNexus MCP 可调用，但 `finance-agent` 索引落后 18 个提交；08-T2/T3 新增的 `ActionLoopService` 与 `ActionLoopRepository` 尚未纳入索引，`context/impact(ActionLoopService)` 与 `context/impact(ActionLoopRepository)` 返回 not found；本轮需要修改调度核心 `BaseDataScheduler.execute_job/run_job`，GitNexus upstream 风险为 HIGH；`build_trigger_scheduler_jobs` upstream 风险为 CRITICAL；`MarketDataRepository` 作为共享行情仓储 upstream 风险为 CRITICAL；staged `detect_changes` 返回 `risk_level=high`，受影响流程集中在 `Execute_job` / `Run_job` 调度分发链路 | 已读取 `$gitnexus-codegraph` skill 并执行 `list_repos`、`query(review_tasks complete_review_task compare execution outcome action loop scheduler analytics.reviews.due)`、`context(BaseDataScheduler/build_trigger_scheduler_jobs/MarketDataRepository)`、`impact(complete_review_task)`、`impact(execute_job)`、`impact(run_job)`、`impact(build_trigger_scheduler_jobs)`、`impact(MarketDataRepository)`；本轮不修改 `MemoryService.complete_review_task`、不修改 `MarketDataRepository` 签名，只在 `ActionLoopService` 新增复盘编排并通过注入 `latest_price_loader` 方便测试；调度器仅追加 `reviews_due` job_type、kwargs 构造、注入执行器和默认运行函数，配置导出仅追加 `analytics.reviews.due` 任务；验证 `tests/test_action_loop_service.py` 13 passed、08 相关回归 27 passed、调度/配置回归 137 passed、全量后端 `423 passed`；staged `detect_changes(scope=staged,max_files=50,max_hunks=300)` 无 warnings、未截断 |
| 2026-06-13 | 批次 4 / 08-T3 | GitNexus MCP 可调用，`finance-agent` 索引落后；08-T2 新增的 `ActionLoopService` 尚未纳入索引，`context/impact(ActionLoopService)` 返回 not found；`PositionORM` upstream 风险为 HIGH，`PortfolioService.upsert_position` upstream 风险为 CRITICAL，主要原因是持仓模型和服务被多个工作流/冒烟脚本复用 | 已在写持仓逻辑前执行 `query(position portfolio upsert position execution records holdings update)`、`context(PositionORM/PortfolioRepository/PortfolioService)`、`impact(PositionORM)`、`impact(PortfolioRepository.upsert_position)`、`impact(PortfolioService.upsert_position)`；本轮不修改 `PositionORM`、不修改 `PortfolioService.upsert_position` 或仓储签名，只在新增 `ActionLoopService.record_execution` 内通过注入端口编排：预校验持仓变更、写 `execution_records`、调用既有持仓服务、沉淀 `execution_note` 和复盘任务；验证服务测试 10 passed、相关回归 31 passed、全量后端 `418 passed`，提交前继续执行 staged `detect_changes` |
| 2026-06-13 | 批次 4 / 08-T2 | GitNexus MCP 可调用，`finance-agent` 索引落后 16 个提交；T2 新增的 `ActionLoopService` 尚未纳入索引，T1 新增的 `ActionLoopRepository` 也无法通过 context 精确查询；`record_user_feedback` 初次查询与测试 fake 方法重名，需要按 `src/finance_agent/application/memory_service.py` disambiguate | 已读取 `$gitnexus-codegraph` skill 并执行 `list_repos`；对 `MemoryService.record_user_feedback` 使用 `file_path` 精确执行 `context` 与 `impact`，upstream 风险为 LOW，直接影响仅两个 smoke 脚本；本轮为新增服务封装与 `application.__init__` 导出，不修改 `MemoryService.record_user_feedback` 既有实现、不改变反馈 API；验证 08-T2 相关测试 19 passed、全量后端 `413 passed`，提交前继续对 staged 范围执行 `detect_changes` |
| 2026-06-13 | 批次 4 / 08-T1 | GitNexus MCP 可调用，`finance-agent` 索引落后 15 个提交；`DecisionLogORM`、`ReviewTaskORM` 和 `repositories.py` upstream 影响面为 HIGH/CRITICAL，主要原因是存储层共享导入点被大量模块直接引用 | 已读取 `$gitnexus-codegraph` skill，并执行 `list_repos`、`query`、`context(DecisionLogRepository)`、`impact(DecisionLogORM)`、`impact(ReviewTaskORM)`、`impact(DecisionLogRepository)`、`impact(repositories.py)`；本轮按低风险追加策略执行：只新增 `order_drafts` / `execution_records` 迁移、`OrderDraftORM` / `ExecutionRecordORM`、`ActionLoopRepository` 和导出，不改既有 `decision_logs`、`review_tasks`、`positions` 字段或已有仓储方法；验证 `alembic upgrade head`、`tests/test_action_loop_repository.py tests/test_decision_feedback_api.py` 7 passed、全量后端 `408 passed` |
| 2026-06-13 | 批次 4 / 07-T9 | GitNexus MCP 可调用，`finance-agent` 索引仍落后；T9 为文档同步和文档一致性测试，不修改业务符号 | 已用 GitNexus `query` 查询 README/Web 控制台/前端闭环相关文档入口；先新增 `tests/test_development_progress_docs.py` 红灯测试确认 README、O8 总览、项目进度和 00 总览未同步，再更新 `README.md`、`docs/优化版本进度跟踪表.md`、`docs/项目进度跟踪表.md`、`docs/开发方案/00-总体规划与执行约定.md` 和 07 方案进度表；验证文档一致性测试、07 相关前端脚本、`tests/test_console_read_api.py`、前端 build 和 `git diff --check` 均通过，提交前对 staged 范围执行 `detect_changes` |
| 2026-06-13 | 批次 4 / 07-T7 | GitNexus MCP 可调用，`finance-agent` 索引落后 13 个提交；按总负责人要求继续开发并记录差异 | 已读取 `$gitnexus-codegraph` skill，并在拆分既有前端组件前对 `OverviewPage`、`TaskMonitorPage`、`OpenAICompatibleModelPanel`、`ChatDock`、`DetailPage` 执行 GitNexus `impact`，upstream 风险均为 LOW；本轮为纯前端结构拆分，`main.tsx` 保留导航、刷新、聊天流和页面挂载，迁出 overview/detail/task/data/model/chat/common 组件，不改变 API 语义或后端逻辑；验证全部 10 个前端 `test-*.mjs` 脚本、`tests/test_console_read_api.py`、前端 build 和 `git diff --check` 均通过，提交前对 staged 范围执行 `detect_changes` |
| 2026-06-13 | 批次 4 / 07-T6 | GitNexus MCP 可调用，`finance-agent` 索引落后 12 个提交；按总负责人要求继续开发并记录差异 | 已读取 `$gitnexus-codegraph` skill，并在修改 `DetailPage` 挂载链路前执行 `context(DetailPage)` 与 `impact(DetailPage)`，upstream 风险为 LOW，直接调用方仅 `App`；本轮新增 `consolePagesView.ts`、portfolio/risk/agent/memory 四个独立页面和四个 API 封装，`main.tsx` 只切换对应 tab 挂载，不改旧 overview/watchlist/data/task/model 行为；验证四个前端 view 脚本、`tests/test_console_read_api.py`、前端 build 和 Playwright/Edge 桌面/移动端截图测量均通过 |
| 2026-06-13 | 批次 4 / 07-T5 | GitNexus MCP 可调用，`finance-agent` 索引落后 10 个提交；按总负责人要求继续开发并记录差异 | 修改 `DashboardService.get_portfolio_overview` 与 `get_risk_overview` 前执行 `impact`，二者 upstream 风险均为 LOW，直接影响 `build_summary`；本轮只做兼容增量字段：组合集中度基于已有持仓字段和 payload，不做外部补数；风险概览新增最近 `risk_findings` 和严重度分布，不改变 triggers/alerts/data_quality 原字段；先写红灯测试确认字段缺口，再实现并验证 `tests/test_console_read_api.py` 7 passed、既有前端 view 脚本通过 |
| 2026-06-13 | 批次 4 / 07-T4 | GitNexus MCP 可调用，`finance-agent` 索引仍落后；按总负责人要求继续开发并记录差异 | 修改既有 `RecommendationPanel`/`App` 前执行 `context` 和 `impact`，upstream 风险均为 LOW；本轮新增 `recommendationView.ts`、`src/pages/RecommendationPage.tsx`，`main.tsx` 仅把 recommendation tab 挂到真实页面并增加 `#report:` 轻量跳转；推荐数据仍全部通过后端 API 读取，反馈只调用既有决策反馈 API，不在前端伪造决策；验证 view 脚本、后端反馈 API 测试、前端 build 和 Playwright/Edge 交互测量通过 |
| 2026-06-13 | 批次 4 / 07-T3 | GitNexus MCP 可调用，`finance-agent` 仓库可见，但 `list_repos` 提示索引落后 9 个 commit；按总负责人要求不中断开发，记录差异，全部开发完成后统一更新索引 | 已读取 `$gitnexus-codegraph` skill；修改既有 `ReportPanel`/`DetailPage` 前执行 `context` 和 `impact`，二者 upstream 风险均为 LOW，直接影响仅在前端 `App` 挂载链路；本轮删除占位 `ReportPanel`，新增 `reportView.ts`、`src/pages/ReportPage.tsx` 和 `loadReports/loadReportDetail`，不改后端接口语义；验证 `test-report-view.mjs`、`test-task-monitor-view.mjs`、`tests/test_console_read_api.py`、前端 build 和 Playwright/Edge 截图测量通过 |
| 2026-06-12 | 批次 4 / 07-T2 | GitNexus MCP 可调用但索引仍落后；新增 API 前已使用 `api_impact(file=src/finance_agent/api/routes.py)` 和 `impact(DashboardService)`，`DashboardService` upstream 风险为 LOW，routes.py 现有端点均无直接消费者 | 先写 `tests/test_console_read_api.py` 红灯测试，再新增 `DashboardService.get_report_list/get_alert_center/get_recent_memories` 和 `GET /api/reports`、`GET /api/alerts`、`GET /api/memories/recent`；路由只透传服务层，事实全部来自入库表；验证 `tests/test_console_read_api.py tests/test_dashboard_service.py tests/test_workflow_run_details.py tests/test_decision_feedback_api.py` 11 passed |
| 2026-06-12 | 批次 4 / 07-T1 | GitNexus MCP 已可调用，`finance-agent` 仓库可见，但 `list_repos` 提示索引落后 7 个 commit；按总负责人要求先不中断开发，记录差异，全部开发完成后统一更新索引 | 已读取 `$gitnexus-codegraph` skill，并使用 `route_map`、`context(FinanceAgentInterface/DashboardService)`、`impact(FinanceAgentInterface.get_report/find_report_from_events)`；`get_report` impact 为 HIGH，直接调用者为 CLI `dispatch_reports`，本轮仅做兼容增量字段和 Markdown 追加，不改变原有 `report` / `markdown` 结构；验证 `tests/test_console_read_api.py tests/test_workflow_run_details.py tests/test_decision_feedback_api.py` 6 passed |
| 2026-06-12 | 批次 3 / 06-T2 | 总负责人再次确认 GitNexus MCP 与 `$gitnexus-codegraph` skill 均存在；本轮已读取 skill，并通过 `tool_search` 检索 `gitnexus MCP code graph impact context detect changes list repos` 与 `list_repos gitnexus`，结果均为 0，`list_mcp_resources` 也为空，当前会话仍无法调用 `gitnexus_impact` / `gitnexus_context` / `gitnexus_detect_changes` | 不使用 `npx` 或 CLI 代替 MCP；本轮修改符号清单：`CollectionRuntime.run_task`、`summarize_archive`、新增 `summarize_archives` / `merge_archive_statuses` / `infer_time_range_payload`，`resolve_fund_bar_collection_symbols`、`resolve_fund_nav_collection_symbols`、`batch_fund_bar_symbols`、`batch_open_fund_nav_symbols`、`_asset_requires_fund_bar_collection`、`_record_fund_symbol_watermark`、新增 `result_watermark_at`，以及 `FundDataCollector.collect_universe` / `_deduplicate_fund_assets_by_symbol` / `AssetRepository.delete_fund_open_placeholders_without_nav` 等 T2 前序未提交改动；已用 PyCharm MCP、pytest 和真实小批采集验证，后续由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 1 / 02-T1 | 总负责人已重新执行 `gitnexus analyze`，当前 `Indexed commit` 与 `Current commit` 均为 `d028988`，暂无索引差异 | 后续如提交后索引落后，不再中断开发；记录差异并在全部开发完成后统一更新 |
| 2026-06-12 | 批次 1 / 02-T2 | `C:\Users\Administrator\.codex\config.toml` 已配置 `[mcp_servers.gitnexus]`，但当前 Codex 会话可调用工具列表未暴露 GitNexus MCP namespace，`tool_search` 未检索到 `gitnexus` 工具，`list_mcp_resources` 返回空，无法在编辑前通过 MCP 执行 `gitnexus_impact` | 不使用 `npx` 或 CLI 代替 MCP；沿用前序已完成的 `export_scheduler_payload` CRITICAL 影响分析结论，保持 `sync_config.py` 纯追加改动，并在全部开发完成后由总负责人统一更新索引 |
| 2026-06-12 | 批次 1 / 02-T3/T4 | 修改 `BaseDataScheduler.__init__`、`run_job`、`execute_job`、新增 `build_trigger_evaluation_kwargs`、`build_agent_loop_consume_kwargs`、`run_trigger_evaluation`、`run_agent_loop_consume` 前，GitNexus MCP namespace 仍未暴露，无法通过 MCP 执行 `gitnexus_impact` / `gitnexus_detect_changes` | 使用 PyCharm MCP 和既有测试定位影响面；改动保持为调度器新增 job_type 分支和可注入执行器，不改已有 collection/recommendation/data_quality/technical_screening 分支；相关回归 `93 passed`，全部开发完成后由总负责人统一更新 GitNexus 索引 |
| 2026-06-12 | 批次 1 / 02-T5 | 修改 `TriggerEvaluationRequest`、`TriggerEvaluationResult`、`TriggerService.evaluate`，新增 `_evaluate_intraday_volatility_triggers`、`_load_recent_realtime_quote_snapshots`、`trigger_group_enabled` 等盘中规则辅助函数前，GitNexus MCP namespace 仍未暴露，无法执行 `gitnexus_impact` | 使用 PyCharm MCP 定位 `TriggerService`，按方案限定只读取入库 `realtime_quote_snapshots`，不改既有持仓/观察池/推荐/风险/质量规则语义；新增 TDD 测试 `tests/test_intraday_trigger_rules.py`，相关验证 `3 passed` + 调度回归 `93 passed` |
| 2026-06-12 | 批次 1 / 02-T6 | 修改 `JOB_TYPES`、`TRADING_DAY_POLICIES` 和 `as_job_group_choice` 前，GitNexus MCP namespace 仍未暴露，无法执行 `gitnexus_impact` / `gitnexus_detect_changes` | 由 runtime 解析红灯暴露缺口，最小扩展调度配置白名单；`runtime/` 为本地忽略文件，已重新导出但不提交；验证 `parse_scheduler_config` 可解析 34 个 jobs，相关 pytest `97 passed` |
| 2026-06-12 | 批次 1 / 02-T7/T8 | `C:\Users\Administrator\.codex\config.toml` 中确认存在 `[mcp_servers.gitnexus]`，但当前 Codex 会话工具清单仍未暴露 GitNexus MCP namespace；`tool_search` 未检索到 GitNexus 工具，`list_mcp_resources` 返回空 | T7/T8 仅做真实冒烟与文档同步，不新增业务符号；已记录 `--only` 参数与当前脚本不一致的验收差异，后续全部开发完成后由总负责人统一执行 GitNexus 更新和影响复核 |
| 2026-06-12 | 批次 2 / 03-T2/T3 | 当前 Codex 会话仍未暴露 GitNexus MCP namespace，无法在新增 `HighRiskReviewService` 前通过 MCP 执行 `gitnexus_impact`，也无法在提交前执行 `gitnexus_detect_changes` | 先按 TDD 新增红灯测试，再新增独立服务文件；当前阶段不改既有 Workflow 同步路径，不新增真实外部模型调用，测试使用 fake model client；后续全部开发完成后由总负责人统一执行 GitNexus 更新和影响复核 |
| 2026-06-12 | 批次 2 / 03-T4 | 修改 `finance_agent.cli.main` 注册 `agent review-pending` 前，当前 Codex 会话仍未暴露 GitNexus MCP namespace，无法执行 `gitnexus_impact` / `gitnexus_detect_changes` | 先写 CLI 红灯测试，再最小增加 `review-pending` 子命令；dry-run 只列待复核队列，非 dry-run 调用 `HighRiskReviewService.run_pending_reviews`，不改变既有 `run-once/run-task/run-loop` 行为 |
| 2026-06-12 | 批次 2 / 03-T5 | 修改 `JOB_TYPES`、`BaseDataScheduler.__init__`、`run_job`、`execute_job`、`build_trigger_scheduler_jobs` 前，当前 Codex 会话仍未暴露 GitNexus MCP namespace，无法执行 `gitnexus_impact` / `gitnexus_detect_changes` | 先写调度配置与执行分发红灯测试；实现限定为新增 `high_risk_reviews` 任务类型、可注入执行器和 after Agent/兜底任务导出，不改既有采集、触发评估或 Agent 消费行为；后续全部开发完成后由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 2 / 03-T8 | 新增 `finance_agent.agents.chat_summary.ChatSummaryService` 前，当前 Codex 会话仍未暴露 GitNexus MCP namespace，无法执行 `gitnexus_impact` / `gitnexus_detect_changes` | 先写聊天摘要红灯测试，限定为新增独立服务与默认 SQLAlchemy store；不改变现有聊天主路径，不触发真实模型调用，测试通过 fake model client 和 fake store 验证 JSON 摘要、会话 summary 回写和 `chat_summary` 记忆沉淀 |
| 2026-06-12 | 批次 2 / 04-T1/T2 | 已按 `$gitnexus-codegraph` 技能检查 GitNexus 可用性；技能文件存在，`C:\Users\Administrator\.codex\config.toml` 已配置 MCP，但当前 Codex 会话可调用工具仍未暴露 `gitnexus_*` / `list_repos`，`tool_search` 未检索到 GitNexus 工具，`list_mcp_resources` 返回空，无法在新增圆桌模型节点前执行 `gitnexus_impact` | 不使用 `npx` 或 CLI 代替 MCP；先按 TDD 新增独立红灯测试和独立模块，保持规则版圆桌函数不删除、不改现有 Workflow 主路径；使用 PyCharm MCP 与 pytest 验证，全部开发完成后由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 2 / 04-T3 | 接入 `langgraph_graphs.py` 中 `build_roundtable_opinions`、`build_report_roundtable_opinions` 和三个 `roundtable_discussion` 节点前，GitNexus MCP namespace 仍未暴露；PyCharm `analyze_calls` 也无法为 Python 函数生成调用层级，只能通过 `search_symbol` / `search_text` 定位调用点 | 先写 `tests/test_roundtable_workflow_model_integration.py` 红灯测试；实现保持规则版函数为第一层 fallback，新增模型增强 helper，Workflow 节点只在圆桌观点生成后包一层增强，不改变节点顺序、DTO 或落库链路 |
| 2026-06-12 | 批次 2 / 04-T4 | 修改 `build_chinese_decision_report`、`build_roundtable_section` 和 `render_report_markdown` 前，GitNexus MCP namespace 仍未暴露；PyCharm `analyze_calls` 对 Python 函数返回不可用，只能通过文本检索定位三处调用 | 先写报告模板红灯测试；改动限定为圆桌观点结构化字段透传和 Markdown 渲染，不改变决策、复核、数据质量和模型路由章节语义 |
| 2026-06-12 | 批次 2 / 04-T5 | 新增决策反馈 API 前，GitNexus MCP namespace 仍未暴露，无法对 `routes.py`、`schemas.py` 和 `MemoryService.record_user_feedback` 做 MCP 影响分析；通过文本检索确认现有 API 使用统一 `status/data` 返回结构，`MemoryService.record_user_feedback` 已存在 | 先写 `tests/test_decision_feedback_api.py` 红灯测试；实现限制为新增 `POST /api/decisions/{decision_id}/feedback` 和 `GET /api/decisions/pending-confirmation`，复用 `MemoryService.record_user_feedback`，不改变既有路由行为 |
| 2026-06-12 | 批次 3 / 05-T1/T2 | 已按 `$gitnexus-codegraph` 技能检查 GitNexus 可用性；当前 Codex 会话仍未暴露 `gitnexus_*` / `list_repos` MCP 工具，`tool_search` 未检索到 GitNexus 工具，无法在修改 `UniverseRecommendationPipeline.run_for_universe` 和 `RecommendationService.rank_from_screening` 前执行 MCP impact，也无法提交前执行 MCP detect_changes | 不使用 `npx` 或 CLI 代替 MCP；先写 `tests/test_recommendation_avoid_pool.py` 红灯测试，再最小实现回避池剔除和审计透传；使用 PyCharm MCP 读取仓储/服务上下文和 lint，验证 `tests/test_recommendation_avoid_pool.py tests/test_recommendation_pipeline_guard.py` 7 passed，后续全部开发完成后由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 3 / 05-T3 | 修改 `sync_config.py` 调度导出、`BaseDataScheduler` 任务类型白名单和执行分发前，GitNexus MCP namespace 仍未暴露，无法执行 MCP impact / detect_changes；通过 PyCharm MCP 检查 `ProductionUniverseService.merge_universes`、`rebuild_avoid_pool` 和既有调度注入模式 | 先写配置导出、配置解析和调度分发红灯测试；实现限定为新增 `universe_merge` / `universe_avoid_pool_rebuild` 两类 analytics job，复用既有 `ProductionUniverseService`，不改候选池合并和回避池策略内部算法；验证 `tests/test_data_sync_config.py tests/test_base_data_scheduler_analytics.py` 120 passed |
| 2026-06-12 | 批次 3 / 05-T4~T6 | 修改 `ScoringService.score_screening`、`compute_asset_score`、新增 `ScoringStrategyRepository`、`ScoringStrategyORM`、`data strategy` CLI 前，GitNexus MCP namespace 仍未暴露，无法执行 MCP impact / detect_changes；PyCharm `analyze_calls` 对 Python FQN 返回不可用，仅能通过 `search_symbol` / `search_text` 定位调用点 | 先写 `tests/test_scoring_strategies.py` 和 `tests/test_data_strategy_cli.py` 红灯测试；实现保持未传 `strategy_id` 时内置权重路径不变，新增策略路径只写 payload 快照，不改 `asset_scores` 表结构；CLI `set` 限定 draft 策略；验证 T4-T6 单测 `9 passed`，方案相关回归 `39 passed`，后续全部开发完成后由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 3 / 05-T7 | 修改 `build_recommendation_scheduler_job`、`build_recommendation_scheduler_jobs`、`BaseDataScheduler.build_recommendation_pipeline_kwargs`、`UniverseRecommendationPipeline.run_for_universe` 前，GitNexus MCP namespace 仍未暴露，无法执行 MCP impact / detect_changes；通过 PyCharm MCP 和文本检索定位推荐任务参数链路 | 先写配置导出、调度 kwargs 和流水线评分透传红灯测试；实现限定为新增 `strategy_id` / `avoid_universe_id` 参数透传，不改变候选池、筛选、因子或推荐排序算法；验证 T7 三测 `3 passed`，方案相关回归 `140 passed`，后续全部开发完成后由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 3 / 05-T8 | 已读取 `$gitnexus-codegraph` 技能文件并确认本机存在技能；但当前 Codex 会话仍未暴露 `gitnexus_*` / `list_repos` MCP 工具，`tool_search` 对 `gitnexus_impact` / `gitnexus_context` / `gitnexus_detect_changes` 返回 0 个工具，`list_mcp_resources` 返回空；PyCharm `analyze_calls` 对 Python 方法 FQN 也无法生成调用层级 | 不使用 `npx` 或 CLI 代替 MCP；通过 PyCharm MCP `search_symbol` / `search_text` 定位 `WatchlistService`、`WatchlistRepository` 和 `PersonalFinanceAgentService.sync_recommendations_to_watchlist`，先写研究池冷却期红灯测试，再最小新增仓储查询、服务冷却判断和推荐入池跳过审计；验证 `tests/test_watchlist_service.py` 4 passed、`tests/test_base_data_scheduler_analytics.py -k recommendation_intake` 3 passed、方案相关回归 145 passed，后续全部开发完成后由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 3 / 05-T9 | 修改 `UniverseRecommendationPipeline.run_for_universe` 审计 payload 透传和 `build_recommendation_payload` 推荐 payload 前，当前 Codex 会话仍未暴露 GitNexus MCP namespace，`tool_search` 未检索到 `gitnexus_impact` / `gitnexus_context` / `gitnexus_detect_changes` / `list_repos`，无法执行 MCP impact / detect_changes | 不使用 `npx` 或 CLI 代替 MCP；先写离线端到端冒烟红灯测试，验证合并池、回避池、`short_swing` 评分和推荐落库缺少策略审计字段；实现限定为新增 `scoring_strategy_id`、`score_strategy_id` 和 `score_weight_snapshot` 透传，不改变推荐排序、动作决策或评分公式；验证 T9 单测 1 passed、方案相关回归 146 passed，后续全部开发完成后由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 3 / 05-T10 | 总负责人确认 GitNexus MCP 与 `$gitnexus-codegraph` skill 均已存在；本会话已读取 skill，且再次通过 `tool_search`、`list_mcp_resources`、`list_mcp_resource_templates` 检查，但当前可调用工具仍未暴露 `gitnexus_*` / `list_repos`，因此无法在提交前执行 MCP `gitnexus_detect_changes` | 本任务仅做文档同步和 1 个测试断言更新；不使用 `npx` 或 CLI 代替 MCP，保留验证记录 `alembic upgrade head`、局部 pytest 35 passed、全量 pytest 352 passed；全部开发完成后由总负责人统一执行 GitNexus 更新和影响复核 |
| 2026-06-12 | 批次 3 / 06-T1 | 已按 `$gitnexus-codegraph` skill 再次确认 GitNexus 使用前提，但当前 Codex 会话仍未暴露 `gitnexus_*` / `list_repos` MCP 工具，`tool_search` 返回 0、MCP resources/templates 为空；修改 `AkshareFundProvider.fetch_lof_ohlcv`、新增 `_tencent_fund_symbol` / `_provider_chain_payload`、修改 `_fetch_tencent_kline_window` 和 `preview_fund_tasks` 前无法执行 MCP impact / detect_changes | 不使用 `npx` 或 CLI 代替 MCP；先写 LOF fallback、腾讯 JSONP 解析、腾讯 transient retry 和任务预览 sources 红灯测试，再最小实现 ProviderChain 降级、腾讯窗口 3 次有界重试和调度预览来源说明；验证 T1 相关回归 52 passed、compileall 通过、真实 `160716` 10 年腾讯直连 2426 条、单标的采集入库 2426 条；全部开发完成后由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 3 / 06-T3 | 已读取 `$gitnexus-codegraph` skill 并确认 `C:\Users\Administrator\.codex\config.toml` 存在 `[mcp_servers.gitnexus]`；但当前 Codex 会话工具清单仍未暴露 `gitnexus_*` / `list_repos`，`tool_search` 查询 GitNexus 返回 0，`list_mcp_resources` / `list_mcp_resource_templates` 为空，无法在修改北向资金链路前执行 MCP impact，也无法提交前执行 MCP detect_changes | 不使用 `npx` 或 CLI 代替 MCP；本轮修改符号清单：`normalize_ashare_northbound_market_flow`、`normalize_ashare_northbound_individual_flow`、`AshareCapitalFlowProvider.fetch_northbound_flow`、`fetch_northbound_market_flow`、`fetch_northbound_individual_flow`、`AshareP1Collector.collect_northbound_flow`、`run_ashare_p1` 的 `northbound_flow_refresh` 分支、`resolve_ashare_northbound_symbols`、`batch_ashare_northbound_symbols`、`_northbound_watermark_time`、`record_ashare_northbound_individual_watermark`、`build_ashare_collection_params`、`preview_ashare_tasks`、`build_capital_flow_group`、`northbound_flow_continuity`；已用 TDD、137 条相关 pytest、真实小样本采集和数据库只读查询验证，后续由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 3 / 06-T4 | 当前 Codex 会话仍未暴露 `gitnexus_*` / `list_repos` MCP 工具，无法在修改限售解禁 Provider、Collector、调度分发和运行摘要前执行 MCP impact / detect_changes | 不使用 `npx` 或 CLI 代替 MCP；本轮修改符号清单：`normalize_ashare_restricted_release_detail`、`AshareRiskProvider.fetch_restricted_release`、`AshareRiskSentimentCollector.collect_restricted_release`、`run_ashare_risk` 的 `restricted_release_refresh` 分支、`ashare_risk_sentiment_watermark_timeframe`、`build_ashare_collection_params`、`preview_ashare_tasks`、`infer_item_count`；已用 TDD、136 条相关 pytest、真实小样本采集和数据库只读查询验证，后续由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 3 / 06-T5 | 总负责人确认 GitNexus MCP 与 `$gitnexus-codegraph` skill 均存在；本轮已读取 skill，并确认 `C:\Users\Administrator\.codex\config.toml` 中存在 `[mcp_servers.gitnexus]`，但当前 Codex 会话调用 `list_mcp_resources(server="gitnexus")` 时 MCP 握手 30 秒超时，`tool_search` 也未暴露 `gitnexus_impact` / `gitnexus_context` / `gitnexus_detect_changes` / `list_repos` 工具，无法在修改股权质押链路前执行 MCP impact，也无法提交前执行 MCP detect_changes | 不使用 `npx` 或 CLI 代替 MCP；本轮修改符号清单：`normalize_ashare_pledge_ratio`、`_normalize_percent_ratio`、`AshareRiskProvider.fetch_pledge_ratio`、`AshareRiskProvider._latest_pledge_ratio_date`、`compact_akshare_date`、`parse_compact_akshare_date`、`AshareRiskSentimentCollector.collect_pledge_ratio`、`run_ashare_risk` 的 `pledge_risk_refresh` 分支、`ashare_risk_sentiment_watermark_timeframe`、`build_ashare_collection_params`、`preview_ashare_tasks`、`AvoidPoolPolicy.high_risk_types`；已按 TDD 先跑红灯，再用 142 条相关 pytest、真实小样本采集和数据库只读查询验证，后续由总负责人统一更新 GitNexus 索引并复核影响 |
| 2026-06-12 | 批次 3 / 06-T6 | 当前 Codex 会话 GitNexus MCP 仍未暴露可调用工具；T6 主要新增部署脚本、README 和模板结构测试，不修改既有 Python 函数/类/方法 | 不使用 `npx` 或 CLI 代替 MCP；新增文件：`deploy/windows/register_scheduler_task.ps1`、`deploy/windows/register_api_task.ps1`、`deploy/windows/unregister_tasks.ps1`、`deploy/windows/README.md`、`tests/test_windows_deploy_templates.py`；验证 `tests/test_windows_deploy_templates.py` 4 passed，三个 PowerShell 脚本 `-WhatIf` 通过，真实注册因当前权限不足待联调 |
| 2026-06-12 | 批次 3 / 06-T7 | 本轮 GitNexus MCP 已可调用 `list_repos` / `query`，`finance-agent` 仓库可见，但索引提示落后 3 个 commit；按总负责人要求先记录差异并继续开发，不在本轮运行 `gitnexus analyze` | T7 仅新增 Docker 部署模板、`.dockerignore` 和模板结构测试，不修改既有 Python 函数/类/方法；已用 GitNexus `query` 查询部署/调度上下文；验证 `tests/test_docker_deploy_templates.py` 3 passed，`docker build -f deploy/docker/Dockerfile.scheduler -t finance-agent-scheduler:test .` 成功；提交前 GitNexus `detect_changes(scope=staged)` 返回 `risk_level=low`、`affected_count=0`、`warnings=[]` |
| 2026-06-12 | 批次 3 / 06-T8 | GitNexus MCP 可调用，但 `finance-agent` 索引仍提示落后；T8 为文档新增任务，不修改既有函数/类/方法 | 已用 GitNexus `query` 查询调度器健康、Cookie 刷新和数据补采上下文；新增 `docs/运维手册.md` 与 `tests/test_ops_runbook.py`，验证 `tests/test_ops_runbook.py` 1 passed，部署文档相关测试 8 passed；提交前 GitNexus `detect_changes(scope=staged)` 返回 `risk_level=low`、`affected_count=0`、`warnings=[]` |
| 2026-06-12 | 批次 3 / 06-T9 | GitNexus MCP 可调用，但 `finance-agent` 索引仍提示落后；T9 为文档决策落档和测试断言，不修改既有业务符号 | 已用 GitNexus `query` / `context(MarketBarIntradayORM)` 确认当前表有 ORM 和迁移但没有执行流程；更新 `docs/数据库设计.md` 标明 `market_bars_intraday` 预留、无写入方，盘中事实源优先使用 `realtime_quote_snapshots`；验证 `tests/test_market_bars_intraday_schema.py` 3 passed；提交前 GitNexus `detect_changes(scope=staged)` 返回 `risk_level=low`、`affected_count=0`、`warnings=[]` |
| 2026-06-12 | 批次 3 / 06-T10 | GitNexus MCP 可调用，但 `finance-agent` 索引仍提示落后；T10 为进度文档同步和测试断言，不修改既有业务符号 | 已用 GitNexus `query` 查询 FUND/O5/A 股数据相关上下文；更新基金方案、项目进度、优化版本进度、06 方案和 00 总览；验证 `tests/test_development_progress_docs.py` 1 passed，文档/部署相关测试 12 passed；提交前 GitNexus `detect_changes(scope=staged)` 返回 `risk_level=low`、`affected_count=0`、`warnings=[]` |
