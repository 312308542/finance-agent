# 方案 06：数据层收尾与部署模板（O5 尾巴）

> 优先级：P1 ｜ 批次 3 ｜ 依赖：无（可与批次 2/3 其他方案并行）
> 前置阅读：`00-总体规划与执行约定.md`

## 1. 背景与现状

数据层生产化主体已完成（任务队列通用化 DSQ-001~014、水位增量、按源限流、K 线生命周期、基金接入、任务监控）。当前剩余事项分为三类：

1. **基金持续放量**（`docs/基金行情同步任务方案.md`）：
   - FUND-013：腾讯直连备源已接入并完成真实样本验证。
   - FUND-015：开放式基金已改为单 worker、有界批次持续初始化，当前仍有剩余候选，不能标记全量完成。
2. **A 股风险/资金数据覆盖**（`docs/项目进度跟踪表.md`）：北向资金、限售解禁、股权质押采集链已接入，后续继续按真实覆盖率补采和验收。
3. **部署真实验收**：调度器、API 的 Windows 任务计划和 Docker 模板已具备；Windows 正式注册仍受当前令牌权限阻塞，不能用手工进程替代验收。

## 2. 目标与非目标

**目标**

1. LOF 历史 K 线备源已完成；持续执行 FUND-015 有界批次，直至全量完成或形成可重跑失败清单。
2. 北向资金、限售解禁、股权质押三类数据接入采集→落库→风险/因子消费链。
3. 提供可复制的常驻部署模板（Windows 任务计划为主，Docker 为辅）与运维 runbook。

**非目标**

- 不接链上/宏观事件（仍是后续增强）。
- 不做正式限流监控大盘（任务监控页已有源级状态展示，够用）。

## 3. 技术方案

### 3.1 LOF 历史 K 线备源（解除 FUND-013/015）

**关键洞察**：LOF 是交易所交易品种（sh5/sz16 开头），与 A 股共享交易所行情通道。仓库已有绕开 AKShare 的直连实现 `src/finance_agent/data/providers/ashare_kline_sources.py`（`fetch_tencent_kline_direct` / `fetch_eastmoney_kline_direct`，含 2 年窗口分页 `two_year_windows`）。

方案：

- 在 `akshare_fund_provider.py` 的 `fetch_lof_ohlcv` 失败链上增加腾讯直连备源：复用 `fetch_tencent_kline_direct`，symbol 映射用现有 `_sina_fund_symbol` 风格的交易所前缀推断（`infer_fund_exchange` 已在 normalizers 中实现）。
- 接入 `ProviderChain`（`scheduler/task_queue_model.py`）作为第二源，失败审计自动进 `ProviderAttempt`。
- ETF 同步受益：同一备源对 ETF 历史 K 线同样适用，作为现有主源的兜底。
- 验证路径：先跑 FUND-013 小批量（10 只 LOF × 10 年），抽样比对腾讯 vs 东财数据一致性（收盘价偏差 < 0.1% 容忍，复权口径写清楚——直连源是否前复权必须实测确认并记录在基金方案文档）。
- 放量（FUND-015）：沿用 `ashare.bars.1d.bootstrap` 的断点续跑模式，分批执行 `fund.etf.bars.1d.bootstrap` / `fund.lof.bars.1d.bootstrap` / `fund.open.nav.bootstrap`，水位表防重。

### 3.2 北向资金 / 限售解禁 / 股权质押接入

按既有 Collector 模式（参考 `AshareRiskSentimentCollector` 的接法：Provider → 能力注册 → Collector → raw_records 归档 → 业务表 → sync_task_type 分发）：

| 数据 | AKShare 接口（实现时以当版 AKShare 实测为准） | 落库 | 下游消费 |
| --- | --- | --- | --- |
| 北向资金（市场级历史） | `stock_hsgt_hist_em` | `capital_flow_snapshots`（市场级 asset_id 约定，沿用现有榜单级水位风格） | 资金流因子北向多窗口 |
| 北向持股（个股） | `stock_hsgt_individual_em`（或当版等价接口） | `capital_flow_snapshots` | 个股北向因子 |
| 限售解禁 | `stock_restricted_release_queue_em` | `event_records`（event_type=`restricted_release`）+ 临近解禁写 `risk_findings` | 事件因子、风险反驳 |
| 股权质押 | `stock_gpzy_pledge_ratio_em` 等 | `risk_findings`（质押比例超阈值） | 风险反驳、回避池 |

实现约束：

- 全部走 SourceGate 限流（eastmoney host 既有策略）+ 失败水位冷却，单接口失败不阻塞批次（DSQ 模式）。
- 每个接口先在 `docs/AKShare能力矩阵.md` 登记（接口名、字段、频率、失败模式），再写代码——这是仓库既有纪律。
- 新增 `sync_task_type`：`ashare.northbound`、`ashare.restricted_release`、`ashare.pledge`，纳入 `data config` 预设（低频：日级/周级）。
- 风险阈值参数化进任务 params（解禁市值占比、质押比例红线）。

### 3.3 部署模板与 runbook

新增 `deploy/` 目录：

1. **Windows 任务计划（主推，当前生产环境）**：
   - `deploy/windows/register_scheduler_task.ps1`：用 `Register-ScheduledTask` 注册开机自启 + 失败重启的调度器常驻任务（`run_base_data_scheduler.py --loop --status-file ... --event-log-file ...`）；参数化安装路径；附卸载脚本。
   - `deploy/windows/register_api_task.ps1`：同模式注册 API 服务常驻任务（`uvicorn finance_agent.api.app:app --app-dir src --host 127.0.0.1 --port 8000`）。前端生产形态用 `npm run build` 产物，由 API 进程托管静态文件或本机 IIS/nginx 指向 `apps/agent-office/dist`（模板二选一给出，默认 API 托管，改动最小）。
   - `deploy/windows/README.md`：安装、查看状态、日志位置、升级流程（停任务→git pull→alembic upgrade→重启）。
   - 安全注意：API 默认只绑 `127.0.0.1`；模板注释明确"如需局域网访问必须自行加反向代理与鉴权"——当前 API 无认证层，不得直接暴露。
2. **Docker（辅）**：
   - `deploy/docker/Dockerfile.scheduler` + compose 片段（挂载 runtime/、连接既有 postgres/redis 服务）；注意 TA-Lib 系统依赖的安装层。
3. **runbook**：`docs/运维手册.md`——常驻进程健康判定（状态文件心跳）、常见故障（Redis 断连降级、源熔断、Cookie 过期跑 `refresh_eastmoney_cookie.py`）、数据缺口补采流程（`check_base_data_health.py` → backfill_jobs）。

### 3.4 决策项 D1：`market_bars_intraday` 表的去留

迁移 0015 建了分钟级 K 线表，但当前**没有任何写入方**（只有 ORM 定义），盘中能力实际由 `ashare.realtime_quotes`（5 分钟实时快照）承担。本方案需要做一次显式决策并落档：

- **选项 A（推荐）**：暂不建分钟 K 线采集任务。理由：实时快照已满足方案 02 盘中触发的需要；分钟 K 线全市场采集的限流与存储成本高，而第一版投资周期是中长期+波段（项目计划 §3.2），分钟级数据无消费方。表保留，在 `docs/数据库设计.md` 标注"预留，无写入方"。
- **选项 B**：仅对持仓+用户观察池小范围采集分钟 K 线（复用 K 线生命周期任务模式，新增 `sync_task_type=market_bars_intraday_refresh`，限定资产清单来自 `EventPriorityResolver`）。在确有盘中精细分析需求时升级到此选项。

执行者按选项 A 落实，除非总负责人在进度表批注改走 B。

## 4. 任务拆解

- [ ] T1 LOF 腾讯直连备源：TDD（`tests/test_fund_lof_fallback_sources.py`，fixture 用腾讯 JSONP 样例）→ 实现 → FUND-013 小批量真实验证（数据一致性记录进基金方案文档）。
- [ ] T2 FUND-015 放量：分批执行三类基金 bootstrap，每批记录水位与失败清单；完成后更新基金方案进度表。
- [ ] T3 北向资金接入（能力矩阵登记 → Provider → 归一化 → Collector → 任务分发 → 因子消费各一步，TDD）。
- [ ] T4 限售解禁接入（同模式，落 event + risk）。
- [ ] T5 股权质押接入（同模式，落 risk + 回避池策略消费：质押超红线进 `AvoidPoolPolicy` 判定，注意与方案 05 T3 的回避池重建任务衔接）。
- [ ] T6 Windows 任务计划模板（调度器 + API 服务两个脚本）+ 卸载脚本 + README，本机真实注册验证一次。
- [ ] T7 Docker 模板（构建通过即可，不要求生产验证）。
- [ ] T8 运维 runbook 编写。
- [ ] T9 D1 决策落档：按选项 A 在 `docs/数据库设计.md` 标注 `market_bars_intraday` 预留状态。
- [ ] T10 文档同步：基金方案 FUND-013/015 状态、`docs/项目进度跟踪表.md` A 股数据进度表、O5 状态。

## 5. 验收命令

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_fund_lof_fallback_sources.py tests/test_ashare_northbound_collection.py -q
.\.venv\Scripts\python.exe scripts\data\collect_base_data.py --sync-task-type fund.lof.bars.1d.bootstrap --limit 10
.\.venv\Scripts\python.exe scripts\data\check_base_data_health.py
powershell -File deploy\windows\register_scheduler_task.ps1 -WhatIf
.\.venv\Scripts\python.exe -m pytest -q
```

验收标准：

- 10 只 LOF 样本 10 年日 K 完整入库且与备源比对一致；全量基金初始化失败率 < 5% 且失败项有水位可重跑。
- 北向/解禁/质押任务在调度器 dry-run 计划中出现，真实跑通后 `raw_records` 有归档、业务表有数据。
- 重启 Windows 后调度器自动拉起，状态文件心跳正常。

## 6. 风险与注意事项

- 直连源的复权口径是最大数据质量风险：实测确认腾讯接口复权方式与现有 `market_bars` 入库口径一致后才能放量；不一致就在归一化层转换并写明。
- AKShare 接口名随版本漂移，T3-T5 动手前先 `python -c "import akshare; help(...)"` 实测当版接口签名，登记进能力矩阵。
- 放量初始化要尊重既有源限流（`source_rate_limiter` 默认策略），不要为了快调大并发——东财封禁的恢复成本远高于慢一点。
- `deploy/` 模板里不得出现真实密码/key，数据库 URL 用占位符。

## 7. 进度表

| 任务 | 状态 | 验证记录 |
| --- | --- | --- |
| T1 LOF 备源 | 已完成 | 2026-06-12：`AkshareFundProvider.fetch_lof_ohlcv` 已接入 `ProviderChain`，主源 `akshare:fund_lof_hist_em` 失败后降级 `tencent:direct:kline`；基金代码按 `infer_fund_exchange` 转换为腾讯 `sh/sz` 前缀；腾讯窗口请求增加 3 次有界重试和 0.5 秒退避，避免单窗口连接重置拖垮 10 年任务；调度预览 `fund.lof.bars.1d.bootstrap` 的 sources 已展示主源与腾讯备源。验证：先跑 LOF fallback 红灯测试，随后 `.\.venv\Scripts\python.exe -m pytest tests/test_fund_lof_fallback_sources.py tests/test_fund_normalizers.py tests/test_akshare_provider_timeout.py tests/test_data_sync_config.py -q`，52 passed；`compileall` 通过；真实只读腾讯直连样本 `160716` 10 年日 K 返回 2426 条、6 个窗口、范围 `2016-06-13` 到 `2026-06-12`；单标的采集命令 `.\.venv\Scripts\python.exe scripts\data\collect_base_data.py --group fund --sync-task-type market_bars_full_history_backfill --fund-asset-type lof --symbol-source manual --fund-symbol 160716 --ashare-start 20160612 --ashare-end 20260612 --limit 0 --batch-size 1 --max-workers 1 --cache-backend null --force-provider` 成功，数据库 `market_bars` 抽样 `fund:lof:160716` 共 2426 条，范围 `2016-06-13` 到 `2026-06-12`；当前库内仅有 1 个 LOF 资产，10 只样本批量验证需在 T2 刷新基金资产池后继续。差异记录：方案验收命令中的 `--sync-task-type fund.lof.bars.1d.bootstrap` 是页面任务 key，当前脚本实际接收内部类型 `market_bars_full_history_backfill`；`--limit` 是单标的 K 线条数限制，不是标的数量限制，因此小批量验证需用 `--symbol-source manual` 或后续补专用 source limit。 |
| T2 基金放量 | 进行中-ETF/LOF 完成，开放式基金分批执行中 | 资产池为 ETF 1,507、LOF 390、开放式基金 20,604。ETF/LOF 全历史初始化已完成，1,897 条 `fund_market_bars` 水位全部 `available`；LOF 日 K 为 708,159 行。开放式基金已固定单 worker、`source_limit=500`。前十九批共处理 9,500 个任务，9,497 个最终 `available`；`005471/005792/006922` 确认为结构性无历史数据，`007665/009133/010802/010966/011978/012703` 的瞬时超时已单标的重试成功。当前事实表为 15,808,520 行、覆盖 9,506 只，另有 3 条终态 `unavailable` 水位；同窗口 resolver 剩余 11,129 个候选，第二十批正在运行。 |
| T3 北向资金 | 已完成 | 2026-06-12：已在 `docs/AKShare能力矩阵.md` 登记 `stock_hsgt_hist_em` / `stock_hsgt_individual_em` 的用途、关键字段、刷新频率和失败模式；`AshareCapitalFlowProvider` 新增市场级与个股级北向资金能力，归一化写入 `capital_flow_snapshots`，个股级只保留用户可交易的主板股票；`AshareP1Collector.collect_northbound_flow` 批量写入资金流快照并归档 `raw_records`；`northbound_flow_refresh` 已纳入 `ashare.northbound` 调度任务，任务参数把 `source_limit` 绑定到批次大小，避免日级任务全量打个股接口；新增北向个股水位 `provider=stock_hsgt_individual_em`、`timeframe=northbound`，按未采/最久未采优先轮转覆盖，失败写入冷却水位并在冷却期内跳过，后续可按失败清单人工重跑；资金流因子组已消费 `northbound_net_inflow`，新增 `northbound_net_inflow_strength` 和 `northbound_flow_continuity` 两个因子。验证：先补红灯测试后实现，`.\\.venv\\Scripts\\python.exe -m pytest tests/test_ashare_northbound_collection.py tests/test_data_sync_config.py tests/test_base_data_scheduler_analytics.py tests/test_collectors_batch_persistence.py tests/test_factor_service.py -q` 通过，137 passed；真实小样本 `.\.venv\Scripts\python.exe scripts\data\collect_base_data.py --group ashare-p1 --sync-task-type northbound_flow_refresh --source-limit 1 --cache-backend null --force-provider` 成功，市场级与 `000001` 个股级各 1 个任务 available；数据库只读确认 `capital_flow_snapshots` 中 `akshare:stock_hsgt_individual_em / ashare:000001` 有 1 条 available 且 `northbound_net_inflow` 非空，`data_sync_watermarks` 中 `ashare:000001` 北向水位为 available。注意：当前 AKShare 市场级北向最新样本返回数值为空，业务快照为 partial，这是源数据现状，不是归一化失败。 |
| T4 限售解禁 | 已完成 | 2026-06-12：已在 `docs/AKShare能力矩阵.md` 登记 `stock_restricted_release_detail_em` / `stock_restricted_release_queue_em` 的用途、关键字段、刷新频率和失败模式；实测当前 AKShare `stock_restricted_release_detail_em(start_date, end_date)` 可返回全市场解禁详情，优先接该接口，`queue_em(symbol)` 作为个股页备选能力暂不接入；新增 `normalize_ashare_restricted_release_detail`，只保留用户可交易主板股票，所有解禁写 `event_records(event_type=restricted_release)`，临近窗口内且占解禁前流通市值比例超过阈值的标的写 `risk_findings(risk_type=restricted_release)`；`AshareRiskProvider.fetch_restricted_release` 和 `AshareRiskSentimentCollector.collect_restricted_release` 已接入 raw_records 归档与批量事件/风险入库；调度新增 `ashare.restricted_release` / `restricted_release_refresh`，18:30 低频日级任务，`source_limit` 绑定批次大小，水位 `timeframe=rr:YYYYMMDD` 避免超过 `data_sync_watermarks.timeframe` 长度限制；修复 `CollectionRuntime.infer_item_count` 在 `RiskFindingsResult` 只有 events 无 risks 时误报 0 的问题。验证：红灯测试后实现，`.\\.venv\\Scripts\\python.exe -m pytest tests/test_collection_runtime_logging.py tests/test_ashare_restricted_release_collection.py tests/test_data_sync_config.py tests/test_base_data_scheduler_analytics.py -q` 通过，136 passed；真实小样本 `.\.venv\Scripts\python.exe scripts\data\collect_base_data.py --group ashare-risk --sync-task-type restricted_release_refresh --risk-start 20260601 --risk-end 20260630 --source-limit 3 --cache-backend null --force-provider` 成功，任务 available、item_count=3；数据库只读确认 `event_records` 中 600303/600750/603286 三条 `restricted_release` 事件已落库，水位 `stock_restricted_release_detail_em / rr:20260630` 为 available；本次真实前三条解禁比例低于 5% 阈值，因此未生成风险，风险入库由单元测试覆盖。 |
| T5 股权质押 | 已完成 | 2026-06-12：已在 `docs/AKShare能力矩阵.md` 登记 `stock_gpzy_pledge_ratio_em` 及后续可扩展的质押明细接口；实测本地 AKShare 当前签名为 `stock_gpzy_pledge_ratio_em(date: str = "20240906")`，优先接入上市公司质押比例全市场接口；新增 `normalize_ashare_pledge_ratio`，只保留用户可交易主板股票，质押比例从百分数口径统一转为 0~1，超过 30% 阈值时写入 `risk_findings(risk_type=pledge_ratio)`，50% 以上标记 critical；`AshareRiskProvider.fetch_pledge_ratio` 支持当请求日期无数据时通过 `stock_gpzy_profile_em()` 降级到源端最近可用质押交易日，payload 记录 requested_date、date 与 fallback_trace；`AshareRiskSentimentCollector.collect_pledge_ratio` 已接入 raw_records 归档与批量风险入库；调度新增 `ashare.pledge` / `pledge_risk_refresh`，18:45 低频日级任务，`source_limit` 绑定批次大小，水位 `timeframe=pledge_ratio` 避免超过 `data_sync_watermarks.timeframe` 长度限制；`AvoidPoolPolicy` 已把 `pledge_ratio` 纳入回避池消费。验证：先跑红灯 `tests/test_ashare_pledge_collection.py` 失败于函数/Collector/回避池缺失，并补充无数据日期 fallback 红灯测试，再实现；新增测试集合 `.\.venv\Scripts\python.exe -m pytest tests/test_ashare_pledge_collection.py tests/test_data_sync_config.py::test_scheduler_payload_exports_ashare_pledge_job tests/test_base_data_scheduler_analytics.py::test_ashare_pledge_risk_refresh_dispatches_to_collector tests/test_base_data_scheduler_analytics.py::test_pledge_risk_watermark_timeframe_fits_schema_limit -q` 通过，7 passed；真实小样本 `.\.venv\Scripts\python.exe scripts\data\collect_base_data.py --group ashare-risk --sync-task-type pledge_risk_refresh --risk-end 20260612 --source-limit 3 --cache-backend null --force-provider` 成功，任务 available、item_count=3、fallback_used=true；数据库只读确认 `risk_findings` 已写入 `pledge_ratio` 风险，最新样本包括 `ashare:000567`、`ashare:002485`、`ashare:600370`，`data_sync_watermarks` 中 `stock_gpzy_pledge_ratio_em / pledge_ratio` 为 available；相关回归 `.\.venv\Scripts\python.exe -m pytest tests/test_ashare_pledge_collection.py tests/test_ashare_restricted_release_collection.py tests/test_data_sync_config.py tests/test_base_data_scheduler_analytics.py tests/test_collection_runtime_logging.py -q` 通过，142 passed。 |
| T6 Windows 部署模板 | 模板已完成，真实注册受权限阻塞 | 2026-06-12：新增 `deploy/windows/register_scheduler_task.ps1`、`register_api_task.ps1`、`unregister_tasks.ps1` 和 `deploy/windows/README.md`；调度器任务使用 `run_base_data_scheduler.py --loop --status-file --event-log-file`，API 任务使用 `python -m uvicorn finance_agent.api.app:app --app-dir src --host 127.0.0.1 --port 8000`，默认仅绑定本机并在脚本/README 中说明“当前 API 无认证层，不得直接暴露”。模板专项测试 4 passed，三个脚本的 `-WhatIf` 均通过。2026-07-13 再次真实联调：可再生配置导出成功（55,553 bytes），两个注册脚本 `-WhatIf` 均退出 0；随后按正式任务名运行真实注册，两次都返回 `HRESULT 0x80070005 / Access is denied`。当前用户虽为 `DESKTOP-ELT87C4\Administrator`，但会话令牌 `IsAdmin=False`、Medium 完整性；查询 `FinanceAgent-BaseDataScheduler` / `FinanceAgent-Api` 均不存在，因此未执行启动、失败恢复和重启后自启验证。联调同时发现两个脚本在 CIM 权限错误后仍打印成功并退出 0；GitNexus upstream impact 为 LOW、无直接调用者/流程，新增红灯后给 `Register-ScheduledTask` 显式加 `-ErrorAction Stop`，专项由 1 failed/4 passed 转为 5 passed。同一权限场景复跑时两个进程均退出 1、保留拒绝访问错误且不再输出成功提示。当前手工 API 健康检查仍为 `status=ok`，只证明既有进程正常，不能替代计划任务验收。下一动作：在提权 PowerShell 中重跑两个注册命令并查询 AtStartup、RestartCount=3 和 API 绑定；整机重启仅在用户明确授权后执行。 |
| T7 Docker 模板 | 已完成 | 2026-06-12：新增 `deploy/docker/Dockerfile.scheduler`、`deploy/docker/compose.scheduler.yml` 和根目录 `.dockerignore`；调度器镜像基于 `python:3.11-slim`，构建层安装 TA-Lib C 库依赖，复制 `src/` 与 `scripts/` 后执行 `pip install --no-cache-dir .`，默认命令为 `scripts/data/run_base_data_scheduler.py --loop --status-file runtime/base_data_scheduler/status.json --event-log-file runtime/base_data_scheduler/events.jsonl`；compose 片段挂载 `runtime/`，通过 `FINANCE_AGENT_DATABASE_URL`、`FINANCE_AGENT_REDIS_URL` 和 `FINANCE_AGENT_CACHE_BACKEND` 连接既有 Postgres/Redis，不内置任何真实密码/key。验证：先补 `.dockerignore` 红灯测试，确认缺失文件导致 `tests/test_docker_deploy_templates.py` 失败；实现后 `.\.venv\Scripts\python.exe -m pytest tests\test_docker_deploy_templates.py -q` 通过，3 passed；`docker build -f deploy/docker/Dockerfile.scheduler -t finance-agent-scheduler:test .` 成功生成镜像。差异记录：首次构建因上下文包含 `.venv`、`runtime`、`artifacts` 等本地大目录超时，补 `.dockerignore` 后已解除。 |
| T8 runbook | 已完成 | 2026-06-12：新增 `docs/运维手册.md`，覆盖调度器状态文件心跳、JSONL 事件日志、Windows 任务计划、Docker 调度器、Redis 断连、源熔断、东方财富 Cookie 过期处理、数据健康检查、`backfill_jobs` 缺口补采、升级流程和安全约束；文档按当前代码事实记录东财 Cookie 入口为 `ensure_eastmoney_cookie()` / `refresh_eastmoney_cookie_file()`，默认文件为 `runtime/secrets/eastmoney_cookie.json`，未写入不存在的独立刷新脚本；同时强调当前 API 无认证层，默认只绑定 `127.0.0.1`，不得直接暴露。验证：先写红灯 `tests/test_ops_runbook.py`，确认 `docs/运维手册.md` 缺失导致失败；补手册后 `.\.venv\Scripts\python.exe -m pytest tests\test_ops_runbook.py -q` 通过，1 passed。 |
| T9 D1 决策落档 | 已完成 | 2026-06-12：按选项 A 更新 `docs/数据库设计.md`，明确 `market_bars_intraday` 当前为“预留，无写入方”，第一版暂不建分钟 K 线采集任务；盘中触发和看盘能力优先使用 `realtime_quote_snapshots`，后续只有在持仓和用户观察池需要更细粒度盘中分析时，才新增小范围分钟 K 线采集任务，且不得与 10 年日 K 生命周期策略混用。验证：先补红灯 `tests/test_market_bars_intraday_schema.py::test_market_bars_intraday_documented_as_reserved_without_writer`，确认文档缺少“预留，无写入方”说明导致失败；补文档后 `.\.venv\Scripts\python.exe -m pytest tests\test_market_bars_intraday_schema.py -q` 通过，3 passed。 |
| T10 文档同步 | 已完成 | 2026-06-12：同步 `docs/基金行情同步任务方案.md`、`docs/项目进度跟踪表.md` 和 `docs/优化版本进度跟踪表.md`；FUND-013 保持已完成，FUND-015 明确为能力完成后按断点队列持续分批执行；项目进度表已把 A 股数据状态更新为 06 数据层尾项已完成，并写明北向资金、限售解禁、股权质押、部署模板和运维手册已补齐；优化版本进度表已将 O5 更新为已完成。验证：先补红灯 `tests/test_development_progress_docs.py`，确认项目进度/O5 状态未同步导致失败；更新文档后 `.\.venv\Scripts\python.exe -m pytest tests\test_development_progress_docs.py -q` 通过，1 passed。 |
