# 项目进度跟踪表

更新时间：2026-07-15

本文档用于跟踪 `finance-agent` 的真实建设进度。后续每次完成数据链路、数据库、因子、推荐或前端能力时，都优先更新这里，避免进度散落在多个设计文档里。

优化版本未完成主线单独跟踪在：[优化版本进度跟踪表](./优化版本进度跟踪表.md)。

## 1. 总体状态

| 模块 | 状态 | 当前结论 | 下一步 |
| --- | --- | --- | --- |
| 项目方向 | 已升级 | 产品定位从单次 AI 标的推荐系统升级为私人金融助手：持仓监控、私人观察池、触发提醒、长期记忆和标的推荐并行；不做无人值守自动交易 | 持续约束后续需求，不回到泛金融工具集或自动交易系统 |
| 推荐主链路 | Agent 决策基础版已接入 | 已跑通“股票池/币池 -> 数据采集 -> 因子计算 -> 初筛 -> 多维评分 -> 信号快照 -> 推荐运行 -> 单标的推荐结果 -> Agent 决策”的基础链路；`UniverseRecommendationPipeline` 已支持按单一候选池批量运行 A 股或数字货币链路 | 接入 LLM/Hermes 节点、中文报告生成和实时触发 |
| 数据库 | V1.5 数据层完成 | PostgreSQL + TimescaleDB 已作为全环境统一方案，M0/M1/M2/M3 表均已迁移；V1.5 新增 `data_sync_watermarks`、`market_bars_intraday`、`fund_nav_snapshots` 和 K 线压缩策略，支撑水位增量、盘中 K 线隔离和开放式基金净值独立落库；`market_bars_intraday` 已明确为预留表，当前无写入方 | 后续按 07/08 方案接前端闭环和人工确认 |
| 缓存层 | 基础采集已接入并懒加载 | Redis 已作为热数据、Agent 输入上下文、任务锁和 Provider 熔断状态缓存；`collect_base_data.py` 已接入任务锁和熔断控制；缓存包已改成 Redis 懒加载，dry-run/smoke 不再被 Redis 导入阻断 | 后续接推荐运行上下文缓存 |
| A 股数据 | 推荐闸门已恢复-覆盖缺口受源能力限制 | AKShare 能力注册表、行情 fallback、P0/P1/P2 Collector、风险/情绪 Collector、raw_records 归档已完成。2026-07-15 已完成全主板日 K 增量、风险榜单动态日期修复、重点池新闻补采、全市场推荐重算和 1,206 个任务的 Universe 全量刷新，`recommendation_readiness=ready`、`executable=true`；日 K 覆盖 3,185/3,482，推荐重算生成 2,920 条评分和 200 条推荐。真实缺口仍包括：72 小时内财务仅 1 只、普通资金流 1,696/3,482、北向停在 2024-08-16、行业/概念归属仅 727/1,090 只、重点池近 7 日新闻 169/200 只。行业/概念主源部分返回 401，fallback 只能返回首屏成员 | 对财务、资金流、北向和行业/概念源能力缺口保留阻塞；方案 18 最终题材校准须等待板块覆盖提升，不以目录任务成功代替成员覆盖验收 |
| 基金数据 | ETF/LOF 完成，开放式基金分批初始化中 | 基金资产池为 ETF 1,507、LOF 390、开放式基金 20,604；ETF/LOF 全历史初始化已完成。开放式基金第三十六批已收口，当前事实表覆盖 17,992 只、`fund_nav_snapshots` 20,998,580 行，全部事实行均为 `available`；唯一水位为 17,991 `available`、9 个结构性 `unavailable`。动态窗口成立前空窗误判已按 TDD 修复 | 仍有 2,603 只从未尝试，继续单 worker、每批 500 只串行执行并逐批做日志、源端和数据库三重验收 |
| 数字货币数据 | M0 生产化基础增强完成 | 已接入 `CryptoDataCollector`，Binance/ccxt markets、OHLCV 和原生衍生品快照均已统一写入 `raw_records`；数据同步配置已支持现货/合约 universe、K 线和合约衍生品任务分发；7x24 日历、现货/合约 K 线补采计划、Binance 限流识别和原生 fapi 备用端点已接入 | 扩展更多交易所/链上事件和正式限流监控 |
| 因子与指标 | 基础版已打通 | `IndicatorService` 已用 TA-Lib + pandas/numpy 从 `market_bars` 计算技术指标并写入 `indicator_frames`；`FactorService` 已合并技术、资金流、基本面、估值、事件、风险和数字货币衍生品输入并写入 `factor_frames`；候选池级冒烟已验证 A 股和数字货币可分别批量生成指标/因子 | 补因子规格配置、历史分位和完整资金流/衍生品窗口因子 |
| 初筛与评分 | 基础版已打通 | `ScreeningService` 已能按候选池执行硬过滤并写入 `screening_results`、`screening_result_items`；`ScoringService` 已能用透明权重写入 `asset_scores`；批量流水线已按单市场候选池生成推荐排序 | 扩展规则集、策略权重配置和同市场多候选池评分 |
| 量化模型研究 | 轻量回测证据链已接入，完整实验室后置 | XGBoost、LightGBM、RandomForest、CatBoost、LSTM、Transformer、PatchTST、多因子回归、风格暴露、风险模型、IC/RankIC 和参数搜索仍作为后续量化实验方向；当前已先落地透明评分 TopN 的轻量回测基础设施，包括 `bt`/`quantstats` 适配、`BacktestService`、`backtest_results`、CLI `backtest run`、`analytics.backtest.weekly` 调度入口，以及推荐 payload/run payload/中文报告中的 `backtest_evidence` | 继续执行真实 A 股主板数据回测冒烟，确认指标合理性后再规划完整量化实验室 |
| Agent 分析 | 主分析模型 Planner、高风险复核回写和圆桌模型节点已接入 | 已实现可审计的 Agent 决策层；`RecommendationDecisionWorkflow` 会结合推荐、持仓、观察池、信号、风险和记忆，决定是否入池、买入、卖出或换股；6 个 Workflow 均已加入受控圆桌阶段，模型配置可用时由真实模型生成结构化观点，不可用时保留规则版 fallback；模型供应商、模型实例、路由规则和检索 profile 已参考 Dify 思路入库；内部 Agent Loop 已能真实调用主分析模型，让模型按需调用只读事实、记忆和图谱工具并选择 Workflow；高风险复核服务、CLI 和调度任务已能回写审计链与决策日志 | 补真实模型统一联调、前端确认反馈和展示 |
| Workflow 层 | 6 个 LangGraph 入口已落地并接入模型增强圆桌 | `FinanceAssistantService` 已统一发现和调度 6 个 Workflow；`portfolio_monitoring`、`watchlist_management`、`recommendation_decision`、`asset_deep_analysis`、`swap_decision`、`daily_review` 均会写入节点、圆桌、`model_route`、`model_review`、高风险复核和完整中文报告审计；圆桌观点已带 `generated_by`、`model_instance_id`、`key_points`、`rebuttals` 和 `data_gaps`；Hermes 侧 CLI/MCP 调用链已完成 O1 冒烟验收 | 补真实 API key 联调和前端渲染 |
| 风险反驳 | 规则版已落地到 M2 | `risk_findings` 已能写入龙虎榜、大宗交易、两融和涨停池过热风险；持仓监控和观察池管理已生成规则版中文风险反驳 | 后续把风险扣分、反方观点和证据引用接到推荐报告 |
| 推荐报告 | 模型圆桌明细模板和前端报告页已接入 | 推荐决策、持仓监控、观察池管理、单标的深度分析、换股/换币比较和每日复盘均已生成包含执行摘要、裁决、行动计划、证据、圆桌观点、风险反驳、数据质量、记忆引用、复核状态、模型路由和 Markdown 的完整中文报告；圆桌观点 Markdown 已展示模型/规则来源、要点、反方和缺口，并写入 `report_draft` 审计事件；Web 报告页已接报告列表和详情渲染 | 后续接导出和真实模型复核结果统一联调 |
| 持仓监控 | 圆桌基础版已完成 | `portfolio_monitoring` 可读取组合持仓、信号、风险和 Finance Memory，输出 hold/watch/reduce 建议；LangGraph 包装已调用持仓、因子/TA、信号风险和记忆工具，生成圆桌观点、高风险复核和完整中文报告；规则版闭环仍写入提醒、审计、决策、记忆和复盘任务 | 接实时行情触发、阈值规则、前端展示和人工确认反馈 |
| 私人观察池 | 圆桌基础版已完成 | `watchlist_management` 可管理既有观察池；LangGraph 包装已调用观察池、因子/TA、信号风险和记忆工具，生成圆桌观点、高风险复核和完整中文报告；`decide_recommendation_actions` 会先由 Agent 决定是否入池，再写入观察池生命周期事件 | 接冷却期规则、候选池页面和人工操作记录 |
| 长期记忆 | Finance Memory 增强闭环、聊天摘要、反馈 API 和前端记忆视图已完成 | 持仓监控、观察池管理、推荐入池和推荐 Agent 决策均已写入 `assistant_memories`、`decision_logs`、`review_tasks`；活跃业务记忆会自动写入 `memory_embeddings`；用户反馈 API 会复用 `MemoryService.record_user_feedback` 写入反馈记忆并回写原决策状态；复盘结果会反写源记忆置信度；每日关注原因可按时间线沉淀；聊天摘要服务可把长会话压缩为 `chat_summary` 记忆；`GraphSyncService` 已可把资产、决策、观察池事件、记忆、风险、证据和复盘投影到配置选择的 GraphStore；Web 记忆页已按资产和时间线展示最近记忆 | 接真实用户反馈落库联调和 Hermes 上下文边界 |
| 主 Agent 调度 | 真实模型 Planner、聊天工具循环、复核调度和聊天摘要服务已完成 | `FinanceAssistantService` 已作为 `PersonalFinanceAgentService` 的兼容包装，提供 `list_workflows()` 和 `run_workflow()` 统一入口；`FinanceAgentInterface` 已给 CLI 和 MCP 共用；`finance-agent chat` 已提供类似 Hermes-Agent 的 CLI 聊天窗口，可持续对话、持久化 `chat_session_id`、通过 `--session-id` 续聊，固定意图可查询 Workflow/工具/模型/历史，普通问题可在模型 ready 时按需调用只读事实工具；`InternalFinanceAgentLoopRunner` 已可消费 `assistant_trigger_events`，默认使用模型增强 planner，并通过 LangGraph `load_task -> plan -> execute_workflow -> persist_result` 图按需调用底层 Workflow；`agent.loop.consume.*` 和 `analytics.high_risk_reviews.*` 已纳入基础数据调度器；聊天摘要服务可供后续调度/CLI 接入 | 接真实复核联调和前端办公室状态页 |
| 触发事件层 | V1.2 调度器接入已完成 | `TriggerService` 已能根据持仓回撤、信号转弱、观察池条件、推荐运行、风险事件和数据质量异常生成 `assistant_trigger_events`，并派发到 Agent 唤醒队列；观察池条件可读取 TA、因子和评分，Workflow 由 Agent 按需调用；`analytics.triggers.evaluate.*` 已纳入基础数据调度器，盘中急跌和放量异动规则已落地，intraday 任务默认关闭待实时快照链路确认 | 接前端提醒中心；实时快照稳定后启用 intraday 触发并调优阈值 |
| 前端办公室 | 08 人工确认与操作闭环完成 | React 前端已接入真实调度器任务监控页，支持任务列表、详情、进度、阶段、日志、单任务配置、执行、取消和失败重跑入口；报告页、推荐页、持仓页、风险中心、Agent 运行页、Finance Memory 页均已接真实 API；推荐页已接入人工确认、订单草案生成，持仓页已接入执行登记和执行历史，风险中心已接入到期复盘提醒；后端已形成“建议 -> 确认 -> 草案 -> 外部执行登记 -> 持仓更新 -> 到期复盘 -> Finance Memory 反写”闭环，执行登记只接受用户在外部完成交易后的手工填报 | 补真实模型/API 统一联调、更多空态/错误态和操作闭环体验打磨 |

### 近期里程碑

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 前端办公室 | 07 前端用户闭环页面主体完成 | 报告页、推荐页、持仓页、风险中心、Agent 运行页、Finance Memory 页均已接真实 API；推荐页反馈入口调用后端决策反馈 API，真实 Workflow 到 `decision_logs` 落库验收留到统一联调阶段。 |

## 2. 推荐链路进度

| 链路节点 | 状态 | 已有资产 | 缺口 |
| --- | --- | --- | --- |
| 股票池/币池 | 生产化策略基础版完成 | `assets` 稳定身份主表、`asset_profiles`、`asset_provider_mappings`、`asset_status_snapshots`、`realtime_quote_snapshots` 资产详情附表，以及 `asset_universes`、`asset_universe_members` 候选池表；A 股全 A、指数成分、行业/概念、资金流榜、人气榜、涨停池和 Binance 交易对池已纳入数据同步配置与调度计划；A 股指数/行业/概念已从目录自动展开；同市场多候选池合并和回避池策略已有服务与 CLI | 推荐运行入口引用回避池、补冷却期和前端配置 |
| 数据采集 | 生产化基础增强完成 | AKShare P0 行情 fallback；AKShare P1 指数/行业/概念/资金流/新闻公告 Provider；AKShare P2 财务/估值 Provider；A 股风险/情绪 Provider；北向资金、限售解禁和股权质押 Provider；Binance/ccxt 基础采集；`raw_records` 归档；Redis 任务锁和 Provider 熔断已接入；`data config init/preview/export/tui` 与 `BaseDataScheduler` 已支持 dry-run 和分发；Scheduler 常驻运行已支持状态文件、JSONL 事件日志、失败重试和健康检查命令；交易日历、缺口补采计划、Windows/Docker 部署模板和运维手册已接入 | 前端配置页和真实部署环境联调 |
| 原始响应审计 | 已完成基础版 | `RawRecordRepository`、`AshareP1Collector`、`AshareP2Collector`、`CryptoDataCollector` 已把成功和失败写入 `raw_records` | 后续新增 Provider 继续沿用同一归档模式 |
| 标准行情 | 生产化基础版完成 | `market_bars` hypertable；A 股 OHLCV Provider；数字货币 OHLCV Provider；A 股交易日历和数字货币 7x24 日历已写入 `market_calendars`，健康检查可生成 A 股/现货/合约 K 线补采任务 | 补真实批量缺口执行和前端数据质量展示 |
| 因子计算 | 候选池批量基础版已打通 | `IndicatorService` 已生成 `indicator_frames`；`FactorService` 已生成 `factor_frames`，并记录缺失因子组和部分可用因子组；`UniverseRecommendationPipeline` 已能按单一候选池批量编排指标和因子计算 | 补完整资金流、估值历史分位、事件衰减和衍生品窗口因子 |
| 初筛规则 | 基础版已打通 | `ScreeningService` 已支持因子快照存在性、因子可用性、可用因子组数量和严重风险阻断 | 扩展 ST/停牌退市、流动性阈值、极端波动和数据完整性规则 |
| 多维评分 | 基础版已打通 | `ScoringService` 已按 A 股/数字货币不同权重生成透明分数，记录缺失惩罚、置信度和分组贡献 | 补策略化权重、市场环境调整和更多风险扣分 |
| 信号快照 | 基础版已打通 | `SignalService` 已把 `factor_frames` 转成方向、分数、置信度和信号组解释，写入 `signal_snapshots` | 补信号冲突识别、证据引用和 Agent 输入上下文对接 |
| Agent 分析 | 主分析模型 Planner、高风险复核回写和圆桌模型节点开发已完成 | `PersonalFinanceAgentService` / `FinanceAssistantService` 已能调度持仓监控、观察池管理、推荐入池和推荐 Agent 决策 Workflow；6 个 LangGraph Workflow 已加入技术分析、因子分析、风险反驳、组合经理和记忆管理员圆桌观点，并在模型配置可用时生成真实模型观点，失败时 fallback；内部 Agent Loop 已支持真实模型按需调用事实工具、记忆和图谱；高风险复核批处理已能消费 `model_review` 并回写审计链 | 补真实模型统一联调和前端触达 |
| Agent 输入上下文 | 工具层基础版完成 | `AgentContextBuilder` 可从事实库组装单标的结构化上下文并写入 Redis TTL 缓存；`FinanceToolRuntime` 已新增 `factor.get_asset_factor_context`，可读取 `indicator_frames`、`factor_frames`、`asset_scores` 和 `evidence`；持仓监控、观察池管理和报告类 Workflow 已统一通过工具层读取因子/TA、信号风险、记忆、组合和观察池事实 | 接更多工具到真实模型上下文和前端查询 |
| 风险反驳 | 规则版已扩展 | `risk_findings`、`AshareRiskSentimentCollector`、龙虎榜/大宗/两融/涨停池、限售解禁和股权质押风险入库；停复牌主源失败时可用 ST/退市替代源补风险；股权质押已纳入回避池策略消费；持仓和观察池工作流已生成中文反方观点 | 风险扣分、证据引用、更多风险源和推荐报告反方观点 |
| 推荐排序 | 候选池批量基础版已打通 | `RecommendationService` 已能读取 `asset_scores`、`signal_snapshots`、`risk_findings` 和资产身份资料，写入推荐运行和单标的推荐；服务层和仓储层已加单市场校验；数据层已提供同市场候选池合并和回避池策略 | 推荐运行入口引用合并池和回避池策略 |
| 中文解释报告 | 模型观点明细模板已完成 | `build_chinese_decision_report` 已输出结构化 JSON 和 Markdown，覆盖执行摘要、裁决、行动计划、关键证据、圆桌观点、风险反驳、数据质量、Finance Memory、复核状态、模型路由和免责声明；圆桌章节已展示模型/规则来源、要点、反方和缺口 | 接前端渲染、导出和真实模型复核文本 |

## 3. A 股数据进度

| 优先级 | 能力 | 状态 | 说明 |
| --- | --- | --- | --- |
| P0 | AKShare 能力注册表 | 已完成 | 已登记 34 个 AKShare 股票接口，按推荐链路映射 |
| P0 | A 股资产和行情 | 部分完成 | `AshareP0Collector` 已把资产列表和 K 线接入 `raw_records`，东财失败后可走腾讯和 curl_cffi fallback |
| P0 | 原始响应归档 | 已完成基础版 | P1/P2 调用成功和失败均已进入 `raw_records` |
| P1 | 指数/行业/概念种子 | 已完成并可用 | `index_stock_info`、`index_stock_cons_csindex`、`stock_board_industry_name_em`、`stock_board_industry_cons_em`、`stock_board_concept_name_em` 和 `stock_board_concept_cons_em` 已纳入 Provider/能力注册；`universe_refresh` 会先读取指数/行业/概念目录再批量展开成分任务；成分接口主源失败后可降级到备用源或同花顺详情页首屏轻量 fallback |
| P1 | 个股资金流 | 已完成并可用 | `stock_individual_fund_flow_rank` 主源失败后可通过 `ths:curl_cffi:stock_fund_flow_individual` 轻量 fallback 写入资金流快照 |
| P1 | 个股新闻 | 已完成并可用 | `stock_news_em` 已写入 `event_records` 和 `evidence` |
| P1 | 公告披露 | 已接入任务分发 | `stock_notice_report` 已写入 `event_records` 和 `evidence`，`event_refresh` 会同时刷新新闻和公告 |
| P2 | 财报/业绩 | 已完成并可用 | 已接 `stock_yjbb_em`、`stock_yjkb_em`、`stock_yjyg_em`；AKShare 主源失败时可走 `eastmoney:curl_cffi` datacenter fallback，失败也会写入 `raw_records` |
| P2 | 财务指标 | 已完成并可用 | `stock_financial_analysis_indicator_em` 已写入 `fundamental_snapshots` |
| P2 | 估值/股息率 | 部分完成 | `stock_value_em` 已写入 `fundamental_snapshots`；`stock_a_gxl_lg` 已有 Provider，当前网络下可能超时 |
| P2 | 风险与情绪 | 已完成第一批并补交易状态替代源 | 已接 `stock_hot_rank_em`、`stock_zt_pool_em`、`stock_lhb_detail_em`、`stock_dzjy_mrmx`、`stock_margin_sse`、`stock_margin_szse`、限售解禁和股权质押；`stock_zh_a_stop_em` 当前上游断连时会归档错误，并继续尝试 ST、沪深退市、两网退市/暂停上市补充源 |

## 4. 数字货币数据进度

| 能力 | 状态 | 说明 | 下一步 |
| --- | --- | --- | --- |
| Binance/ccxt 基础 Provider | 生产化基础增强完成 | 已区分 Binance 原生和 ccxt 能力边界，并统一接入 `CryptoDataCollector` 与统一采集命令；ccxt 错误 payload 会标记 `rate_limited`，Binance 原生 fapi 支持备用端点轮换和 request trace | 扩展正式限流监控 |
| 现货/合约资产池 | 调度基础版完成 | markets 归一化已写入 `assets`、`asset_profiles`、`asset_provider_mappings`、`asset_universes`、`asset_universe_members`，成功和失败都进 `raw_records`；`crypto_spot.universe.binance` 与 `crypto_future.universe.binance` 已可由配置导出 | 接更多交易所或自定义币池 |
| OHLCV 行情 | 生产化基础增强完成 | ccxt OHLCV 已通过 `CryptoDataCollector` 写入 `market_bars` 和 `raw_records`；现货/合约 7x24 日历已写入 `market_calendars`，健康检查可生成现货/合约 K 线补采任务 | 接真实批量缺口执行和限流监控 |
| 合约衍生品快照 | 已完成基础版 | 资金费率、未平仓量、多空比已通过 `CryptoDataCollector` 落表并绑定 `raw_record_id` | 与因子服务对接，生成拥挤度和风险因子 |
| 链上/宏观事件 | 未开始 | 暂未接入 | 后续作为事件和风险增强，不影响第一版推荐闭环 |

## 5. 数据库进度

| 迁移 | 状态 | 内容 |
| --- | --- | --- |
| `20260514_0001_create_m0_schema.py` | 已应用 | M0 推荐主链路表、TimescaleDB hypertable、候选池、行情、因子、推荐、Agent 审计 |
| `20260514_0002_create_crypto_derivative_snapshots.py` | 已应用 | 数字货币衍生品快照表 |
| `20260515_0003_create_ashare_m1_tables.py` | 已应用 | A 股财务估值、资金流、事件记录表 |
| `20260517_0004_create_personal_assistant_m2_tables.py` | 已应用 | 私人金融助手 M2 表：组合、持仓、观察池、提醒、决策日志、Finance Memory、复盘和 Workflow 审计 |
| `20260517_0005_create_m3_reliability_tables.py` | 已应用 | M3 数据可靠性表：组合历史快照、持仓历史快照、观察池事件和数据质量快照；快照表使用 TimescaleDB hypertable |
| `20260518_0006_create_assistant_trigger_events.py` | 已应用 | V1.2 触发事件表：触发类型、来源、冷却去重、派发状态、Agent 任务和建议内部 Workflow |
| `20260519_0008_create_chat_memory_tables.py` | 已应用 | CLI 聊天记忆表：聊天会话、消息流水、会话恢复和历史查询 |
| `20260521_0009_create_model_runtime_config_tables.py` | 已应用 | 模型运行时配置表：模型供应商、模型实例、路由规则和检索 profile |
| `20260529_0010_create_asset_detail_tables.py` | 已应用 | 资产详情附表：`asset_profiles`、`asset_provider_mappings`、`asset_status_snapshots`、`realtime_quote_snapshots`；`assets` 仅保留稳定身份主表职责 |
| `20260614_0019_create_backtest_tables.py` | 已应用 | 轻量回测结果表：`backtest_results`，保存策略窗口、绩效指标、数据版本和回测 payload |

私人金融助手新增表当前状态：

| 表 | 当前状态 | 说明 |
| --- | --- | --- |
| `portfolios` / `positions` | 已迁移并冒烟 | 支撑持仓监控、成本、盈亏和组合风险 |
| `portfolio_snapshots` / `position_snapshots` | 已迁移并冒烟 | 支撑组合和持仓历史追踪，作为长期波动监控、回撤复盘和换股判断的数据地基 |
| `watchlists` / `watchlist_items` / `asset_theses` | 已迁移并冒烟 | 支撑私人观察池、关注理由、启动条件、失效条件和状态流转 |
| `watchlist_item_events` | 已迁移并冒烟 | 支撑观察项入池、升级、剔除和推荐同步的生命周期追踪 |
| `data_quality_snapshots` | 已迁移并冒烟 | 支撑推荐报告说明行情、因子、资金流、基本面等数据新鲜度和缺口 |
| `monitoring_alerts` | 已迁移并冒烟 | 支撑实时/定时触发提醒 |
| `assistant_trigger_events` | 已迁移并冒烟 | 支撑 V1.2 Trigger Engine，把已入库事实变化转成 pending/dispatched/skipped 触发事件 |
| `decision_logs` | 已迁移并冒烟 | 支撑建议、用户确认、用户反馈和证据追溯 |
| `assistant_memories` / `review_tasks` | 已迁移并冒烟 | 支撑 Finance Memory 和复盘，不保存 Hermes 通用记忆 |
| `assistant_chat_sessions` / `assistant_chat_messages` | 已迁移并冒烟 | 支撑 CLI 聊天窗口跨进程恢复，不替代 Finance Memory，也不作为金融事实源 |
| `model_providers` / `model_instances` / `model_routing_rules` / `retrieval_profiles` | 已迁移并冒烟 | 支撑类似 Dify 的模型供应商入库、在线切换模型路由，以及 Finance Memory / RAG 检索 profile 配置 |
| `memory_embeddings` | 已迁移并写入 | `MemoryService.upsert_memory`、用户反馈、复盘结果和每日关注原因都会同步写入 embedding；用于相似历史场景、观察理由和复盘教训检索 |
| `financial_memory_edges` | 已迁移并冒烟，转为历史兼容 | 既有 M2/M3 关系边表继续保留；长期知识图谱改由 GraphStore 承载，默认 Neo4j / DozerDB，可选 Apache AGE |
| `agent_workflow_runs` / `agent_workflow_events` | 已迁移并冒烟 | 支撑主 Agent 调度审计和前端办公室状态 |

当前推荐链路市场边界：

- A 股和数字货币可以共用表结构、仓储和服务接口，但候选池、初筛、评分、信号和推荐运行必须按市场分别执行。
- `mixed` 只允许作为信号方向里的“多空冲突”枚举，不允许作为候选池或推荐运行市场。
- 历史数据库中保留 2 个 mixed 冒烟候选池用于追溯；新脚本和服务不再新增 mixed 候选池。

当前实测状态：

| 表 | 当前验证结果 | 说明 |
| --- | --- | --- |
| `raw_records` | 已有 81 条 | A 股与数字货币 Provider 成功和失败响应均已归档，fallback 使用情况会写入 `actual_source`、`fallback_trace` 和必要的覆盖范围说明 |
| `event_records` | 已有 13 条 | 来自个股新闻、人气榜和涨停池等事件 |
| `evidence` | 已有 6 条 | 新闻、龙虎榜和大宗交易证据可供推荐解释引用 |
| `risk_findings` | 已有 11 条 | 已写入龙虎榜、大宗交易、两融和涨停池过热风险 |
| `asset_universe_members` | 行业/概念冒烟池各 5 条 | 当前通过同花顺详情页首屏轻量 fallback 写入候选池种子 |
| `asset_universe_members` | Binance 现货冒烟池 5 条 | 当前通过 ccxt markets 写入币种候选池种子 |
| `asset_universe_members` | 统一采集样例池各 2 条 | `collect_base_data.py` 已验证 A 股 P0、P1、风险情绪和 crypto 样例候选池 |
| `asset_universes` / `asset_universe_members` | 单市场拆分已验证 | 当前候选池共 18 个；已新增 A 股批量推荐冒烟池和数字货币批量推荐冒烟池；新拆分路径不再写入 mixed |
| `capital_flow_snapshots` | 17 条 | 个股资金流 5 日榜已通过同花顺轻量 fallback 写入 |
| `fundamental_snapshots` | 9 条 | 平安银行财务指标 3 条、估值 3 条、业绩报表 3 条已通过 P2 冒烟写入 |
| `market_bars` | A 股和 BTCUSDT K 线已写入 | 当前 `market_bars` 共 100 条 |
| `indicator_frames` | 技术指标已写入 | 当前 4 条指标快照，A 股批量样例和数字货币批量样例均已覆盖 RSI、MACD、ATR、布林带、均线、收益率、波动率和回撤等基础指标 |
| `factor_frames` | 因子快照已写入 | 当前 4 条因子快照；批量流水线已验证 A 股和数字货币均可分别生成因子 |
| `screening_results` / `screening_result_items` | 冒烟候选池初筛已写入 | 当前 `screening_results` 10 条、`screening_result_items` 14 条；A 股和数字货币批量候选池均已通过第一版硬过滤 |
| `asset_scores` | 透明评分已写入 | 当前 5 条；A 股和数字货币批量流水线均已生成候选池内排序 |
| `signal_snapshots` | 信号快照已写入 | 当前 4 条；A 股和数字货币批量样例均已写入信号方向、信号分和置信度 |
| `recommendation_runs` / `recommendation_run_universes` | 推荐运行已写入 | 当前各 8 条；最新 A 股运行和数字货币运行均为 `available` |
| `asset_recommendations` | 单标的推荐已写入 | 当前 12 条；A 股和数字货币批量流水线均已生成 2 条推荐结果 |
| `crypto_derivative_snapshots` | BTCUSDT 快照已写入 | 当前 4 条衍生品快照带 `raw_record_id`，可追溯到 Binance 原生公开接口响应 |
| `portfolios` / `positions` | 组合和持仓已写入 | 当前 `portfolios` 2 条、`positions` 2 条 |
| `portfolio_snapshots` / `position_snapshots` | 历史快照已写入 | 当前 `portfolio_snapshots` 2 条、`position_snapshots` 2 条 |
| `watchlists` / `watchlist_items` / `asset_theses` | 私人观察池已写入 | 当前 `watchlists` 3 条、`watchlist_items` 3 条、`asset_theses` 2 条 |
| `watchlist_item_events` | 观察池事件已写入 | 已覆盖观察池管理流转、推荐结果入池、Agent 入池原因 `candidate_intake_reason` 和每日继续关注原因 `daily_watch_reason` |
| `data_quality_snapshots` | 数据质量快照已写入 | 当前 1 条；已验证资产级数据新鲜度和缺口记录 |
| `monitoring_alerts` / `decision_logs` | 提醒和决策已写入 | 当前 `monitoring_alerts` 8 条、`decision_logs` 9 条 |
| `assistant_memories` / `memory_embeddings` / `financial_memory_edges` / `review_tasks` | 记忆、向量、历史兼容关系和复盘已写入 | 已写入 `candidate_intake_reason`、`watchlist_daily_reason`、`user_feedback`、`review_result` 等记忆；向量召回会综合语义相似度、置信度和时间衰减；反馈和复盘会反写源记忆；既有边表关系保留，GraphSync 已能投影到配置选择的 GraphStore，并支持相似历史决策、风险传导和冲突查询 |
| `assistant_chat_sessions` / `assistant_chat_messages` | 聊天记忆已写入 | 已验证 `finance-agent chat` 返回 `chat_session_id`，指定 `--session-id` 后可读取历史消息 |
| `agent_workflow_runs` / `agent_workflow_events` | Workflow 审计已写入 | 当前已通过 Workflow、触发层和内部 Agent Loop 冒烟写入多类运行审计 |

当前 Agent 决策层冒烟状态：

| 工作流 | 当前状态 | 输出 |
| --- | --- | --- |
| `RecommendationDecisionWorkflow` | 圆桌基础版已完成 | `smoke_m4_agent_decision_workflow.py` 已验证 Agent 根据高分推荐、弱持仓、高风险和历史记忆输出 `swap`；`smoke_roundtable_workflow.py` 已验证 LangGraph 节点流转和技术分析、因子分析、风险反驳、组合经理、记忆管理员圆桌观点 |
| `FinanceToolRuntime` | 因子、记忆与图谱工具已接入 | `smoke_finance_tool_factor_context.py` 已验证 `factor.get_asset_factor_context` 可读取 TA 指标、因子快照、多维评分和证据；`smoke_finance_memory_tools.py` 已验证 `memory.get_asset_memory_context`、`memory.get_asset_memory_timeline` 和增强后的 `memory.recall_asset_memories`；`smoke_graph_store.py` 已验证图谱工具可通过 GraphStore 查询 |
| `FinanceAgentInterface` | CLI/MCP 共用门面已完成 | 已统一封装 Workflow 清单、Workflow 运行、审计查询、报告读取和只读事实工具调用；`recommendation_decision`、`portfolio_monitoring`、`watchlist_management` 可自动从数据库组装 DTO |
| CLI 入口 | 已完成 | `finance-agent` / `python -m finance_agent.cli` 已支持 `workflows list/run/show`、`reports show`、`tools list/call`、`memory recall/timeline`、`graph health/init/sync-asset/sync-owner/sync-all/trace/reason-chain/similar-decisions/risk-contagion/conflicts`，默认输出结构化 JSON |
| MCP Server | 已完成并完成 Hermes 冒烟 | `finance-agent-mcp` / `python -m finance_agent.mcp_server` 已暴露 `list_workflows`、`run_workflow`、`get_workflow_run`、`get_report`、`list_tools`、`call_tool`、2 个 memory 工具和 9 个 graph 工具；本地已验证 `mcp 1.27.1` 可以创建 server，并通过 `smoke_mcp_handshake.py` 完成 initialize/list_tools 握手 |
| `ModelRoutingPolicy` | 路由协议已完成 | `smoke_model_router.py` 已验证常规分析路由到 `deepseek-v4-pro`，高风险复核路由到 `gpt-5.5-pro` |
| 模型配置 CLI/TUI | DB 配置中心基础版已完成 | `finance-agent models init/list/set-provider/set-model/set-route/set-retrieval/retrieval/config/route-preview/test/tui` 已支持模型供应商入库、模型实例入库、路由规则在线切换、检索 profile 入库、JSON/env 调试回退、脱敏输出、路由预览、dry-run 请求预览和脚本化轻量 TUI；`smoke_model_cli_and_tui.py` 已验证数据库路由覆盖默认双模型 |
| CLI 聊天窗口 | 聊天记忆基础版已完成 | `finance-agent chat --owner-id ...` 已支持交互式 stdin 聊天、`--message` 脚本化多轮对话、`--session-id` 续聊、`--new-session` 新建会话和 `/history` 历史查看，当前可路由 Workflow 清单、工具清单、模型配置、模型路由预览、历史查看和退出意图 |
| CLI 聊天模型循环 | 已完成 | `smoke_agent_chat_model_tool_loop.py` 已验证普通自然语言问题可进入模型聊天，模型按需调用 `memory.find_memory_conflicts` 等只读工具后返回中文摘要；模型不可用时自动降级为能力说明 |
| 聊天摘要服务 | 已完成 | `ChatSummaryService` 已支持长会话摘要抽取、非法 JSON 一次重试、会话 summary 回写和 `assistant_memories(memory_type=chat_summary)` 沉淀；当前保持独立服务入口，不自动触发真实模型调用 |
| `FinanceAssistantService.run_workflow` | 统一调度基础版已完成 | `smoke_workflow_registry_and_builders.py` 已验证服务、工具和构建器均暴露 6 个 Workflow；`smoke_finance_assistant_workflow_dispatch.py` 已验证推荐决策统一调度会写入节点、圆桌、模型路由、高风险复核和报告审计 |
| 运营类 Workflow | 圆桌基础版已完成 | `smoke_portfolio_watchlist_roundtable_workflows.py` 已验证 `portfolio_monitoring`、`watchlist_management` 均会调用组合/观察池、因子/TA、信号风险和记忆工具，并落库圆桌观点、高风险复核和完整中文报告；旧的规则版闭环冒烟仍通过 |
| 报告类 Workflow | 模型增强圆桌和完整报告模板已完成 | `smoke_roundtable_report_workflows.py` 已验证 `asset_deep_analysis`、`swap_decision`、`daily_review` 均会调用组合、观察池、推荐、信号风险、因子/TA 和记忆工具，并落库圆桌观点、高风险复核和完整中文报告；`smoke_report_template_and_model_review.py` 已验证报告字段、模型路由和复核审计；04 方案新增 fake model client 和禁用模型配置 smoke 验证模型增强与 fallback |
| `WatchlistManagementWorkflow` | 已完成基础版 | `smoke_watchlist_management_workflow.py` 已验证持续关注标的写入 `daily_watch_reason` 事件和 `watchlist_daily_reason` 记忆 |
| `TriggerService` | V1.2 基础版已完成 | `smoke_v12_trigger_events.py` 已验证持仓回撤、信号转弱、观察池条件、推荐运行、风险事件、数据质量异常 6 类触发，支持冷却去重和 Agent 唤醒派发 |
| `InternalFinanceAgentLoopRunner` | 模型增强与图化轮询已完成 | `smoke_internal_agent_loop.py` 已验证已派发触发事件能被内部 Agent Loop 消费、按需调用 Domain Workflow、回写 `agent_loop_status/workflow_run_id`，并避免重复处理；`smoke_internal_agent_loop_graph_and_daemon.py` 已验证 LangGraph 图入口、`agent_node_trace` 和 `agent run-loop --max-iterations 1`；`smoke_real_model_agent_loop_planner.py` 已验证模型可先请求因子/图谱工具，再选择 `asset_deep_analysis` |
| Workflow 层设计 | 已完成新方案记录 | `docs/Workflow层设计.md` 已明确不自研 AI Workflow 框架，底层固定金融团队 Workflow 使用 LangGraph；实施拆解已写入 `docs/superpowers/plans/2026-05-18-Hermes上层运行时与Workflow层落地.md` |

当前 CLI / MCP 工具入口验证：

| 验证项 | 当前状态 | 说明 |
| --- | --- | --- |
| CLI 与接口层清单一致 | 已通过 | `smoke_agent_cli_interface.py` 验证 Workflow 清单和工具清单 |
| Hermes Skill 命令冒烟 | 已通过 | `smoke_hermes_skill_commands.py --mode direct` 和 WSL `--mode wsl-bridge` 均已验证 Workflow/工具清单、模型配置脱敏、图谱健康、Agent 单轮消费、单标的分析和 Markdown 报告读取 |
| Hermes MCP 握手冒烟 | 已通过 | `smoke_mcp_handshake.py` 已启动 MCP Server 子进程并完成 initialize/list_tools，确认 MCP wrapper 工具 20 个、内部事实工具 15 个且均为只读 |
| CLI 运行圆桌报告 Workflow | 已通过 | 已通过 `finance_agent.cli workflows run asset_deep_analysis` 生成结构化 JSON 和中文报告 |
| MCP Server 创建 | 已通过 | `smoke_agent_mcp_server.py` 在安装 MCP SDK 后可以创建 server |
| GraphStore 图谱入口 | 已通过 | `smoke_graph_store.py` 已验证 schema/health、资产/用户同步、路径追踪、原因链、相似决策、风险传导、冲突查询、CLI graph 命令和 MCP graph 工具注册 |
| 数据同步配置 CLI/TUI | 已通过 | `smoke_data_sync_config_cli_and_tui.py` 已验证配置生成、校验、预览、导出和脚本化 TUI，默认生成 12 个任务且不要求手填股票；A 股 Universe 导出会携带目录展开参数 |
| 数据同步调度分发 | 已通过 | `smoke_data_sync_scheduler_dispatch.py` 已验证 `BaseDataScheduler` dry-run 和 `collect_base_data.py` 的 `sync_task_type` 分发；`smoke_ashare_universe_catalog_dispatch.py` 已验证 A 股指数/行业/概念目录会展开为多任务 |
| MCP SDK 安装 | 已通过 | 清华镜像 SSL 断连后，已使用官方 PyPI 安装 `mcp 1.27.1` |
| 模型 CLI/TUI | 已通过 | `smoke_model_cli_and_tui.py` 验证 JSON/env 配置、数据库模型供应商、模型实例、路由规则、检索 profile、路由预览、dry-run 测试和脚本化 TUI；默认不真实请求外部模型 |
| CLI 聊天窗口 | 已通过 | `smoke_agent_chat_cli.py` 验证 `finance-agent chat` 可以脚本化多轮对话、查询 Workflow、查看脱敏模型配置并识别退出意图 |
| CLI 聊天记忆 | 已通过 | `smoke_agent_chat_memory.py` 验证聊天会话和消息落库、`chat_session_id` 返回、`--session-id` 续聊和 `/history` 历史查看 |
| CLI 聊天模型工具循环 | 已通过 | `smoke_agent_chat_model_tool_loop.py` 验证模型可按需调用只读事实工具并返回中文摘要 |
| 内部 Agent 真实模型 Planner | 已通过 | `smoke_real_model_agent_loop_planner.py` 验证模型可先调因子和图谱工具，再选择 Domain Workflow |

当前 A 股风险/情绪采集状态：

| 接口 | 当前状态 | 落库说明 |
| --- | --- | --- |
| `stock_hot_rank_em` | 可用 | 写入热度候选池、事件；若 AKShare 普通请求断连，可走 `eastmoney:curl_cffi:stock_hot_rank_em` fallback；报价段断连时会降级为 `rank_only` 热度种子 |
| `stock_zt_pool_em` | 可用 | 写入涨停池候选池、事件和短线情绪拥挤风险 |
| `stock_lhb_detail_em` | 可用 | 写入龙虎榜风险和证据 |
| `stock_dzjy_mrmx` | 可用 | 写入大宗交易风险和证据 |
| `stock_margin_sse` | 可用 | 写入上交所市场级两融杠杆风险 |
| `stock_margin_szse` | 可用 | 写入深交所市场级两融杠杆风险 |
| `stock_zh_a_stop_em` | 主源可能失败，替代源已接入 | AKShare 和仓库侧 `curl_cffi` fallback 被 Eastmoney `clist/get` 上游断开时会写入错误归档，同时继续尝试 ST、沪深退市、两网退市/暂停上市补充源，替代源可用时返回 `partial` |

当前真实数据源冒烟状态：

| 数据源 | 当前状态 | 说明 |
| --- | --- | --- |
| AKShare A 股行情 | 可用 | `smoke_providers.py` 已拉到 3 条样例 K 线，并写入 A 股独立候选池 |
| Binance/ccxt 数字货币行情 | 当前可能被限流，限流识别已接入 | Binance 返回 418 时会结构化输出错误和 `rate_limited`；Binance 原生 fapi Provider 会尝试备用端点，不影响市场边界验证 |

当前候选池级推荐流水线验证：

| 市场 | 候选池 | 当前状态 | 输出 |
| --- | --- | --- | --- |
| A 股 | `universe:smoke:ashare:batch` | `available` | 2 个成员、2 条指标、2 条因子、2 条评分、2 条信号、2 条推荐 |
| 数字货币现货 | `universe:smoke:crypto_spot:batch` | `available` | 2 个成员、2 条指标、2 条因子、2 条评分、2 条信号、2 条推荐 |

当前缓存层状态：

| 能力 | 当前验证结果 | 说明 |
| --- | --- | --- |
| Redis compose 服务 | 已配置 | `docker compose up -d redis` 可启动 |
| 缓存端口 | 已完成基础版 | `CacheClient`、`LockClient` 定义在 `ports/cache.py` |
| Redis 实现 | 已完成基础版 | `RedisCacheClient` 支持 JSON 缓存和轻量锁 |
| 缓存工厂 | 已完成基础版 | `create_cache_client` 支持 `auto`、`redis`、`null` 三种模式；本地命令可自动降级，生产调度建议显式使用 `redis` |
| 采集任务锁 | 已接入统一命令 | `CollectionRuntime` 为同参数采集生成 `base_data_collect:*` 锁，避免重复刷新同一接口 |
| Provider 熔断 | 已接入统一命令 | Provider 连续失败后会写入 `provider_state:*`，冷却期内任务返回 `skipped`；`--force-provider` 可强制重试 |
| 基础数据调度器 | 生产化基础增强完成 | `run_base_data_scheduler.py` 支持数据同步配置或已导出的调度计划、全局开关、单任务开关、任务频率、`print-plan`、`run-once`、`loop`、dry-run、状态文件、JSONL 事件日志、失败重试和 `--health-check`；`collect_base_data.py` 已支持 `calendar_refresh`，健康检查可输出 `backfill_jobs` |
| Agent 上下文缓存 | 已完成骨架 | `AgentContextBuilder` 支持单标的上下文构建和 TTL 缓存 |
| 基础数据健康检查 | 已完成生产化基础版 | `scripts/data/check_base_data_health.py` 可只读 PostgreSQL + Redis，汇总表计数、最近 Provider 状态、熔断状态、交易日历/K 线等数据缺口，并输出可转调度的 `backfill_jobs` |

## 6. 最近提交

| 提交 | 说明 |
| --- | --- |
| `4e38a8c feat(decision): 增加决策反馈API` | 新增待确认决策查询和用户反馈写回 API，反馈会沉淀 Finance Memory 并回写原决策状态 |
| `e32bb14 feat(roundtable): 渲染模型圆桌观点明细` | 中文报告圆桌章节透传并渲染模型来源、要点、反方和数据缺口 |
| `b488e47 feat(roundtable): 接入Workflow模型观点增强` | 6 个 Workflow 接入“规则观点 -> 模型增强 -> fallback”层，并保留禁用模型 smoke 回归 |
| `afdccfd feat(roundtable): 增加圆桌模型观点节点骨架` | 新增圆桌模型节点、Prompt 和 fake model client 测试，支持 JSON 校验、证据白名单和 fallback |
| `1101c8c feat(chat): 增加聊天摘要沉淀服务` | 新增聊天摘要服务和测试，支持摘要回写会话与沉淀 `chat_summary` 记忆 |
| `65fecbd feat(review): 接入高风险复核调度任务` | 新增高风险复核调度任务类型、after Agent 和兜底 sweep 任务 |
| `0ec260f feat(review): 增加高风险复核CLI入口` | 新增 `agent review-pending` CLI，支持 dry-run 查看待复核队列 |
| `2686f1e feat(review): 增加高风险复核服务` | 新增高风险复核服务，支持结构化模型复核、降级和回写 |
| `c43d9c3 新增金融助手CLI和MCP入口` | 新增 `FinanceAgentInterface`、CLI、MCP Server 和入口冒烟脚本 |
| `81ff3a3 拆分A股和数字货币推荐链路` | 推荐链路按市场拆分，禁止 mixed 候选池和跨市场推荐运行 |
| `145a8ae 完成数据层推荐结果闭环` | 推荐结果运行表和单标的推荐结果已写入 |
| `816d7cf 打通初筛评分与信号基础链路` | 初筛、透明评分和信号快照服务已接入 |
| `2f7e03a 打通指标与因子基础计算链路` | TA-Lib 指标和第一版因子快照已落库 |
| `0406ca8 统一数字货币数据归档链路` | 新增 `CryptoDataCollector`，统一 markets、OHLCV 和衍生品快照 raw 归档 |
| `02ad253 补齐 A 股行业概念成分备用源` | 新增同花顺详情页首屏轻量 fallback，并把概念池写入 P1 冒烟 |
| `57b74d8 增强 AKShare P1 采集归档链路` | 新增 `AshareP1Collector`，P1 调用成功/失败写入 `raw_records` |
| `0878579 feat: add ashare m1 data pipeline` | 新增 A 股 M1 表、P1 Provider 和冒烟脚本 |
| `023ba76 feat: register AKShare capabilities and fallback` | 新增 AKShare 能力注册表和行情 fallback |
| `6bdf423 docs: clarify universe sources versus recommendations` | 澄清候选池来源和推荐结果边界 |
| `f78ad40 docs: map AKShare capabilities to recommendation pipeline` | 映射 AKShare 能力到推荐链路 |

## 7. 当前阻塞和风险

| 问题 | 影响 | 当前处理 |
| --- | --- | --- |
| 部分东方财富板块成分和交易状态接口被上游断连 | 全量板块成分和停复牌/退市状态可能需要降级 | 行业/概念已补同花顺详情页首屏轻量 fallback；指数成分已提供多接口 fallback；`stock_zh_a_stop_em` 失败时会继续尝试 ST/退市替代源并记录 fallback trace |
| 个别 P2 财报接口可能超时 | 业绩报表、快报、预告偶发不能通过 AKShare 主源刷新 | 已补 `eastmoney:curl_cffi` fallback，并保留错误归档 |
| 数据层仍是基础版 | 推荐链路已经能从行情进入因子、初筛、评分、信号快照和推荐结果，但还不能支撑大规模候选池、完整因子口径和生产级推荐策略 | 已实现确定性数据层闭环；后续分别补 A 股链路的历史分位/资金流/事件衰减，以及数字货币链路的衍生品窗口/流动性/事件因子 |
| 专业量化模型未实现 | 不影响第一版推荐闭环，但后续无法做统计 alpha 学习、风格中性化和组合优化 | 已标记为 TODO；第一版不让 LLM 替代统计验证，只让 LLM 消费因子和证据做解释、比较和风险反驳 |
| 调度器真实部署待联调 | 常驻运行、Windows 任务计划模板、Docker 调度器模板和运维手册已补齐；当前 Windows 任务计划真实注册受本机会话权限限制，Docker 模板已完成构建验证 | 后续在具备目标机器权限后执行 Windows 任务计划真实注册，或按实际生产环境接 Docker/服务化部署 |
| GitNexus 批次 0 后需重建索引 | 当前索引可用但仍指向批次 0 提交前基线；P0-1 已完成分组提交，后续批次前必须由总负责人在 WSL 重建图谱 | P0-4 停下并提醒总负责人执行 `npx gitnexus analyze` |

## 8. 下一步执行顺序

1. 同步 Hermes + LangGraph 新方案到 `docs/架构方案.md`、`docs/项目计划.md` 和 `docs/模型选型与职责分配.md`，避免旧的 `PersonalFinanceAgent` 表述继续扩散。
2. 将 `PersonalFinanceAgentService` 保留为兼容入口，并新增目标类名 `FinanceAssistantService`，后续工具入口优先依赖新类名。（已完成基础版）
3. 建立 `FinanceToolRuntime`：统一封装持仓、观察池、推荐、信号、风险、数据质量和 Finance Memory 查询工具，工具只读已入库清洗数据。（已完成基础版）
4. 建立 LangGraph Workflow 适配层：使用 LangGraph 负责编排底层固定金融团队 Workflow，本项目只负责 DTO、工具端口、审计落库、fallback 和高风险复核。（已完成基础版）
5. 把 `RecommendationDecisionWorkflow` 从确定性规则 fallback 升级为 LangGraph + 工具调用决策节点，保留当前 DTO、落库链路和 smoke 场景。（已完成圆桌基础版）
6. 补齐 `asset_deep_analysis`、`swap_decision`、`daily_review` 圆桌报告基础版，统一调用已入库数据工具并落库报告、复核和圆桌审计。（已完成基础版）
7. 建立模型路由与高风险复核协议：按 `docs/模型选型与职责分配.md` 接 DeepSeek V4 Pro 日常分析和 GPT-5.5 Pro 卖出、换股、大仓位调整、强风险冲突复核；当前已完成协议、审计事件、模型配置 CLI/TUI 和 dry-run 测试入口，真实模型调用后续接模型客户端。
8. 补中文解释报告完整模板：推荐报告、持仓建议报告、换股比较报告和每日复盘报告，优先消费 Agent 决策、推荐结果、观察池事件、数据质量快照和 Finance Memory。（已完成基础模板）
9. 接实时/准实时触发：行情波动、持仓盈亏、观察池触发条件、风险事件先进入 `assistant_trigger_events`，再唤醒 Hermes-Agent 或内部金融 Agent 进行 loop 决策；Workflow 只作为 Agent 按需调用的内部分析工具。（已完成调度器常驻评估与 Agent 消费接入）
10. 接 Scheduler 工具入口、内部 `InternalFinanceAgentLoop` 和前端办公室状态页，让私人金融助手可以被长期唤起和持续监控。（CLI/MCP 基础入口、内部 Loop 图化、`run-loop` 基础版和 Scheduler 常驻唤醒已完成，下一步接前端办公室状态页）
11. 增加因子规格配置，补完整资金流、估值、基本面、事件和数字货币衍生品窗口因子。
12. 将推荐运行入口接入同市场合并池和回避池，保证推荐前先完成多来源合并、风险剔除和市场边界校验。
13. 按实际部署环境补基础数据调度器的容器/systemd/Windows 任务计划模板，并接入前端或监控面板展示状态文件、事件日志和 `backfill_jobs`。
14. 推荐和持仓闭环稳定后，再启动量化模型研究 TODO：因子有效性、机器学习打分、时间序列模型、风格暴露、风险模型和参数搜索。
